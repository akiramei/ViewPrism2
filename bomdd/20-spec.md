# 仕様書 — ViewPrism2 (BOM v4.0)

> 製造パッケージに含まれる(製造装置が読む)。REQ への双方向トレースを保つ。
> 原典: view-prism(TypeScript/Electron)のリバース仕様。原典ソースは工場非開示のため、本書が機能記述の唯一の典拠。
> 版: v4.0(loop-v4-repair-lifecycle 改訂 — §2.11 修復ライフサイクル・REQ-068〜072 追加・状態機械 T1〜T8・
> OC-19〜22・golden G-10・INV-013/014/015 追加・INV-009(物理非破壊)の第 2 の実アクション=復元/完全削除に
> L3 物理差分 CP(S-26)を適用・§4 へ V4 行追加・G2 で B-1(relink 候補 status=pending∪untagged-normal)/B-2(復元の存在分岐)を裁定。
> §2.0〜2.10 の確定分は loop-v1-r1〜loop-v3-r1 の凍結オラクルで保護され、意味論の変更はない)
> 旧版: v3.0(loop-v3-similarity — §2.10 類似検索+マージ・REQ-061〜067・OC-14〜18・golden G-9・INV-011/012・マージ L3 物理差分)

## 1. 概要と用語

ViewPrism2 は Windows 向けデスクトップ画像管理アプリケーション(.NET 10 / Avalonia UI 12 / SQLite)。
中核コンセプトは「タグ × 仮想ビュー」: 物理フォルダ階層に依存せず、タグ付けした画像を
ユーザー定義の仮想階層(ビュー)でフォルダのように辿る。

| 用語 | 定義 |
|---|---|
| 同期フォルダ | ユーザーが登録する物理フォルダ。スキャン対象の根。原典では「コレクション」とも呼ぶ |
| 画像(image) | 同期フォルダ配下で発見された画像ファイルの DB レコード。実体はファイルシステム上に残る(取り込みコピーはしない) |
| ステータス | 画像の状態: normal(正常)/missing(物理消失)/deleted(論理削除)/pending(再リンク候補確定待ち) |
| タグ | 画像に付与する分類子。simple(値なし)/textual(文字列値)/numeric(数値)の 3 種 |
| ビュー | 絞り込み条件・ソート・表示列・タグ階層を束ねた保存可能な仮想フォルダ定義 |
| タグ階層(hierarchy) | ビュー内に定義する木構造。各ノードがタグ(+値条件)を参照する |
| NodeGraph | タグ階層と実際のタグ付け状態から構築される、ユーザーが辿る展開済みノード木 |
| 再リンク | missing 画像と新発見ファイル(pending)を同一物として結合し、タグ等の関連を保持する操作 |
| 表示モード(ビューア) | ビューアの画像配置方式: normal(単一)/scroll(縦連続)/spread-right(右開き見開き)/spread-left(左開き見開き)(v2.0) |
| ページ送り | 見開きモードで現在位置を進める/戻す操作。ステップは 2(doublePage 既定)または 1(singlePage / SHIFT 併用)(v2.0) |
| pHash(知覚ハッシュ) | 画像の見た目を表す 64bit の指紋。DCT ベースで算出し、似た画像は近い値になる(v3.0) |
| ハミング距離 | 2 つの pHash の異なるビット数(0〜64)。小さいほど似ている。類似度%へ変換する(v3.0) |
| 類似検索 | 基準画像 1 枚に対し、pHash の近い画像を同一コレクション内から類似度順に集める操作(v3.0) |
| マージ | 重複画像を 1 枚に統合する論理操作。タグを集約しマージ元を deleted にする。物理ファイルは触れない(v3.0) |

## 2. 機能仕様

### 2.0 論理データモデル (REQ-001〜REQ-005)

**DB 基盤(REQ-003〜005 — v2.0 で本文へ明記。意味論は v1 から不変)**:
- DB ファイルは `%APPDATA%/ViewPrism2/data/viewprism.db` に配置する (REQ-005)
- 接続は journal_mode=WAL・foreign_keys=ON で開く(接続毎に PRAGMA を明示発行) (REQ-003)
- スキーマ変更はマイグレーション基盤で管理する: migrations テーブル(id TEXT PK, executed_at)に適用履歴を
  記録し、未適用分のみを id 順にトランザクション内で適用する。新規 DB は最新スキーマで作成して全
  マイグレーション id を適用済みとしてマークする (REQ-004)

エンティティと関係(物理スキーマは E-BOM/M-BOM で確定。カラム名は原典準拠):

- **sync_folders** 1—n **images**(フォルダ削除で画像も削除)
- **images** n—n **tags**(中間 **image_tags**、PK=(image_id, tag_id)、value 列に値)
- **tags** は自己参照 parent_id(単一親階層)。type 別設定として textual_tag_settings(predefined_values JSON 配列)、numeric_tag_settings(min/max/step/unit)
- **views** 1—n **view_conditions**(絞り込み条件)、1—n **view_tag_hierarchies**(階層ノード、自己参照 parent_id)
- **(v3.0)** **images** 1—1 **image_features**(pHash 特徴量。image_id PK、FK→CASCADE)、**images** n—n **image_similarity**(画像ペアの類似度キャッシュ。image_id1/image_id2 とも FK→CASCADE)。**列定義・索引・cache_key 生成規則は §2.10.3**。両テーブルはマイグレーション基盤(REQ-004)で追加(§2.10 REQ-063)。L2 スキーマ検査は v3.0 の 2 テーブルを含めて新規作成 DB と照合する
- ID は全エンティティ UUIDv4 小文字(REQ-001)。日時は ISO 8601 UTC 文字列 `YYYY-MM-DDTHH:mm:ss.fffZ`(REQ-002)
- 参照整合性は FK で固定: images.sync_folder_id → CASCADE / image_tags 両 FK → CASCADE /
  view_conditions.view_id → CASCADE, .tag_id → SET NULL / view_tag_hierarchies.view_id, .tag_id → CASCADE,
  .parent_id(自己参照)→ SET NULL / tags.parent_id → SET NULL
- 核/表面: core
- 受入観点: スキーマ検査(L2: PRAGMA/information クエリで FK・既定値・索引を照合)

### 2.1 同期フォルダとスキャン (REQ-010〜REQ-017)

**登録・管理 (REQ-010)**
- フィールド: name(表示名)、path(絶対パス)、is_active(既定 true)、include_subfolders(既定 true)、
  exclude_patterns(文字列配列、ファイル名の完全一致・大文字小文字無視で除外。例 `Thumbs.db`。glob/regex は対象外)、
  last_scan(NULL 初期)
- path はシステム内で一意(大文字小文字無視)。重複登録は明示エラーで拒否。name の重複は許可
- is_active=false のフォルダはスキャン・表示の対象外
- 削除時は配下 images が連鎖削除される(確認ダイアログ必須 — タグ関連も消えるため)

**スキャン (REQ-011〜015)**

> **実行順序(重要・ECO-001 で明確化)**: 以下 1〜6 は論理的記述順であり、実行順は **手順 4・5(missing 化・孤立 pending 削除)を手順 3(各ファイル判定)より先に**適用する。これによりリネーム(旧ファイル消失＋新ファイル出現)を**単一スキャン**で `旧行=missing` ＋ `新行=pending(candidate=旧行 id)` に確定できる(判定規則 3a は missing 化後の DB 状態を参照するため)。実装・凍結済み固定オラクル(工場非開示)・原典挙動(porting-spec RVP-SCAN-003 / 02§1 / 06§2)はいずれもこの順序で一致する(設計者注 — v2.0 表記是正: 旧文の実装クラス名・オラクル ID 直記を一般化)。番号は手順の論理的呼称であり実行順位ではない。

1. path 配下を列挙(include_subfolders=true なら再帰)。拡張子(小文字化)∈ {.jpg,.jpeg,.png,.gif,.bmp,.webp} のみ対象 (REQ-011)
2. 各ファイルの相対パス(スラッシュ統一、先頭末尾スラッシュなし、比較は case-insensitive)を算出 (REQ-014)
3. 各ファイルを以下の優先順で判定する(最初に成立した規則のみ適用)。**手順 4・5 を適用済みの DB 状態に対して**判定する(上記実行順序参照) (REQ-012):
   - (1) DB に同一相対パスの行があり、file_size と modified_date がともに一致 → 変更なし(スキップ)
   - (2) DB に同一相対パスの行があり、file_size または modified_date が異なる → SHA-256 再計算し
     hash/file_size/modified_date を更新(status は変更しない)
   - (3) DB に同一相対パスの行がない(新規) → SHA-256 計算:
     - (3a) 初回スキャン(last_scan が NULL)でない、かつ同一フォルダ内に同ハッシュ・status=missing の行が
       存在する → status=pending、candidate_link_id=その missing 行の id(複数一致時は id 昇順の先頭)で登録
     - (3b) それ以外 → status=normal で登録
     (created_date/modified_date はファイルのタイムスタンプの ISO 8601 UTC 変換)
   - 読み取り不能ファイル(ロック・アクセス拒否等)は DB を変更せずスキップし警告ログ。次回スキャンで再試行
4. DB 上 status=normal で物理ファイルが存在しない行 → status=missing へ更新(**手順 3 の判定より先に実行** — 上記実行順序参照)
5. 物理ファイルが存在しない status=pending の行 → 行を削除(候補が消えたため。**手順 3 より先に実行**)
6. 完了時(例外時も)last_scan を現在時刻に更新し、サマリ {added, missing, pending, updated, skipped} 件数を返す (REQ-015)

**ステータス遷移の全列挙 (REQ-016)** — 以下以外の遷移は存在しない:
| 遷移 | 契機 |
|---|---|
| (新規) → normal | スキャン規則 (3b) |
| (新規) → pending | スキャン規則 (3a) |
| normal → missing | スキャン規則 4(物理消失) |
| missing → normal | 再リンク確定(REQ-017。id 不変、パス情報を上書き) |
| pending → (行削除) | スキャン規則 5(物理消失)、または再リンク確定時の pending 側 |
| deleted | V1 では予約値。deleted へ遷移させる操作・deleted からの遷移は実装しない |

missing → pending の直接遷移は存在しない(pending は新規発見ファイル側にのみ付く)。
スキャンの二重起動は禁止(実行中は同一フォルダの再スキャン要求を拒否)。

**ハッシュ (REQ-013)**: SHA-256、ファイル全体、小文字 hex 64 文字。

**再リンク (REQ-017)**
- missing 画像を選択 → 同一 sync_folder 内・同ハッシュの pending 画像を候補として提示。
  複数候補は relative_path 昇順で列挙し、各候補に相対パス・ファイルサイズ・更新日時を表示する
- 確定前に確認(タグ・ノートが missing 側の画像として引き継がれる旨)を表示する
- 確定: missing 行に pending 行の relative_path/file_name/file_size/modified_date/created_date/hash を上書きし
  status=normal、candidate_link_id=NULL へ。pending 行は削除。**missing 側の image_id は不変**(タグ・関連保持が目的)
- 核/表面: core
- 受入観点: 遷移 4 ケース+再リンクを固定フィクスチャ(実ファイル+DB)で unit〜L2 exact 検査

### 2.2 タグシステム (REQ-020〜REQ-029)

- 3 種類の意味論 (REQ-020): simple は付いている/いないのみ(value=NULL)。textual は value に文字列。
  numeric は value に数値の不変文字列表現(InvariantCulture、比較は数値として行う)
- タグ名はシステム全体で一意・case-sensitive・空白のみは拒否 (REQ-021)。重複は DuplicateTagName エラー
- 階層 (REQ-022): 単一親。親削除→子の parent_id=NULL。循環(自己・子孫を親に指定)は拒否
- タグ定義階層の UI 非露出 (TAG-009 裁定・ECO-047): tags.parent_id は Core 制約(REQ-022)として
  維持するが、現リリースの製品概念として採用せず UI には露出しない(編集・表示とも)。
  画像管理上の階層分類はビュー階層(§2.3)で表現する。意味情報(性別・職業・所属・種別など)は
  タグ定義階層でなく将来の「辞書タグの属性」の方向で表現し、tags.parent_id を意味分類の
  主要モデルとはしない
- color は `^#[0-9A-Fa-f]{6}$` のみ受理、NULL 可 (REQ-023)。description は自由記述、NULL 可
- textual の predefined_values は順序保持の文字列配列。付与値はリスト外も許可(入力補助のみ) (REQ-024)
- numeric の min/max/step/unit は NULL 可。min/max 設定時は範囲外の付与を拒否(両端は含む) (REQ-025)
- 付与は UPSERT(再付与=値上書き、行は増えない) (REQ-026)。解除は行削除(冪等: 無い行の解除はエラーにしない)
- 一括付与・解除は単一トランザクション、失敗時全ロールバック (REQ-027)
- 削除可否 (REQ-082 / TAG-008 裁定・ECO-045): 使用中(画像への値付与・ビュー階層への配置・
  ビュー条件からの参照のいずれかがある)のタグ定義の削除は TagInUse で拒否(定義・参照とも無傷)。
  子タグの親は「使用」でない(削除可・子はルート化)。無効化/非推奨化は削除とは別操作(未導入)
- 「使用中」は未保存の編集状態も含む (REQ-083 / TAG-008 U-a 裁定・ECO-046): 未保存(dirty)の
  ビュー階層編集ツリーに配置されているタグの削除は確認ダイアログの前に UI 層で拒否・理由提示
  (タグ・編集ツリーとも無傷)。防御層として、階層一括保存(SaveHierarchyAsync)は不存在タグを
  参照するノードを NotFound の Result で拒否し既存階層を無傷に保つ(FK 例外の未処理伝播に
  しない= INV-008 の書き込み版)
- 削除カスケード (REQ-028): §2.0 の FK 規則どおり(削除が実行された場合の防御層として存続)
- 一覧は name 昇順(大文字小文字を無視した序数比較=OrdinalIgnoreCase、同値時 id 昇順 — §4 掃討表と
  同一規則。v2.0 表記是正: 旧文「序数比較」は実装・掃討表と不一致だった)、使用数 = COUNT(DISTINCT image_id) (REQ-029)
- 核/表面: core(タグ編集 UI・色見本の描画のみ surface)
- 受入観点: 全規則 unit(exact、境界値ベクタ付き)。UI は golden+承認者

### 2.3 仮想ビュー (REQ-030〜REQ-033, REQ-038)

**CRUD (REQ-030)**: §2.0 のフィールド。name 必須(空白のみ拒否)。削除は条件・階層を連鎖削除。

**条件評価 (REQ-031)**: ビューの全条件を AND 結合し、status=normal の画像集合を絞り込む。
| operator | 対象 | 意味 | value / value2 |
|---|---|---|---|
| exists | 全 type | 当該タグが付与されている | 不使用 |
| equals | textual/numeric | value が完全一致(numeric は数値比較) | value=比較値 |
| between | numeric | value ≦ タグ値 ≦ value2(両端含む、数値比較) | value=下限, value2=上限 |
| regexp | textual | タグ値が正規表現にマッチ(.NET Regex、部分一致) | value=パターン |
| in | textual | タグ値が JSON 配列のいずれかと一致 | value=JSON 配列文字列 |

エッジケースの確定規則(いずれも例外でクラッシュしない):
- 対象タグが付与されていない画像 → いずれの演算子でも条件不成立(exists は false)。AND 結合のため当該画像は結果に含まれない
- simple タグ(value=NULL)への equals/between/regexp/in → 不成立。UI は simple タグに exists のみ提示する
- 数値変換できないタグ値(空文字含む) → equals/between の数値比較で不成立
- operator≠exists で value が NULL の条件 → その条件を評価から除外(無視)し警告ログ
- 不正な正規表現パターン・マッチタイムアウト(1 秒) → 条件不成立+警告(ログ+非モーダル通知)
- 実装方式(SQL/インメモリ)は自由。観測契約は §2.8 OC-1

**整列 (REQ-038)**: UI で選択可能な sort_field ∈ {name, modified_date, file_size}(既定 name。
created_date は v1.3/ECO-002 CR-4 で UI ソート軸から除外 — リスト基本列との整合・承認者裁定。
DB 既存値 'created_date' は後方互換として受理し従来どおり整列に使える)、
sort_direction ∈ {asc, desc}(既定 asc)。name は大文字小文字を無視した序数比較(OrdinalIgnoreCase 相当)。
二次キーは id 昇順(同値時も実行ごとに並びが変わらない安定ソート)。

**modified_at 規則 (REQ-032)**: ビュー本体・条件・階層のいずれの変更でも更新。閲覧では更新しない。

**一覧 (REQ-033)**: お気に入り=is_favorite=true を name 昇順。最近=modified_at 降順 limit 件(既定 10)。
いずれも同値時は id 昇順。
- 核/表面: core
- 受入観点: 演算子×型×境界値のテストベクタで unit(exact)。

### 2.4 タグ階層と NodeGraph (REQ-034〜REQ-037)

**階層定義 (REQ-034)**: ノード = {id, view_id, tag_id 必須, parent_id(NULL=ルート直下), position(同一親内 0 起点昇順),
alias(NULL なら tag.name を表示), condition_type ∈ {equals, range, pattern, values, NULL}, condition_value(JSON)}。
condition_value スキーマ: equals=`{"value":…}` / range=`{"valueFrom":…,"valueTo":…}` / pattern=`{"pattern":…}` /
values=`{"values":[…]}`。ノード移動で自己・子孫を親に指定→拒否。

**NodeGraph 構築 (REQ-035)**: 階層ノードを position 順に走査し、タグ type 別に展開する。
値の抽出対象は常に **status=normal の画像**のみ(REQ-016、INV-010):
- simple/numeric タグ → 1 ノード(配下に階層上の子ノードを接続)
- textual タグ → タグ名ノードの配下に「値ノード」を生成。値 = 当該タグが status=normal の画像に付与された
  distinct 値(predefined_values 外の自由入力値も含む。ノードに condition_type があればその条件で値を制限)、
  序数昇順
  - distinct 値が 0 件 → タグ名ノードのみ(値ノードなし)。ノードは表示・選択可能で、選択時は exists 条件と
    して評価される(結果 0 件なら空状態表示)
  - distinct 値が 1 件 → タグ名ノードと値ノードを統合した一体型ノード(表示名「タグ名: 値」)とし、
    階層上の子ノードは一体型ノードの配下に接続する
  - 2 件以上 → 値ノード群を生成し、階層上の子ノードは**各値ノード**の配下に接続する

NodeGraph は表示のたびにタグ付け状態から再構築する(キャッシュする場合はタグ付け・階層変更で無効化)。
タグ付けにより distinct 値数が 0↔1↔2 と変化した場合、次回構築で構造が変わる(選択状態は解決できなければルートへ)。

**絞り込み (REQ-036)**: 選択ノードまでのパス上の全ノードから条件列を生成し AND 評価(§2.3 の評価器を使用):
ルート=無条件 / simple・textual タグ名ノード=exists / 値ノード・一体型ノード=equals(その値) /
numeric ノード=condition_type に応じ equals または between(range の場合 valueFrom/valueTo)。

**ホームタグ (REQ-037)**: ビューを開いたとき home_tag_id(階層ノード id)が解決できれば該当ノードを初期選択。
解決不能(削除済み等)ならルート。エラーにしない。
- 核/表面: core(木構造の描画のみ surface)
- 受入観点: フィクスチャ(階層+タグ付け)→期待ノード木・期待画像集合の unit exact。値 0/1/2 件の境界必須

### 2.5 サムネイルと画像表示 (REQ-040, REQ-045, REQ-085)

- 生成 (REQ-040): 長辺 256px へ縮小(アスペクト比保持・拡大しない)。PNG 入力→PNG 出力、その他→JPEG 品質 80
  (品質値は実装規定 — L2 照合対象は寸法・形式のみ。v2.0 明確化)。
  保存先 `%APPDATA%/ViewPrism2/thumbnails/{MD5(画像絶対パスの小文字)}-v2.{png|jpg}`(MD5 は小文字 hex。
  **`-v2` は生成規則の世代サフィックス — ECO-049/REQ-085 の EXIF 適用世代。旧 `{MD5}.{png|jpg}` は
  参照されない孤児として無害**)。存在すれば再生成しない。
  生成失敗(壊れた画像)はキャッシュへ記録せずプレースホルダ表示とし(次回表示時に再試行)、スキャン・一覧表示を
  停止させない。読み取り不能なキャッシュファイルは削除して再生成する
- **EXIF Orientation の表示系適用 (REQ-085 / ECO-049 — v4.1 追補)**: サムネイル生成・ビューアの
  フルサイズ表示・解像度メタデータは EXIF Orientation(1〜8・SKCodec.EncodedOrigin)を適用し
  **正立**で扱う。変換は 8 方位(D4 — §2.10.1a と同じ変換族)の添字置換で決定的。
  Orientation 5〜8 は実効寸法= W/H 入替。サムネの正立化は縮小後に適用してよい
  (アスペクト比保持=一様スケールは D4 と可換)。**適用は表示系に限る** — pHash 入力・SHA-256・
  スキャンは非適用のまま(向き耐性は REQ-084 変種が担う。adapter 世代 P-09 を発動させない)
- フルサイズ表示キャッシュ (REQ-045): メモリ LRU、上限 50 枚、有効期限 3 分(**絶対 TTL: ロード時刻
  起点。アクセスで延長しない** — v2.0 明確化、実装・既存 unit と一致)
- 核/表面: 生成パラメータ=surface(画像ライブラリ API へトレース)、キャッシュ規則=core
- 受入観点: 寸法・形式・キャッシュ動作は L2(出力ファイルのメタデータ検査)。LRU は unit(読込回数カウント)。
  EXIF 正立変換の 8 方位契約と実効寸法は S-41(unit exact)・ビューア正立表示は golden(G-9 系)

### 2.6 シェルと画面構成 (REQ-041〜REQ-044, REQ-046, REQ-053, REQ-060) — v1.2 改訂・v2.0 GF 是正・画像タブ evolved design 同期(ECO-010〜024)

**シェル**: 上部タブナビゲーション「タグ」「画像」+右端に「設定」。原典の「作業」タブは V3 スコープ
のため V1 では設けない。多重起動は禁止(2 つ目の起動は既存ウィンドウをアクティブ化して終了)。
単一ウィンドウ(§4 掃討表の同項と対応 — v2.0 で本文へ明記)。

**タグタブ(3 ペイン)** — タグ・ビュー・階層の管理:
- 左「ビュー管理」: 新規ビュー作成ボタン、ビュー一覧(各行: 名前+**タグ数バッジ**+編集/削除アイコン。お気に入り★は出さず説明は tooltip — v4.0/ECO-007/E1)。選択中のビューが
  中央の階層編集対象になる。ビュー作成/編集ダイアログ = 名前(必須)+説明+お気に入り(v1.3 で実装・原典能力に整合)
- 中央「階層構造」: 選択ビューのタグ階層ツリー。タグパレットから D&D(またはボタン)でノード追加。
  ノード操作: 展開/折畳、ホームタグ設定/解除(設定中ノードは強調表示。**トグル・強調のアイコンは
  家アイコンを用いる。★は不可 — v2.0/REQ-060(c)、GF-03 是正**)、別名のインライン編集、
  条件設定(textual/numeric のみ。condition_type/condition_value のダイアログ)、削除。
  **ノードの条件サマリ表示は人間可読に整形する。書式は i18n リソースの条件型別テンプレートキーで
  定義し、ja 既定: equals=`値が一致: {value}` / range=`範囲: {valueFrom}〜{valueTo}`(片側のみの
  場合は `{valueFrom} 以上`・`{valueTo} 以下`)/ pattern=`パターン: {pattern}` / values=
  `いずれか: {値1}, {値2}, …`(区切りは `, `)。JSON 文字列や Unicode エスケープ(`男` 等)を画面に
  露出しない。格納値の JSON 形式(REQ-034)は不変 — v2.0/REQ-060(e)、GF-05 是正**。
  **編集はメモリ内で行い「保存」で一括コミット**(REQ-032 の modified_at 更新は保存時に 1 回)、
  「キャンセル」は確認後に破棄。未保存変更がある間のみ保存/キャンセルを活性化。
  **ノード追加ボタンは汎用ラベル『タグを配置』を表示する(対象タグ名は出さない — v4.0/ECO-007/E3 で
  GF-04 の対象タグ名表示を撤回)。パレット未選択時は非活性+選択を促す文言は維持(M1 3ペインのパレット選択を前提)**
- 右「タグパレット」: 検索ボックス(名前の部分一致・大文字小文字無視)、「追加」ボタン→タグ作成ダイアログ、
  タグ一覧(色スウォッチ+名前+種類チップ+編集/削除)。**選択中タグの行は明確な選択視覚(背景強調+
  境界)で判別できること — v2.0/REQ-060(d)、GF-04 是正**。
  タグ作成/編集ダイアログ: タグ名(必須)・種類(シンプル/テキスト/数値、**種別セレクタはセグメント3タブ** —
  v4.0/ECO-007/E6。作成後の種類変更は不可)・カラー(**プリセット9色**(Radix 系)+hex 表示 — v4.0/ECO-007/E7)・説明。
  **付与プレビュー(★/数値±/候補チップ)を常時表示し画像タブ実付与 UI と付与表現を一致させる(DC-TAGPREVIEW-001 —
  v4.0/ECO-007/E5。整形は核側ヘルパ・unit 検査可)**。テキスト型: 候補値の追加・削除・D&D 並べ替え
  (**ダイアログは高さ固定とし、候補値の増加ではウィンドウを伸長させず候補値リスト内部のみ
  スクロールする — v2.0/REQ-060(a)、GF-01 是正**)。
  数値型: min/max/step/unit の入力

**画像タブ** — 閲覧・ブラウズ・タグ付け・整理・修復(as-built = evolved design):

> **as-built 権威(ECO-010〜024/M4)**: 画像タブは新 surface `ImageTabView`(`ImageTabViewModel`)一本化済
> (ECO-024 で原典 MainWindow 画像タブ Grid(3ペイン)・harness トグル・legacy VM=`ImageBrowserViewModel`/
> `DetailPanelViewModel`/`TaggingPanelViewModel`・シェル `TreeRoots`/`FolderPane` を撤去)。逐条の権威は
> **E-UI-BROWSE-022**(描画・母集合・選択計算)/ **E-UI-AXIS-NAV-040**(表示軸ナビ)/ **E-UI-MODE-041**(文脈モード
> 統制)+ ViewPrismUI UI-IR(CAD)。本節はその evolved design を as-built 散文として同期する(v1.2 の左 NodeGraph
> ツリー・詳細パネルは撤去済)。設計意図(二軸ブラウズ・潜る/戻るの統一・値入力のインライン化・文脈ペイン)は
> `bomdd/ui/image-tab/design-intent.md`(I-01〜I-05)。

**左ペイン — コレクション(選択スコープ)**: 同期フォルダ一覧+スキャン実行+「追加(＋)」。**コレクションは選択
スコープ(REQ-053、v1.3/ECO-002 CR-2)**: 選択中コレクションが一覧・「全画像」入口・タグビュー軸評価の母集合
(status=normal・コレクションファースト・横断表示しない)になる。各項目は名前+パス muted+画像数バッジ+スキャン中
表示(パスはツールチップ、CR-8)。選択コレクションは settings へ永続化し次回起動時に復元(CR-5)。未選択時は中央に
選択を促す空状態。コレクション管理(追加)は左「追加(＋)」に集約(ECO-013)し `⋯` に重複させない。ペインは
276/64 折り畳み(E-UI-SHELL-021 と同骨格)。旧・左 NodeGraph ツリー(ECO-006/BL-001)+ビュー一覧の左ペイン常設は
ECO-024 で原典 Grid ごと撤去済(ビュー選択は表示軸セレクタへ集約)。

**表示軸ナビ(中央上部 — E-UI-AXIS-NAV-040)**: 表示軸ナビは左ペインの NodeGraph ツリーでなく、中央上部の
**表示軸セレクタ+チップ+パンくず**で行う(原典のフォルダ+パンくず流儀・二軸ブラウズ=design-intent I-01/I-02、
ECO-010/I01・ECO-011/I04。AND パス意味論は不変):
- **表示軸セレクタ**: FS フォルダ軸 / タグビュー軸を切替(アイコン+ラベル+▼)。両軸とも「チップで潜り・パンくずで
  戻る」同一操作(その場分岐禁止)。
- **FS フォルダ軸**: relative_path から派生する読み取り専用の階層(新スキーマ/スキャン不要・INV-009)。現フォルダ表示中の
  画像に実在するタグを**現階層スコープで集計**してタグチップに出す(再帰しない — ECO-012/I10)。
- **タグビュー軸**: タグタブで組んだ保存ビューの階層を消費する(E-VIEWSVC-009)。表示軸セレクタがビュー管理のビュー
  一覧を展開して選び(単一ビューは直接入る — ECO-012/I06)、チップで子ノードへ潜る。ルート=ビューのフィルタ集合
  (無条件)は「ホーム(パンくず先頭)」に相当。子ノードの無いビューはチップを出さない。
- **「全画像」固定入口**: 選択コレクション内の全 normal 画像(フラット・条件なし)。2軸ナビとは別に維持(ECO-011/I04)。

**ツールバー**: 表示軸セレクタ / **表示列(ECO-025 β-2)** / ソート / レイアウト切替(グリッド⇔リスト・選択は設定へ永続化
REQ-052・v1.3/CR-6)/ 文脈モード入口(「タグ編集」「作業」「⋯(その他)」)。列数セレクタは撤去(列数はレスポンシブ
自動算出 — v1.3/CR-1)。**モード中(タグ編集/整理/作業/削除)はツールバーが他モード入口・`⋯`・表示列 を隠し、当該モードの
「終了」+モード固有操作+表示軸/ソート/レイアウトのみ残す(排他隠し規律 — ECO-014 §8、作業/削除へも拡張。表示列を残さないのは
ECO-027 で CAD 準拠に是正 — ViewPrismUI image_tab.md「残すのは表示軸・ソート・グリッド/リスト・終了だけ」)。**
- **狭幅レスポンシブ収納(IMG-014/ECO-027・as-built)**: ツールバーは判定を**ビューポート幅でなくツールバー実測幅**で行い
  (左ペイン折り畳み 276/64・右ペイン開閉で使える幅が変わるため CSS メディアクエリ相当では不可)、段階的に収納する:
  ①ラベル畳み(<約820px=入口ボタン「タグ編集/整理/作業」をアイコン化・ToolTip 維持)→②「整理」を`⋯`へ退避(<約640px)→
  ③回り込み(左右クラスタが単一行に入らない時、右クラスタ[ソート+グリッド/リスト]を2段目へ・左クラスタも極狭では内部折り返し)。
  確定契約(px 値でなく挙動)=①どの幅でも重ならない/潜り込まない ②畳む順序(ラベル→退避→回り込み) ③離脱/実行ボタン
  (◯◯を終了・ゴミ箱へ移動 等)のラベルは狭幅でも維持。しきい値は目安で調整可。実測は View(レイアウト確定後の
  実測幅・左右クラスタ希望幅)を VM(段階/回り込み状態)へ供給し、判定ロジックは描画から独立して unit 検査可能(CP-TOOLBAR-RESPONSIVE-027)。
- **ソート = アクティブビューの表示列を軸に統一(ECO-025 β・FL-003 v2)**: 旧 sort_field×方向の固定ソート導線(名前/更新日/
  サイズ)は browse から撤去。ソート対象=ビューの display_columns 列(基本情報+タグ)で grid/list は状態(列・方向)を共有。
  リスト=列ヘッダー(ソート入口)+ツールバー要約チップ(方向+列名+✕クリア)、アイコン=単一「並び替え」メニュー(候補=表示列・
  昇順/降順)+タイルにソート項目。ソート規則(空値末尾・型別比較・安定タイブレーク)は E-UI-BROWSE-022 の決定論ロジック(REQ-081)。
  **ソート状態の永続範囲=画面ローカル(揮発・再起動で解除・永続しない — FL-002=S-a・ECO-039 明文化)**。ビュー切替・表示列編集の自動解除と整合。
  **列はリスト専用(FL-001・ECO-042 明文化・現状追認)**: グリッド(アイコン表示)に列(ヘッダー+セル)は持ち込まない。アイコンへの表示列の露出は上記 FL-003 の範囲(並び替え候補+ソート中タイルのソート項目)まで。
- **「表示列」= 表示列ポップオーバー(ECO-025 β-2)**: view 軸+ビュー選択時のみ表示。列ピッカー(SC-COLUMN-PICKER-001)で
  列を追加/削除/並べ替え、アクティブビューの display_columns へ**即書き戻す(VE-003 ライブ編集・保存/キャンセル無し)**。
  除去列がソート中ならソート解除。上限5(VE-002)で件数バッジ アンバー+追加元不活性。名前固定(VE-001)。ビュー編集モーダル(§2.3)と
  同一の列ピッカー VM を消費(導線のみ差=モーダルは 2 カラム・保存で反映 / ポップオーバーは単一マージ・ライブ)。

**中央 — グリッド/リスト(E-UI-BROWSE-022)**: 描画・母集合・既定選択計算を所有(下記「各部品の規則」)。

**文脈モード(E-UI-MODE-041)**: 画像タブは中央/右ペインの挙動を切替える**排他の文脈モード**を持つ。閲覧時クリック=
ビューア起動、モード時クリック=選択(design-intent I-03)。クリック意味論のディスパッチは E-UI-MODE-041、描画・
選択機構の提供は E-UI-BROWSE-022。状態遷移は Core のみ経由(UI で再実装しない)・判定/反復は描画から独立して unit
検査可能:
- **タグ編集**(ECO-014): 右ペインにタグ付与パネル(REQ-046・下記)。グリッドが選択可能になる(選択許可 inSelect)。
- **整理**(類似+マージ・ECO-014): 中央ペインが検索結果表示へ切替わり候補+一致率を提示。整理中の画像クリックは
  マージ先/整理対象の割当として解釈する(既定選択でない)。詳細は §2.10。
- **作業**(ECO-017/ECO-020): グリッド選択を `workTargets` 集合へ和集合追加する「追加」+「作業対象 N 枚」チップを持つ
  (選択あり時のみ活性・追加後に選択クリア)。右ペインは開かず中央フル幅。「追加」(AddToWork)は session 蓄積に加え、
  選択画像を **E-WORKSPACE-042.AddImagesToDefault でデフォルト作業スペースへ永続追加**する(受け渡し先=作業タブ
  E-UI-WORKSPACE-043)。作業対象セル個別の視覚マークは出さない(描画モック準拠)。
- **削除**(ECO-018): `⋯` から入る4つ目の排他モード(ツールバー入口は持たない)。グリッド選択を「ゴミ箱へ移動」で
  E-TRASH-038.DeleteToTrashAsync(normal→deleted ソフト削除・物理非破壊 INV-009・復元可)へ送る(選択あり時のみ活性・
  実行後に選択クリア+normal 母集合から除外+ゴミ箱件数更新)。
- **`⋯`(その他)= メンテナンスメニュー**(ECO-015/018): **修復 / 削除 / ゴミ箱** の 3 項目。**修復**=既存修復モーダル
  (→§2.11)。**ゴミ箱**=画像タブ内ポップアップ(中央オーバーレイ・deleted 件数バッジ併記 — ECO-019 が ECO-018 の
  別ウィンドウモーダルを置換)。修復/ゴミ箱はモードに入らず開き、閉じ後に画像データを再読込して status/件数変化を
  反映する。コレクション未選択時は無効・モード中は非表示。

**右パネル**: タグ編集モード時=タグ付与パネル(REQ-046・下記)。整理モード時=整理トレイ(§2.10)。作業/削除モードは
右ペインを開かない(中央フル幅)。※「通常時=詳細パネル(REQ-043)」は ECO-023 で撤回(リバース誤り=原典は
ImageDetailModal モーダル・新設計は詳細を廃止)。新設計の右ペインは文脈モード時のみ表示。

各部品の規則:
- グリッド (REQ-041): 列数はコンテンツ幅から自動算出 — **列数 = clamp(⌊幅/220px⌋, 2, 8)**(固定の列数セレクタは
  置かない。v1.3/CR-1 — 原典 NewUI のレスポンシブ準拠)。正方形セル+ファイル名。クリック=単一選択、
  Ctrl+クリック=トグル(選択順の番号バッジ表示)、**SHIFT+クリック=最後の選択アイテムからクリック位置までの
  一覧 index 範囲を既存選択へ追加**(union・置換しない。選択順は index 昇順で末尾へ追番。v1.3/CR-3 —
  原典 useImageSelection 方式)、**ダブルクリック=ビューア起動(タグ編集モード中は無効)**。
  100 件以上で仮想化必須(目標: 1 万件でスクロール操作可能)。**as-built(ECO-026/#1)**: グリッドは
  **ItemsRepeater + UniformGridLayout**(別 NuGet Avalonia.Controls.ItemsRepeater 12.0.0)で仮想化=画面外
  セル/サムネを実体化しない。旧 ItemsControl+WrapPanel は非仮想(FMEA-013 罠)だったため REQ-041 不適合を是正。
  サムネは非可視化/リサイクルで進行中ロードを cancel し Bitmap を Dispose(ECO-026/#6)。maintainer 実機 golden 承認済。
  **グリッドの選択視覚はセルの強調枠+選択順バッジのみとし、行(横一列)の背景色を変化させない
  (行リスト仮想化の実装が行選択視覚として漏れ出ることの禁止。リスト表示モードの行選択視覚は正として維持)
  — v2.0/REQ-060(b)、GF-02 是正**。
  **セル構成=サムネイル+ファイル名(ファイルサイズはグリッドセルから除去しリスト列でのみ提示 — ECO-010/DC-GRID-001
  改訂・I05)。閲覧時はタグ色ドット(左下・最大3・超過は先頭3・白リング)を重畳する(ECO-010/ECO-012/I13)。
  タグ編集/作業/削除モード時は選択許可フラグ inSelect によりチェック+選択順バッジを出しタグドットは非表示にする
  (選択機構の再利用・モード側で再宣言しない — ECO-017)**
- リスト (REQ-042): display_columns(JSON 配列)に従う。列 = {type: basic|tag, key, label, width}。
  basic.key ∈ {name, size, modified_date}。tag.key=タグ id(削除済みタグの列は無視して描画し、残り列で全幅を埋める)。
  既定 3 列。**列描画契約+列ヘッダーソート(ECO-025 β・REQ-081)**: 名前=固定列(先頭・削除/移動不可・伸縮 minmax)、
  他=固定幅。型別セル描画(basic size=`X.X MB`/`X KB`・date=日付 / num=★×値+数値[金色] / text=値チップ[タグ色] /
  simple=有無ドット・未設定は淡色)。列ヘッダー=ソートトグル(別列昇順→同列で昇⇄降)。表示列の編集は「表示列」ポップオーバー
  (ツールバー・ECO-025 β-2)= ビュー編集モーダル(§2.3)と同一の列ピッカー(SC-COLUMN-PICKER-001・display_columns をビュー定義へ
  書き戻し VE-003)。
- ~~詳細 (REQ-043): ファイル名/サイズ/解像度/作成・更新日時/ノート/タグ一覧~~ **← ECO-023 で撤回**
  (リバース誤り=原典は ImageDetailModal モーダル・新設計は詳細を廃止)。サイズ/解像度の整形表示は
  list/grid(ECO-004・CP-DISPLAY-PARITY-022)が担当・OC-7 整形器は共有継続。images.notes は DB 残置(dormant)
- タグ付与パネル (REQ-046): 「現在のタグ」(選択画像に付与済み。複数選択時は共通タグ。各行に解除×)と
  「タグを追加」(全タグ一覧+検索)の 2 区画。追加候補を選んで「適用」→選択画像全件へ一括付与(REQ-027 の
  原子バッチ)。**値入力はインライン(ポップアップ廃止 — design-intent I-04・ECO-010/I02・CP-TAGUI-013)**:
  textual は右パネル内で行を展開して候補値チップ列から選択(+自由入力)、numeric は範囲セルで値入力。
  「全画像に同じ値」=固定値 / **「選択順に連番」=開始値+選択順 i(連番は独立アクションで存置 — ECO-010/I02b)**。
  範囲外値はインラインで検証して拒否
- ビューア (REQ-044): 単一画像をウィンドウフィット表示(縮小のみ、拡大なし)。→/PageDown=次、←/PageUp=前、
  Esc/閉じるボタン/**画像外余白のクリック**=閉じる(背景クリックは原典準拠 — モーダルオーバーレイから
  ウィンドウ方式への適応。v1.3/CR-7。**v2.0: 余白クリックで閉じるは normal モードのみ有効** — REQ-054、
  scroll/spread ではページ送りクリックとの衝突防止のため無効)。並びは呼び出し元一覧の整列結果。
  端で停止(ループ・例外なし)。表示中はタイトル等に「現在位置/総数」を表示。
  **v2.0: 本項の単一画像表示はビューア表示モードの normal に相当する。拡張モード(scroll/spread)と
  共通設定・モード切替は §2.9(REQ-054〜059)** — Esc・閉じるボタン・現在位置/総数・端停止は全モード共通

空状態の規則(全画面共通):
- グリッド/リスト: 画像 0 件 → 中央に「画像がありません」相当のメッセージ(i18n キー)。何もない場所の
  ダブルクリックは無視
- タグ付与パネル: 選択 0 件 → 「画像を選択してください」相当のプレースホルダ(旧・詳細パネルは ECO-023 で撤回)
- 表示軸ナビ: 現チップ/フォルダ/ノードの結果 0 件 → グリッドの空状態と同じ(E-UI-AXIS-NAV-040)
- タグタブ: ビュー未選択 → 階層ペインに「ビューを選択してください」/ 階層ノード 0 件 → 「まだタグが
  追加されていません」相当の案内
- ~~**画像タブ NodeGraph ナビ(ECO-006/BL-001)**: 選択ビューの階層ツリーは**ルートを常に表示**する~~
  **← ECO-024 で撤去**(原典左 NodeGraph ツリーごと)。as-built(ECO-010/011): タグビュー軸は表示軸セレクタ+
  チップ+パンくずで潜り、ルート=ビューのフィルタ集合(無条件)は「ホーム(パンくず先頭)」に相当する。
  子ノードの無いビューはチップを出さない。空状態は grid 0件・ビュー未選択・コレクション未選択プロンプトで表現する

堅牢性規則(全 UI 共通 — v1.3/ECO-002 DF-1): UI 操作に起因する未処理例外でプロセスを終了させない。
未処理例外はグローバルハンドラで捕捉し、スタック全文をログ(%APPDATA%/ViewPrism2/logs/)へ記録のうえ
非モーダル通知(ステータスバー)で利用者へ示す。検証は CP-ROBUST-001(NFR-002 — いずれも
30-ebom/33-control-plan で定義される受入 ID。製造パッケージに含まれ本書外で閉じる)。

**GF 是正の統合定義 (REQ-060 — v2.0)**: V1 golden 承認時の minor 所見 5 件の是正。機能の意味論は
全項目で不変、視覚・表示整形のみ。各項目の規定は本節内に太字注記で織り込み済み:
(a) タグ作成/編集ダイアログの高さ固定(タグパレット項) (b) グリッド行選択視覚の禁止(グリッド項)
(c) ホームタグの家アイコン(階層構造項) (d) パレット選択強調+追加ボタンの対象明示(階層構造・タグパレット項)
(e) 条件サマリの整形表示(階層構造項)。受入: (e) は unit(整形ベクタ)、(a)〜(d) は golden G-1/G-6 の
v2.0 追加項+承認者。

golden 判定基準(承認者 maintainer 向けチェックリスト — 合否は固定オラクルでなく承認で記録。
各項目に操作経路〔〕を併記 — v1.3/ECO-002 P1-5):
- G-1 シェル+グリッド+コレクション: タブ構成(タグ/画像+設定)。〔画像タブでコレクションを選択〕一覧が当該
  コレクションに絞り込まれ、項目に画像数が表示される。グリッドはウィンドウ幅に応じて列数が自動調整され、
  正方形セルが整列し、ファイル名が 1 行省略(…)表示。選択セルに強調枠+選択順バッジ(1 起点昇順)。
  〔SHIFT+クリック〕で範囲が既存選択へ追加される。
  **(v2.0 追加)〔セルを 1 つ選択〕同じ行の他セルや行背景の色が変化しない(GF-02 是正確認)**
- G-2 リスト: 〔ツールバーでリストへ切替〕display_columns どおりの列構成・ヘッダ表示。行選択が機能。
  〔アプリ再起動〕で表示モードが復元される
- **G-3 表示軸ナビ+文脈モード(ECO-010〜021 as-built — §2.6)**: 〔表示軸セレクタで FS フォルダ軸/タグビュー軸を
  切替〕チップで潜り・パンくずで戻る同一操作で母集合が変わる(タグビュー軸は保存ビューの階層を消費)。閲覧時グリッド
  にタグ色ドット(左下・最大3)が出る。〔閲覧時にセルをダブルクリック〕ビューアが起動する。〔タグ編集/作業に入る/
  `⋯`→削除に入る〕当該モードでのみグリッドが選択可能(チェック+選択順バッジ・タグドット非表示)になり、ツールバーが
  他モード入口・`⋯` を隠す(排他隠し)。旧・詳細パネル(REQ-043)は表示されない(ECO-023 撤回の確認)
- G-4 ビューア: 〔前提: ビューア設定のモードを既定(normal)に戻しておく — v2.0 で mode が永続化されるため〕
  〔グリッドの画像をダブルクリック〕で起動。画像がウィンドウに収まり(縮小のみ)、端で停止。
  「現在位置/総数」表示。〔Esc / 閉じるボタン / 画像外余白クリック〕のいずれでも閉じる
- G-5 i18n: 〔設定で ja/en 切替〕主要画面のラベル・ボタン・見出しが即時切替、生キー露出なし
  (タグ名・ビュー名・コレクション名等のユーザーデータは不変が正 — porting-spec 03 L328)
- G-6 タグタブ: 3 ペイン構成。タグ作成ダイアログ(型別フォーム=種別セグメント3タブ・候補値並べ替え・カラー9色 hex・
  **付与プレビュー(★/数値±/候補チップ)が実付与 UI と一致** — v4.0/ECO-007/E5/E6/E7)、ビュー作成ダイアログ(名前+説明+お気に入り)、
  **ビュー行は ★無し+タグ数バッジ+説明 tooltip(ECO-007/E1)・パレット行は説明なし(ECO-007/E2)**、
  階層への追加・別名・条件・ホームタグ・削除、保存/キャンセルのバッチ動作。
  textual タグ(候補値あり)の作成・保存がクラッシュなく完了する。
  **(v2.0 追加)〔タグ作成ダイアログで候補値を 10 件以上追加〕ウィンドウ高さが伸びず候補値リスト内部が
  スクロールする(GF-01)。ホームタグのトグル/強調が家アイコンで表示される(GF-03)。〔パレットでタグを選択〕
  選択行が明確に強調され、追加ボタンに汎用ラベル『タグを配置』が表示される(対象タグ名は出さない — ECO-007/E3。未選択時は非活性は維持)。
  〔textual ノードに条件(値=男 等)を設定〕条件サマリが整形表示され \\uXXXX や生 JSON が露出しない(GF-05)**
- G-7 タグ付与: 〔タグ編集モード ON で画像を選択〕右パネルが切替わり、現在タグ/追加/適用が機能。
  **値入力はインライン**(ポップアップ無し — ECO-010/I02): numeric の固定値/連番(連番は独立アクション I02b)、
  textual の候補値チップ選択が右パネル内で機能する
- **G-8 ビューア拡張(v2.0 新設 — §2.9)**: 〔ビューアを開きモード切替 UI で scroll へ〕画像が一覧順で縦に
  連続表示され、スクロールで前後の画像が順次読み込まれる。〔いったん下方の画像まで眺めて spread-right へ
  切替→scroll へ戻す〕scroll の前回位置付近へ復元される(モード別位置記憶)。
  〔spread-right で先頭から〕1 枚目が右・2 枚目が左に表示され、〔←キー / 左ページクリック〕で次の見開きへ、
  〔→キー / 右ページクリック〕で前へ戻る(操作が紙の本と一致)。〔SHIFT+←〕で 1 ページだけ送られ、
  以後の見開きペアがずれて組み直される。〔spread-left へ切替〕左右配置と操作方向が反転する。
  〔最終画像が奇数番目になる位置〕相方側が無地の空白ページになる(エラー・文言なし)。
  〔設定 UI で resizeMode=matchLargerHeight / alignMode=top / gapMode=loose(16px)を順に変更〕見開きの
  両画像の高さが揃い、上端揃え・16px 間隔になる。〔空白ページ開始 ON+先頭で次へ〕1 枚目(index 0)が
  進行方向側に単独表示→次の送りで 2 枚目・3 枚目(index 1,2)のペアになる。〔アプリを再起動してビューアを開く〕モード・表示設定が復元される。
  〔読み込み不能な画像(ロック等)を含む一覧で scroll/spread を操作〕該当ページのみエラー表示になり、
  ビューア全体は操作可能なまま(クラッシュ・全面エラーなし)。〔Esc / 閉じるボタン〕全モードで閉じる。
  〔scroll/spread 中に余白相当をクリック〕ビューアは閉じない(normal のみの規則)
- **G-9 類似検索+マージ(v3.0 新設 — §2.10。v4.1/ECO-052: 整理トレイの as-built へ手順同期 —
  旧記述はモーダル時代のまま ECO-014 の M4 から漏れており、実施時のトレイ読み替えで
  「実行前プレビュー・非破壊明示」項目が暗黙スキップされていた実績)**:
  〔ツールバー「整理」で整理モード開始(タグ編集モードと排他)〕
  〔トレイの構造(ECO-056 as-built= v2 CAD 3 ゾーン)〕**マージ先=上部ピン・アクション(「似た画像を
  探す」折りたたみ+「マージを実行」)=下部ピン・内部スクロールは整理対象リストのみ — 対象を大量に
  積んでもマージ先と実行ボタンはスクロールアウトしない。マージ先カードの ✕ で解除できる(整理対象は
  保持・実行/検索は不活性化 — REQ-067)。グリッドタイルは宛先=緑チェック+「宛先」バッジ/対象=
  青チェック+「対象」バッジの同型。**
  〔トレイ「似た画像を探す」(折りたたみを開く)〕タブは「条件検索」「類似画像検索」の順。切替コンテンツは
  150px 固定(タブ切替で高さが変わらない・グリッドも再構築しない=GF-056-01/02)。類似画像検索は
  閾値スライダー(50〜100、**既定 70=仕様値・ECO-050**)を持ち、
  照合の精度モード切替(原典の pHash のみ / pHash+ORB)の選択肢は表示されない(本実装は pHash 照合のみ)。
  〔条件検索タブ(ECO-055 as-built)〕**マージ先との属性一致トグル 5 種(ハッシュ値/拡張子/サイズ/
  ファイル名/更新日)が全数表示される(自由入力欄は無い)。マージ先必須∧条件 1 つ以上で「探す」が
  即活性(タブ往復不要=GF-055-01)・マージ先未選択やトグル全 OFF では押せない。**
  〔検索実行=「探す」〕結果が候補+一致率%で類似度の高い順に並ぶ(似た画像が上・見比べ重視)。結果 0 件なら空状態。
  **結果ヘッダの「グリッドへ」でグリッドに戻れる(整理モード維持・結果は再検索まで保持 — ECO-056)。**
  〔グリッドクリックでマージ先(残す 1 枚)→整理対象をトレイへ割り当て、マージを実行〕
  実行前はトレイのサムネイル一覧でマージ先・整理対象を視認できる(**統合後タグの事前プレビューと
  非破壊の個別明示は無し — ECO-052 裁定 b。保護は完了パネル+「取り消す」=補償 Undo が担う**)。
  マージ元がグリッドから消え(deleted)、マージ先にマージ元のタグが集約される(同名タグはマージ先の値を保持)。
  〔完了パネル〕「統合しました」+結果+「取り消す」「別の整理を続ける」。〔取り消す〕マージ元が一覧へ復帰しタグが復元される。
  〔ゴミ箱ポップアップを開く〕マージ元の画像が deleted として一覧・件数表示される(復元/完全削除は G-10)。
  〔マージ前後で元画像ファイルをエクスプローラで確認〕ファイルは移動・削除されていない(物理非破壊)
- **G-10 修復ライフサイクル(v4.0 新設 — §2.11)**: 〔missing 画像に対し修復(relink)を開く〕candidate(pending)と
  criteria 条件検索(hash/名前/拡張子/mtime/サイズ)の結果が候補として安定順に並ぶ。〔候補を選んで確定〕missing 行が
  その画像として normal 化し、付与済みタグは保持される(候補側は消える)。タグ付きの候補は relink できず「マージを使う」旨が示される。
  〔トラッシュで deleted を選び「復元」〕物理ファイルがあれば一覧へ戻り(normal)、無ければ missing として扱われる旨が分かる。
  〔「完全削除」〕『画像ファイルは削除されません(DB から除去)』の非破壊明示と確認を経て、一覧から消える。
  〔復元・完全削除の前後で元画像ファイルをエクスプローラで確認〕ファイルは移動・削除されていない(物理非破壊)
- **G-11 タグ制御モード(ECO-022 新設 — §2.12)**: 〔ビューア設定で「タグ制御モード」を ON にし「タグ制御マッピング」を開く〕
  6 つの予約アクションが各行にグリフ・名前・英名・説明・割り当てるタグ picker(色ドット+ラベル/未割り当て)で並ぶ。〔広告ページに
  割り当てたタグを `skip` に、保持したいページのタグを `forceRightPage` 等に割り当てる〕見開きで広告ページが除外され、保持ページが
  物理側(右だったページは右)に出てダミーページが自動配置される。〔ビューアを開き直す〕タグ制御 ON とマッピングが復元される。
- 核/表面: 選択・ナビゲーション・整形・適用対象計算ロジック=core(ViewModel 単体で観測可能にする)。描画=surface
- 受入観点: ロジックは unit exact(テストベクタ: 1023B→`1023 B`、1024B→`1.0 KB`、端での停止、連番 startValue+i)。
  画面は golden(G-1〜G-7)+承認者(ダークテーマ不要)

### 2.7 i18n と設定 (REQ-050〜REQ-052)

- 2 言語(ja 既定/en)。リソースはキー→文言の辞書。欠落キーは ja へフォールバック、それも無ければキー文字列を
  返す(例外を投げない) (REQ-050)。文言資産は原典 messages/ja.json・en.json から該当分を変換(K-BOM 資産)
- 言語切替は即時反映(再起動不要)+ settings.json へ永続化 (REQ-051)
- settings.json(%APPDATA%/ViewPrism2/): locale、ウィンドウ位置・サイズ・最大化状態、表示モード(グリッド/リスト・画像タブ用)、
  作業タブ表示モード(FL-004=D-b・ECO-039: タブ独立永続。未保存(null)時は画像タブの表示モードを初期値に読む)、
  最後に開いたビュー id、最後に選択したコレクション id(v1.3/CR-5・6。グリッド列数キーは CR-1 で廃止 —
  残存しても無視)。破損時は既定値で起動し、ファイルを再生成 (REQ-052)
- 核/表面: フォールバック・永続化=core。文言・書式=surface(翻訳資産へトレース)
- 受入観点: フォールバックと破損耐性は unit exact。表示言語は golden+承認者

### 2.8 観測契約(in-process 受入の入出力定義)

UI を介さず検査する部品の契約。シグネチャは論理形(言語上の型は E-BOM/M-BOM で確定)。

| ID | 部品 | 入力 | 出力 | 検査 |
|---|---|---|---|---|
| OC-1 | 条件評価器 (REQ-031) | 画像集合(各画像のタグ付け状態付き)+条件列 | 条件を満たす画像 id 集合 | 固定フィクスチャで exact(unit) |
| OC-2 | NodeGraph 構築器 (REQ-035) | 階層ノード列+タグ定義+タグ付け状態(status 込み) | ノード木(型・表示名・親子・順序・各ノードの条件) | 期待木と exact(unit) |
| OC-3 | パス→条件変換 (REQ-036) | 選択ノードまでのノード列 | 条件列(OC-1 の入力形式) | 期待条件列と exact(unit) |
| OC-4 | 整列器 (REQ-038) | 画像集合+sort_field+sort_direction | 整列済み id 列 | テストベクタ exact(unit) |
| OC-5 | スキャン判定器 (REQ-012) | ファイル情報(相対パス・サイズ・更新日時・ハッシュ遅延計算)+DB 状態 | 判定結果(skip/update/add-normal/add-pending+candidate)と適用後 DB 状態 | 遷移 4 ケース+境界を exact(unit〜L2) |
| OC-6 | 画像メモリキャッシュ (REQ-045) | 取得要求列 | 実ロード回数・保持集合 | LRU/期限/上限を exact(unit) |
| OC-7 | サイズ整形器 (共有・list/grid サイズ表示=ECO-004。旧 REQ-043 は ECO-023 で撤回) | バイト数 | 表示文字列 | ベクタ exact(unit): 0→`0 B`、1023→`1023 B`、1024→`1.0 KB`、1048576→`1.0 MB` |
| OC-8 | i18n 解決器 (REQ-050) | キー+ロケール+リソース状態 | 文言(フォールバック適用後) | 欠落ケース exact(unit) |
| OC-9 (v2.0) | 見開きペアリング計算器 (REQ-056) | 現在 index+総数+方向(right/left)+空白ページ開始 | (左ページ index \| 空白, 右ページ index \| 空白) | 全組合せベクタ exact(unit) |
| OC-10 (v2.0) | ページ送り計算器 (REQ-057) | 現在 index+総数+方向+ページ送りモード+SHIFT+操作(キー/クリック面)+空白ページ開始 | 遷移後 index(または変化なし) | 境界含む全組合せベクタ exact(unit) |
| OC-11 (v2.0) | スクロール現在位置追跡器 (REQ-055) | 各画像の表示矩形(縦位置・高さ)列+表示領域(高さ・スクロール量) | 現在位置 index(中央最近傍) | 疑似ビューポートのベクタ exact(unit) |
| OC-12 (v2.0) | 見開き高さ統一計算器 (REQ-058) | 両画像の自然寸法(片側空白可)+resizeMode+表示領域高さ | 統一表示高さ(または原寸指示)。アスペクト比は常に維持 | ベクタ exact(unit): 高低ペア・片側空白・90% 上限 |
| OC-13 (v2.0) | モード別位置記憶 (REQ-054) | (モード切替・位置変更)の操作列 | 各モードの記憶 index と切替後の現在 index | 操作列ベクタ exact(unit) |
| OC-14 (v3.0) | pHash 計算器 (REQ-061) | デコード済み画像ピクセル | 64bit pHash(16hex) | 決定性(同入力→同出力)+合成画像ベクタ(単色・既知パターン)+同一内容=距離 0・近傍<閾値・非近傍>閾値(unit) |
| OC-15 (v3.0) | 類似度変換器 (REQ-062) | ハミング距離(0〜64) | 類似度%(0〜100) | 段階式の全境界ベクタ exact(unit): d=0→100・各ブレークポイント・丸め規則 |
| OC-16 (v3.0) | 類似検索エンジン (REQ-064) | 基準画像 id+候補集合(status・コレクション付)+閾値(整数)+特徴量/類似度キャッシュ状態 | 類似度降順(同値 id 昇順)の (id, 類似度%) 列 | **候補=normal 限定**(フィルタはキャッシュ参照先行)・自身/コレクション境界・閾値境界(≧)・安定ソートを固定フィクスチャで exact(unit) |
| OC-17 (v3.0) | マージ計算器 (REQ-066) | マージ先タグ集合+マージ元タグ集合(**1 つ以上**・値付き)+各 status(normal 前提) | 統合後タグ集合(マージ先優先・NULL 補完・多元は id 昇順先勝ち・simple タグは存在 union)+マージ元 status=deleted | 衝突/NULL 補完/多元決着順/simple タグ union/原子性のベクタ exact(unit)。物理非破壊は L3 物理差分(CP-MERGE-001) |
| OC-18 (v3.0) | 特徴量キャッシュ無効化器 (REQ-063) | 画像メタ変化(file_size/modified_date/hash)+キャッシュ状態 | 再計算要否+ペア正規化キー+CASCADE 整合 | 内容変化での再計算・ペア (A,B)=(B,A) 正規化・削除 CASCADE を exact(unit/L2) |
| OC-19 (v4.0) | criteria 条件検索 (REQ-068) | 条件集合(hash/名前/拡張子/mtime 範囲/サイズ範囲・一部指定可)+対象 status 集合+コレクション+画像集合 | AND 一致の id 列(relative_path 昇順・同値 id 昇順) | 各条件 AND・status 対象・安定順・空条件は非実行を固定フィクスチャで exact(unit) |
| OC-20 (v4.0) | relink 確定器 (REQ-069) | missing 画像+選択候補(pending/untagged-normal)+DB 状態 | 適用後 DB 状態(missing 行=候補パス/メタ・status=normal・candidate_link_id=NULL・候補行削除・image_id 不変) | T4 遷移・候補プール(pending∪untagged-normal)・タグ付き候補拒否・ID 不変・原子性を exact(unit) |
| OC-21 (v4.0) | トラッシュ復元器 (REQ-070) | deleted 画像+物理ファイル存在有無 | 復元後 status(存在→normal / 不在→missing)。タグ/ID/特徴量不変 | T6/T7 分岐・deleted 限定・不変を exact(unit)。物理非破壊は L3 物理差分(S-26) |
| OC-22 (v4.0) | 完全削除器 (REQ-071) | deleted 画像+DB 状態 | 適用後 DB 状態(images 行削除・image_tags/features/similarity は CASCADE 消滅) | T8・CASCADE 整合(孤児ゼロ)・deleted 限定を exact(unit/L2)。物理非破壊は L3 物理差分(S-26) |
| OC-23 (ECO-022) | タグアクション解決器 (REQ-076) | 画像のタグ ID 集合+マッピング `action→tag_id?` | 支配アクション(`forceLeftPage/forceRightPage/spread/skip/leftPageEmpty/rightPageEmpty` のいずれか \| 無し) | 全順序 `skip>spread>forceLeftPage>forceRightPage>leftPageEmpty>rightPageEmpty`・未マッピング/現存しない tag_id は無視を固定フィクスチャで exact(unit) |
| OC-24 (ECO-022) | タグ制御ページプラン構築器 (REQ-076) | 順序付き (画像 index, 解決アクション) 列+方向(right/left)+空白ページ開始 | 見開き列(各=左/右ページ index \| 空白 \| 見開き占有)+ 各見開きの canonical 現在画像 index + 画像→見開き index の対応 | 6 アクション各/複合/端/空白開始合成/右開き左開き・**OFF-identity(アクション無し=OC-9 全位置一致)**・**空白開始×先頭配置アクション(seed 抑止=両面空白見開きを生まない)**・canonical 現在画像/画像→見開き対応を固定フィクスチャで exact(unit) |
| OC-25 (ECO-022) | タグ制御ナビゲーション計算器 (REQ-076) | 現在プラン見開き index+プラン見開き数+操作(次へ/前へ) | 遷移後プラン見開き index(端クランプ=変化なし)+ canonical 現在画像 index | プラン見開き単位の送り・端クランプ・SHIFT/pageTurnMode 非適用・モード復元の画像→見開き解決を固定フィクスチャで exact(unit) |

### 2.9 ビューア拡張モード (REQ-054〜REQ-059) — v2.0 新設

V1 の単一画像ビューア(§2.6 REQ-044)を 4 つの表示モードへ拡張する。REQ-044 の規則(起動経路・
Esc/閉じるボタン・現在位置/総数表示・端停止・呼び出し元一覧順)は全モードの共通基盤として不変。

**キーボード規則の共通基盤**: REQ-044 の「→/PageDown=次、←/PageUp=前(1 画像)」は normal と
scroll にそのまま適用される。見開き(spread)のみ矢印キーを REQ-057 の規則(ステップ・右開き反転)で
再定義する。**PageDown/PageUp は全モードで論理キー(常に 次へ/前へ・見開きでは現行ステップ分)であり、
読書方向で反転しない**。

**表示モードとモード切替 (REQ-054)**
- モード: normal(既定。§2.6 REQ-044 の単一画像表示そのもの)/ scroll / spread-right / spread-left
- ビューア上部のコントロールバーにモード切替と表示設定(REQ-058 の resizeMode/alignMode/gapMode+
  customGapPx の数値入力・ページ送りモード・空白ページ開始)の UI を集約する(細部レイアウトは
  K-DESIGN の範囲で製造判断、golden G-8 で承認)
- モードごとに最後に表示した画像 index を記憶し、モード切替時にそのモードの前回位置を復元する。
  **各モードの記憶の初期値はビューア起動時の index(クリックした画像)** — 未訪問モードへの初回切替は
  起動時の画像から始まる(原典準拠)。記憶はビューアを開いている間のみ(閉じると破棄。永続化しない — REQ-059)
- 画像外余白クリックで閉じる(REQ-044)は normal のみ。scroll/spread では無効(ページ送りクリックと
  衝突するため。Esc・閉じるボタンは全モード有効)

**スクロールモード (REQ-055)**
- 画像を一覧順(REQ-038 整列結果)で縦一列に表示。水平は中央揃え。画像間隔は gapMode(REQ-058)
- **画像サイズ**: 各画像は表示領域の幅に収める(幅超過時は縮小・拡大はしない・アスペクト比維持 —
  REQ-044 のフィット規則の幅適用)。横スクロールを生じさせない。縦方向は画像の自然な高さ(縮小時は
  比例縮小後の高さ)で積む
- **クリック**: スクロールモードでは画像・余白のいずれのクリックも操作を持たない(閉じない・送らない。
  誤操作防止 — REQ-054)
- 段階読み込み: 全件を一括読み込みしない。現在位置の前後ウィンドウを読み込み、上端・下端への接近で
  範囲を拡張する(ウィンドウ幅・接近閾値は実装定数 — E-BOM で確定し固定オラクルの合否対象にしない)
- 現在位置の追跡: ユーザーのスクロール停止後、表示領域の垂直中央に最も近い画像(画像矩形中心と
  表示領域中心の距離最小。**同距離の場合は一覧 index の小さい方**)を現在位置とする(OC-11)。
  この追跡が「現在位置/総数」表示とモード別位置記憶(REQ-054)に反映される
- ←→キーまたは外部からの位置変更(モード復元含む)では、対象画像の位置までスクロールする
- 個別画像の読み込み失敗は当該画像の位置にエラー表示(ファイル名+失敗の旨)を出し、前後の閲覧を
  継続できる(ビューア全体を壊さない — porting-spec 05§5 検収項目)

**見開きモード (REQ-056, REQ-057)**
- ページ配置(OC-9): spread-right(右開き・日本式)は 右=現在 index・左=現在+1。
  spread-left(左開き)は 左=現在 index・右=現在+1。ペア相方が末尾を超える場合は空白ページ
  (無地。エラー表示・文言を出さない)
- 空白ページ開始(startWithEmptyPage、既定 OFF): ON のとき index 0 では最初の画像を進行方向側
  (右開き=左ページ / 左開き=右ページ)に配置し反対側を空白に(表紙を単独で見せ、以後 index (1,2)(3,4)…の
  ペアを揃える — 0 起点 index)
- ページ送り(OC-10): ステップ = SHIFT 押下時 1 / 通常時 pageTurnMode(doublePage=2 既定、singlePage=1)。
  - キーボード: spread-right は →=前へ・←=次へ(進行方向反転 — 紙の右開き本と一致)。
    spread-left は →=次へ・←=前へ。**PageDown/PageUp は論理キーとして方向反転せず常に 次へ/前へ**
    (REQ-044 共通規則の継承。v2.0 確定 — 沈黙次元 §4)
  - ページクリック: **クリック領域は表示領域全体を左右半分に二分した各半面**(画像の上・画像外の
    余白・空白ページを含む面全体)。進行方向側の半面(右開き=左半面 / 左開き=右半面)=次へ、
    反対側の半面=前へ(原典準拠 — 見開きに「閉じる余白」は存在しない。REQ-054)
  - 境界: 前へは現在-step<0 なら 0 へ。次へは現在+step>総数-1 なら総数-1 へ、既に総数-1 なら何もしない。
    空白ページ開始 ON・index 0・2 ページ送りの「次へ」は index 1 へ(以後の偶奇を揃える)。
    末尾クランプの帰結として、最終画像がペアの相方として見えている状態からさらに「次へ」を送ると
    最終画像が単独ペア(最終, 空白)として再表示される — **意図的(原典準拠)**: 最終画像を読み位置として
    確定する遷移であり、エラーやループにしない
- 画像読み込み失敗はその面にエラー表示(もう片面と操作は継続可能)

**共通表示設定 (REQ-058)** — 適用対象をモードで限定する(原典実装準拠)。表中の**太字が既定値**:
| 設定 | 値(既定) | 適用対象 | 意味 |
|---|---|---|---|
| resizeMode | matchLargerHeight / matchSmallerHeight / **noResize** | 見開きのみ | ペアの高い方/低い方の画像高さへ両者を統一(OC-12)。統一高さは表示領域高さの 90% を上限。**片側が空白ページの場合(match 系のみの規則)は有効画像 1 枚の自然高さを統一高さとする(90% 上限は適用)**。noResize は原寸で「統一高さ」概念を持たない(片側空白でも同じ。表示領域超過時は領域内に収める=上限 100%。match 系 90% との非対称は原典準拠の意図的差)。アスペクト比は常に維持 |
| alignMode | top / **middle** / bottom | 見開きのみ | ページ内の垂直揃え(スクロールは対象外 — 原典仕様書記載はあるが実装が存在せず、実装準拠で不採用と確定) |
| gapMode (+customGapPx) | **tight** / loose | 見開きの左右間+スクロールの画像間 | tight=間隔 0(見開きでは左右画像を中央境界へ寄せる)。loose=customGapPx の間隔(customGapPx 既定 0 — loose+0px は tight と視覚同一になってよい) |

normal モードは V1 のウィンドウフィット表示(REQ-044)を維持し、上記設定の対象外。

**設定の永続化 (REQ-059)**
- mode/resizeMode/alignMode/gapMode/customGapPx/pageTurnMode/startWithEmptyPage を settings.json
  (§2.7 REQ-052 の機構)に保存し、次回ビューア起動時に復元。変更は即時保存
- customGapPx は 0〜100 の整数。範囲外・型不正の保存値は既定 0 として読む(破損耐性は REQ-052 準拠)
- モード別位置記憶(REQ-054)は永続化しない

- 核/表面: ペアリング・送り・追跡・高さ統一・位置記憶の各計算=core(OC-9〜13 として ViewModel 単体で
  観測可能にする)。描画・スクロール体感・設定 UI=surface
- 受入観点: 計算ロジックは unit exact(OC-9〜13 のベクタ)。設定永続化は unit ラウンドトリップ
  (REQ-052 の既存検査へ追加)。画面・操作感は golden G-8+承認者。大量画像のスクロール性能は
  探索プローブ(合否に入れない)

### 2.10 類似検索とマージ (REQ-061〜REQ-067) — v3.0 新設

重複画像を pHash で見つけて 1 枚に統合する。検出系(pHash 計算・距離・検索)は核、操作系(検索 UI・
マージ・トラッシュ)は表面。スコープは検出+マージまで(00-charter-v3.md §裁定記録): **detailed/ORB・
criteria 条件検索・relink 修復・トラッシュの復元/完全削除は本ループ対象外**(§6 カバレッジ監査)。

**2.10.1 知覚ハッシュ pHash (REQ-061, OC-14)**

pHash は決定的な DCT ベースのアルゴリズムで算出する。原典(sharp ベース)とのビット完全一致は要求しない
(CPOL-103: pHash は preserve_with_adapter)。本書のレシピを当方の正準とし、Phase 3 ADR で確定・pin する。
レシピ(spec 固定 — 決定性の根拠。下記の数値規約はすべて Phase 3 ADR で pin する):
1. デコード後の画像を 32×32 にリサイズ(アスペクト比は無視し正方化、補間は**双線形**に固定)し、
   グレースケール化する(輝度 y = 0.299·R + 0.587·G + 0.114·B を計算し、**`(int)(y + 0.5)` で 0.5 切り上げ**して
   8bit 整数化する。.NET 既定の銀行丸め(ToEven)は使わない — 決定性のため明示固定)
2. 32×32 の輝度行列に **直交正規化形の 2 次元 DCT-II**(orthonormal DCT-II)を適用する。行方向→列方向の
   2 パスで計算し、cos テーブルは**事前計算した固定定数表**を用いる。基底・符号規約は ADR で一意に固定する
   (中央値比較 (c>m) はスケール不変だが符号規約が違うとビットが反転するため固定が必須)
3. 出力行列の左上 8×8(行 i・列 j、0≤i,j<8 → 係数 [i][j])= 低周波 64 係数を取り出す
4. DC 係数 [0,0] を除いた 63 係数の中央値 m を求める(DC は全体輝度を表し支配的なため中央値計算から除外)
5. 8×8 の各係数 c(DC 位置も含む)について bit = (c > m) ? 1 : 0 を**行優先(row-major、[0,0]→[0,7]→[1,0]…)**で
   64bit に並べる。**DC 位置のビットは c=DCT[0,0] >> m でほぼ常に 1 となる低エントロピービット**であり、
   64bit 長を保つための意図的設計(実効情報量は概ね 63bit)
6. 64bit を最上位ビットから 16 桁の**小文字 16 進**で表現する

- リサイズ補間方式・グレースケール係数と丸め・DCT 規約・cos テーブルは決定性の生命線であり spec/ADR で
  固定する。SkiaSharp は版差で出力が変わり得るため**版を exact ピン**(53-service-bom)し、リサイズの
  サンプリング指定を明示する(K-BOM/ADR で確認)
- pHash は画像のピクセル内容のみに依存し、ファイル名・パス・更新日時には依存しない
- 縮退ケース: 単色画像など全係数が等しい場合、c=m となり手順 5 は bit=0 に倒れる(全 0 に近い pHash)。
  受入ベクタの代表として明記する
- 受入(OC-14): (a)同一ピクセル内容→常に同一 pHash(決定性)、(b)単色・既知パターン画像の手計算可能な
  pHash ベクタ(縮退ケース含む)、(c)同一内容=距離 0・微小変化(再エンコード/わずかな縮小)=閾値内の小距離・
  明確に異なる画像=大距離。16hex の網羅ベクタは手書きせず(b)(c)の性質ベースで凍結する
  (固定オラクル S-19 系 — 41-fixed-oracle で実装・本書外で閉じる)

**2.10.1a 8 オリエンテーション変種 (REQ-084 / ECO-048) — v4.1 追補**

- pHash 計算時に、32×32 格子(2.10.1 手順 1 の出力)の **8 オリエンテーション変種**の pHash を併せて算出する。
  変種は格子の**添字置換のみ**(再デコード・再リサイズなし)で決定的(INV-012)。順序は本書で固定:
  **[0]=identity [1]=rotate90(時計回り) [2]=rotate180 [3]=rotate270 [4]=flipH(左右反転)
  [5]=flipV(上下反転) [6]=transpose(主対角転置) [7]=transverse(反対角転置)**(二面体群 D4)
- 変種 [0](identity)は 2.10.1 レシピの pHash と**常に一致**する(既存 OC-14 契約は不変)
- 検出契約(S-40): 8 変換のいずれかを施した同一格子の pHash は、元格子の 8 変種のいずれかと距離 0
- 変種の計算は reader(adapter)の能力とする。変種非対応の reader(旧実装・テスト用 fake)では
  identity のみで動作する(後方互換 — 2.10.4 のフォールバック)

**2.10.2 距離と類似度 (REQ-062, OC-15)**

- 距離 = 2 つの pHash の**ハミング距離**(64bit XOR の立ちビット数、0〜64)。popcount で算出
- 類似度% = 距離の単調非増加な区分線形関数(spec 固定のブレークポイント)。
  ブレークポイント (距離, 類似度%): **(0,100) (5,90) (10,70) (15,50) (20,30) (25,10) (64,0)**
- **変換式(一意・決定的)**: 距離 d が含まれる区間 [d_lo, d_hi](対応 [s_lo, s_hi])に対し
  `raw = s_lo + (s_hi − s_lo) · (d − d_lo) / (d_hi − d_lo)`(double 演算)を計算し、
  **`sim = (int)Math.Floor(raw + 0.5)`(0.5 切り上げ=AwayFromZero)で整数化、[0,100] にクランプ**する。
  ブレークポイント上の距離はその区間の端点 s をそのまま採る。距離が増えれば類似度%は決して増えない(単調非増加)
- **期待値ベクタ(OC-15 / 固定オラクルの正)**: d=0→100, 3→94, 5→90, 7→82, 10→70, 12→62, 15→50,
  20→30, 25→10, 26→10, 27→9, 40→6, 64→0(最終区間は緩勾配のため丸め後に同値が連続し得るが非増加は保つ)
- この変換は pHash のビット表現に依存しない(距離値のみの関数)
- 受入(OC-15): 全ブレークポイント+上記期待値ベクタ+各区間の丸め境界をテストベクタで exact(unit)

**2.10.3 特徴量・類似度の永続化とキャッシュ無効化 (REQ-063, OC-18)**

- **image_features**(列: image_id PK、phash、file_size、modified_date、hash、last_calculated、**hash_adapter**(P-09)、
  **phash_variants**(REQ-084/ECO-048 — migration 006))。
  pHash 等の特徴量を画像ごとに保存。ORB 列(orb_descriptors/orb_keypoints)は**本ループでは作らない**(ORB defer)。
  **hash_adapter**(P-09)= pHash を計算した adapter 世代の識別子。decode 経路/レシピ/SkiaSharp 版が pHash の
  絶対値を動かす変更をしたら新 id を採番する(production 現行 = `skia-scaled-decode-v2` —
  ECO-054 経路一貫性=全フォーマット一様の中間縮小段で世代交代。8 変種の追加(ECO-048)は
  identity pHash の絶対値を動かさないため世代交代に**該当しない**)
  **phash_variants**(REQ-084)= 8 オリエンテーション変種の pHash(2.10.1a の順序)を `,` で連結した TEXT。
  NULL = 変種なし(旧レコード/変種非対応 reader)
- **image_similarity**(列: cache_key PK、image_id1、image_id2、similarity_score、last_compared)。
  画像ペアの類似度キャッシュ。**ペアは文字列比較で小さい方を image_id1・大きい方を image_id2 に正規化**し、
  **cache_key = `{image_id1}-{image_id2}`**(本ループは pHash 単一モードのためモード識別子は付けない。
  ORB 導入時に `-{mode}` 接尾辞を追加する将来拡張余地のみ残す)。(A,B) と (B,A) は同一キャッシュ
- **無効化は内容ベース + adapter 世代 + 変種欠落**: 対象画像の file_size / modified_date / hash のいずれかが
  image_features 記録と異なれば、**または記録の hash_adapter が現行 adapter と異なれば**(P-09)、
  **または現行 reader が変種対応で記録に phash_variants が無ければ**(REQ-084 — 旧レコードの自動アップグレード)、
  特徴量を無効として再計算する。
  後者は adapter 世代交代(例 full-decode→scaled-decode)時に**旧 adapter 由来の pHash 値を自動的に無効化**し、
  連鎖無効化で旧類似度キャッシュも purge する(adapter をまたいだ値の混在=ランキング破壊を防ぐ)。
  旧 DB(P-09 以前)の hash_adapter は NULL=空で、現行 adapter と必ず不一致のため初回検索で再計算される。
  **時間ベース失効(原典の 24h TTL)は採用しない**(決定性・単純性。内容変化の検知で十分 — §4 第3回掃討)
- **連鎖無効化**: 画像の特徴量(pHash)を再計算した場合、その画像が image_id1 または image_id2 に含まれる
  image_similarity 行を**削除**する(古い類似度を返さない)。次回検索時に再計算・再保存する
- 画像削除時は FK CASCADE で両テーブルの関連行を削除する
- 受入(OC-18): 特徴量の保存→取得ラウンドトリップ、内容変化での再計算+関与ペアの連鎖無効化、ペア正規化と
  cache_key 生成、CASCADE 削除を exact(unit/L2)。マイグレーション適用後スキーマ(列・FK・索引・既定値)を
  新規作成 DB と一致(REQ-004 の L2 検査へ追加)

**2.10.4 類似検索 (REQ-064 エンジン=核 / REQ-065 UI=表面, OC-16)**

- エンジン: 基準画像 1 枚に対し、**同一コレクション内**(REQ-053)の **status=normal の画像のみ**を候補とし
  (基準画像自身は除外)、各候補との pHash 類似度を算出して**閾値以上(≧)**(既定 70・範囲 50〜100・整数刻み)を
  **類似度降順(同値時は id 昇順で安定)**で返す。
  **候補の status フィルタ(normal のみ)はキャッシュ参照より先に適用**し、非候補(deleted/missing/pending)は
  キャッシュ値があっても結果に含めない。フィルタ後の候補について特徴量・ペア類似度をキャッシュ(2.10.3)で
  参照し、無ければ計算して保存する。検索は UI をブロックしない(非同期実行・進捗通知が可能)
  - **ペア距離(REQ-084 / ECO-048)**: ペアの距離は **序数比較で小さい id の identity pHash × 大きい id の
    全 8 変種(2.10.1a)のハミング距離の最小値**とする(役割が id 順で決まるため対称・決定的 —
    ペア正規化キャッシュ(2.10.3)と整合)。変種 [0]=identity を含むため距離は identity 同士の距離を
    **上回らない**(スコアは従来値以上 = 既存検出の単調拡張・検出漏れの純減)。
    大きい id 側の特徴量に変種が無い(旧レコード/変種非対応 reader)場合は identity 同士の距離のみ(後方互換)
  - **候補を normal に限定する根拠(v3.0 — G2 補正)**: V3 のユースケースは「重複 normal 画像を見つけて統合」。
    missing は物理ファイルが無く pHash を新規計算できない。pending/missing を候補にする検索は relink 修復の
    用途であり後続ループ(V4)へ defer(§6)。これにより INV-010(既定の表示・抽出は normal のみ)とも整合する
- UI: 一覧/グリッドで画像 1 枚を選択して起動。閾値スライダー(50〜100・整数・既定 70)を持つ。
  **本実装の照合は pHash のみ**であり、原典が持つ精度モード(simple=pHash / detailed=pHash+ORB)の**選択肢は
  表示しない**(RVP-REPAIR-005/§裁定記録 4 — 効かない設定の誤解防止。ORB は後続ループ)。
  結果は類似度%付きで降順に一覧表示し選択できる。検索中は進捗、結果 0 件は空状態を表示
- 性能(1,000 枚規模)は探索プローブ P-07 対象(原典は 1,000 枚で約 1.1 秒・5,000 枚でフリーズ感の記録 —
  42-exploratory-probes で実装・合否に入れない・本書外で閉じる)
- 受入: エンジンは OC-16(候補抽出=normal 限定・自身/コレクション境界・閾値境界(≧)・安定ソートを
  固定フィクスチャで exact、unit)。UI は golden G-9+承認者

**2.10.5 マージ (REQ-066 規則=核 / REQ-067 UI=表面, OC-17)**

- 規則: マージ先(保持)1 枚とマージ元(統合)1 枚以上を指定する。マージ先・マージ元はいずれも
  **status=normal を前提**とし、マージ先とマージ元が同一の指定・マージ元の重複は拒否する。
  操作(単一トランザクション・原子 — INV-006):
  1. **タグ集約(union)**: マージ元のタグをマージ先へ付与する。両方に同一タグ(同一 tag_id)がある場合は
     **マージ先の値を保持**し、**マージ先の値が NULL/空でマージ元に値があればマージ元の値を採用**する
     (RVP-MERGE-002=マージ先優先・§裁定記録 3)。simple タグ(値なし)は存在の union のみ。
     付与は UPSERT(REQ-026)に従い (image_id, tag_id) は高々 1 行(INV-003)
  - **マージ元が複数の場合の決着順**: マージ元を **id 昇順**で処理する。あるタグについてマージ先が NULL/空の
     とき、**最初に非空値を与えたマージ元の値を採用**し、以降のマージ元の同タグ値では上書きしない
     (決定的・id 昇順先勝ち)
  2. **マージ元の status を deleted にする**(論理操作)。マージ元の image_tags は削除しない
     (deleted ステータスで一覧から除外されるのみ。完全削除/復元は後続ループ)。
     deleted 化したマージ元の image_features/image_similarity 行は残置してよい(候補は normal 限定=2.10.4 の
     ため検索に混入しない)
  3. **物理画像ファイルは一切移動・削除・変更しない**(INV-009 woven 安全制約)。本ループはこの制約の
     **初の実アクション適用**である
  4. **(ECO-044/IMG-011 裁定③)マージ操作ログを同一トランザクションで 1 行記録する**
     (merge_operations: sources・タグ差分=追加行+NULL/空補完行と元値・実行時刻・
     destination/sources の内容指紋=status+hash+タグ集合)
  - 失敗時は全ロールバック(部分適用を残さない)
- **タグ統合は常時 ON(ECO-044/IMG-011 裁定②)**: 「マージ時にタグを含める」の OFF 選択肢は
  v1 に存在しない(UI にトグルを置かない)
- **取り消す(ECO-044/IMG-011 裁定③)**: 通常の UI Undo ではなく**操作ログに基づく補償操作**
  (原子: 追加タグ行の削除+補完値の元値復帰+sources deleted→normal+undone_at マーク)。
  実行可能条件= sources 行が全て存在(完全削除で不在なら不可)かつ destination/sources の
  現在指紋がログ指紋と一致(=マージ直後から不変)かつ未取り消し。条件を満たさない場合は
  完了パネルの「取り消す」を不活性化し理由を表示する
- UI: 類似検索結果または複数選択からマージ先(1 枚)とマージ元(1 枚以上)を指定して実行できる。実行前に
  マージ先と整理対象(マージ元)をトレイのサムネイル一覧で視認できる。
  **統合後タグの事前プレビューと物理非破壊の機能個別の明示は提供しない(ECO-052 裁定 b・v4.1 改訂)**:
  誤マージ保護は完了パネル+補償 Undo(§2.10.5 の「取り消す」)が担い、物理非破壊は INV-009 の
  プロダクト全体原則として L3 物理差分(CP-MERGE-001)で機械実証する(機能個別の UI 通知はノイズとして
  不採用。例外=ゴミ箱の完全削除の確認は「元に戻せません」の不可逆警告とセットで G-10 裁定どおり存続)。
  マージで deleted になった画像が「見えないまま消える」ことを避けるため、最小のトラッシュ(deleted 画像一覧)
  表示を提供する(**選択中コレクション内**(REQ-053)の deleted 画像の一覧と件数のみ。**復元・完全削除の操作は
  後続ループ** — RVP-STATUS-003/004)
- 受入(二経路は相補・両方必須):
  - **核**: OC-17(タグ union 衝突=マージ先優先・NULL 補完・多元 id 昇順先勝ち・simple union・status 遷移・
    原子性を unit exact)
  - **物理非破壊**: **L3 物理差分 CP-MERGE-001**(操作前後でマージ元・マージ先のファイルのバイト列と mtime が
    不変=自動の固定検査。33-control-plan で定義・**本書外で閉じる**)
  - **表面**: マージ UI・統合タグプレビュー・トラッシュ一覧は golden G-9+承認者(承認者の目視確認。L3 CP の
    自動実証とは別経路で、視覚・操作の妥当性を担保する)

- 核/表面: pHash 計算・距離変換・検索エンジン・マージ規則・キャッシュ無効化=core(OC-14〜18 として ViewModel/
  サービス単体で観測可能にする)。検索 UI・マージ UI・トラッシュ表示・進捗描画=surface
- 受入観点まとめ: 核は unit exact(OC-14〜18)+マージの物理非破壊は L3 物理差分。表面は golden G-9+承認者。
  大規模類似検索の応答性は探索プローブ(合否に入れない)

### 2.11 修復ライフサイクル (REQ-068〜REQ-072) — v4.0 新設

画像状態機械(normal/pending/missing/deleted)を閉じ、missing の **relink(再リンク)**、deleted の
**復元・完全削除**を規定する。V3 が開いた deleted・トラッシュ閲覧を完成させる。INV-009(物理非破壊)の
**第 2 の実アクション**(復元・完全削除)を含み、L3 物理差分 CP(S-26)で前後不変を実証する。

**2.11.0 状態機械(T1〜T8)** — 全遷移を列挙(これ以外の遷移は起こさない/拒否する)。`△`=v4.0 新規。

| # | 遷移 | トリガ | 効果 | 前提/拒否 |
|---|---|---|---|---|
| T1 | →normal | スキャン: 新規・missing 一致なし(ScanJudge 3b) | normal 登録 | — |
| T2 | →pending | スキャン: 新規が同フォルダ同ハッシュ missing に一致(3a) | pending 登録+candidate_link_id | — |
| T3 | normal→missing | スキャン: 記録パスのファイル消失 | missing 化(image_id 不変) | — |
| T4 | missing(+候補消費)→normal | relink 確定(§2.11.2) | missing 行へ候補のパス/メタ上書き・status=normal・candidate_link_id=NULL・**候補行は削除** | 対象 missing でない/別コレクション/候補が許可外 status・タグ付き → 拒否 |
| T5 | normal→deleted | マージ元の論理削除(§2.10.5) | status=deleted(物理非破壊) | — |
| T6 △ | deleted→normal | トラッシュ復元 **かつ 記録パスに物理ファイル存在** | status=normal | deleted 以外は拒否 |
| T7 △ | deleted→missing | トラッシュ復元 **だが 物理ファイル不在** | status=missing(幽霊 normal 防止) | deleted 以外は拒否 |
| T8 △ | deleted→(行消滅) | 完全削除 | images 行 DELETE → image_tags/image_features/image_similarity は FK CASCADE。**物理ファイル非破壊** | deleted 以外は拒否 |
| T9 △ | missing→deleted | 除外(修復 UI・§2.11.6 / ECO-005) | status=deleted(物理非破壊・トラッシュへ。復元は T6/T7 経路) | **missing 以外は拒否**(ValidationError) |

**2.11.1 criteria 条件検索 (REQ-068, OC-19)**
- 条件(指定されたもののみ AND): hash(SHA-256 完全一致)/ ファイル名(部分一致・大小無視)/ 拡張子 /
  mtime 範囲(以上・以下)/ ファイルサイズ範囲(以上・以下)。同一コレクション内に限定
- **対象 status は呼び出し用途で固定**: 単体検索=**normal**(INV-010 整合)/ relink 候補探索=**pending ∪ normal**(§2.11.2)
- 結果は安定順(relative_path 昇順・同値 id 昇順)。**条件が 1 つも指定されない場合は実行せず全件を返さない**(誤操作防止)
- 検索は UI をブロックしない。受入: 各条件の AND 絞り込み・status 対象・安定順・空条件非実行を固定フィクスチャで exact(unit)

**2.11.2 relink 再リンク (REQ-069, OC-20)**
- 候補プール(同一コレクション): ①pending(ScanJudge 3a が付けた candidate_link_id 経由)②criteria 検索(§2.11.1)結果
  のうち status ∈ {pending, normal}
- **タグ安全ガード(G2 裁定)**: 置換候補は **user タグを持たない(image_tags 行が無い)** ものに限る。タグ付き候補は
  消費するとそのタグが失われるため **relink 拒否**し、マージ(§2.10.5)を案内する。pending は新規スキャンで未タグ、
  未タグ normal は安全に消費可。**= 置換候補 status は pending ∪ untagged-normal**(§ゲート記録 v4.0 B-1)
- **candidate_link_id は『候補ヒント』であって最終真実ではない**: ScanJudge 3a が付与するが、確定操作(ユーザー選択)で
  初めてリンクが成立する。ヒントだけで自動リンクしない
- 確定(T4・単一トランザクション・原子): missing 行へ選んだ候補の relative_path/file_name/file_size/modified_date/hash を
  上書き・status=normal・candidate_link_id=NULL、**候補行は削除**。**missing 側 image_id は不変(INV-001 — タグ参照保全)**
- 複数候補は安定順(relative_path 昇順・同値 id 昇順)で提示。遷移表(T4)外の入力は拒否。
  受入: 候補列挙(pending+criteria・安定順・タグ付き除外)・確定(上書き・status・candidate_link_id=NULL・候補削除・ID 不変)・
  拒否・原子性を固定フィクスチャで exact(unit)

**2.11.3 トラッシュ復元 (REQ-070, OC-21)**
- deleted 画像のみ対象。記録パス(sync_folder.path + relative_path)に**物理ファイルが存在すれば status=normal(T6)、
  不在なら status=missing(T7)**(deleted→normal 固定にすると幽霊 normal 行が生じる — INV-013)。
  物理ファイルには触れない(読み取りの存在確認のみ・open/move/delete しない — INV-009)
- image_id・付与タグ・特徴量は不変。単一トランザクション。受入: 存在/不在の分岐・タグ/ID/特徴量不変・deleted 限定を
  unit exact + **物理非破壊は L3 物理差分(S-26・本書外で閉じる)**

**2.11.4 完全削除 (REQ-071, OC-22)**
- deleted 画像のみ対象。images 行を削除し、image_tags/image_features/image_similarity は **FK CASCADE** で消滅(T8)。
  **物理画像ファイルには一切触れない**(INV-009 第 2 の実アクション — INV-014)。単一トランザクション・原子
- 受入: images 行削除・CASCADE(孤児行ゼロ)・deleted 限定を unit exact + **物理非破壊は L3 物理差分(S-26)**

**2.11.5 修復ライフサイクル UI (REQ-072, golden G-10)**
- (a) criteria フォーム(hash/名前/拡張子/mtime/サイズ・AND)= **再リンク候補の絞り込み条件**、(b) relink フロー
  (missing 選択→候補自動提示→確定)、(c) トラッシュ(deleted 一覧)からの復元・完全削除
- **(GF-V4-03・原典 AdvancedRepairModal 準拠) 検索ボタン=選択中 missing の再リンク候補(Pending∪Normal)を
  現在のフォーム条件で再探索**する(検索結果は再リンク候補に統一。Normal 限定の別「検索結果」リストは持たない)。
  これにより「候補はあるのに検索結果が空(=pending が Normal 限定検索から漏れる)」の不一致を解消。
  検索は missing 選択中のみ活性。条件を編集(例: hash を消してサイズだけ)→検索 で候補を広げ/絞れる
- **(GF-V4-01・自動修復 — 原典 view-prism RepairModal 準拠) missing を選択したら、その画像自身の
  hash+拡張子+サイズを criteria へ自動導出し、候補を手入力なしで提示する**(既定 useHash/useExtension/useSize=ON、
  filename/mtime はリネームで変わるため OFF)。ユーザーは criteria を編集して再エンコード等へ手動で広げられる。
  注: 物理ファイルが元の記録パスへ戻った missing 自身は候補にしない(自己除外 — 原典も同挙動。スキャン側の
  missing→normal 回復は本ループ範囲外の improve-on-port 候補)。再エンコード(hash 変化)の pHash 類似修復は後続(原典 AdvancedRepairModal 相当)
- **(GF-V4-02・原典 view-prism 準拠) 修復を開く時にコレクションをスキャンしてリンク切れを検出する**
  (記録パスのファイル消失→missing 化 / リネーム後の新ファイル→pending 登録=自動修復候補)。明示の再スキャン
  なしで broken link が修復一覧へ現れる(原典は再起動/コレクション展開で検出)。ルート消失時の一括 missing 化は
  ScanService が抑止(誤検出防止 — V1 F-3 保護)。見出しに **「N 件のリンク切れ画像(M 件が自動修復可能)」を表示**
  (M=hash+拡張子+サイズで候補が**ちょうど 1 件**の missing 数 — 原典 RepairModal の auto-repair count 準拠)
- **完全削除は非破壊を明示する文言を必須**(例『DB から完全に除去します(画像ファイルは削除されません)』— 裁定 6)。
  復元で物理不在のため missing 化した場合は結果が分かるよう表示。破壊的に見える操作(完全削除)は実行前確認を伴う
- **(GF-V4-04・表示パリティ契約 — 原典 AdvancedRepairModal 準拠) 再リンク候補は、ユーザーが再リンク可否を判断するに
  足る情報を提示する**: **サムネイル + ファイル名 + 相対パス + サイズ + 更新日時**。原典は各候補をこの 5 要素のカードで
  描画する(`AdvancedRepairModal.tsx` の検索結果カード)。サムネイルは候補(実体が存在する pending/normal)の物理パスから
  生成する(missing 自身は実体欠損のためサムネイルを持たない=ファイル名+パスのみ)。**表示パリティは固定オラクル(論理)
  では捕捉されない**(ヘッドレス工場は視覚を検証不能)ため、ported 画面ごとに「提示すべき情報」を本契約として明記し golden で
  確認する。注: 旧 RelinkWindow(フォルダ管理経由)はサムネイル非表示の簡易 UI として残置(本契約の対象は修復ライフサイクル UI)

- 核/表面: criteria マッチ・relink 確定規則・復元の存在分岐・完全削除の CASCADE=core(OC-19〜22)。検索/修復/トラッシュ UI=surface
- 受入観点: 核は unit exact(OC-19〜22)+復元/完全削除の物理非破壊は L3 物理差分(S-26)。表面は golden G-10+承認者

**2.11.6 一括自動修復・除外 (REQ-072/073, ECO-005)**
- **自動修復可能**=その missing の hash+拡張子+サイズ(§2.11.2 の auto-criteria)で候補が**ちょうど 1 件**(一意=曖昧でない)。0/2 件以上は自動修復可能としない(§2.11.5 の auto-repair count と同基準)。
- **単一自動修復**: 当該 missing の auto-candidate(1 件)を relink 確定(T4・§2.11.2)する。1 件でなければ何もしない。
- **すべて自動修復**: 自動修復可能な全 missing を順に relink する。**タグ付き候補で拒否(INV-015)等の失敗は当該 1 件をスキップ**し、成功数を結果に出す(原典 RepairModal の失敗握り潰しと同義。一括処理を 1 件の失敗で止めない)。
- **除外(T9 / missing→deleted)**: 修復をあきらめる missing を status=deleted にしてトラッシュへ移す(復元可=T6/T7)。**missing 以外は拒否**(ValidationError)。物理ファイルに触れない(INV-009)。relink・除外は core サービス(RelinkService.CommitRelinkAsync / TrashService.ExcludeAsync)経由のみ。一括/単一の判定・反復は ViewModel に置き unit 検査可能にする
- 受入: ExcludeAsync(missing→deleted・missing 限定・物理非破壊)は unit + 固定オラクル S-31。AutoRepairAll(auto-repairable 集合のみ修復・失敗スキップ)は unit。ボタン動線・結果文言は golden(G-10)

### 2.12 タグ制御モード (REQ-076〜REQ-078) — v4.0 新設・ECO-022・仕様§5

§2.9 の見開き(REQ-056/057)を拡張し、画像に付いたタグで見開き配置を自動制御する。**有効/無効は設定 `enableTagControl`(既定 OFF)**。OFF のときビューアは §2.9 と**完全同一**(回帰保証点 — 既存 OC-9/10 不変)。

**2.12.0 動機・スコープ (REQ-076 / TC-1)**

動機=雑誌スキャンの広告ページ除外。広告を**表示から除外**しつつ、物理的に右だったページは右に出す(左右パリティ保存)。ダミーページ画像の手挿入を避け、タグでパリティを制御する。有効モード(仕様§5.5):

| モード | タグ制御 | 挙動 |
|---|---|---|
| 見開き(右開き/左開き) | ◎ フル | 6 アクション全適用(2.12.2 配置エンジン) |
| スクロール / ノーマル | △ | `skip` のみ(列から除外)。他 5 アクションは無視 |
| その他 | × | 無効 |

**2.12.1 アクション解決 (REQ-076 / OC-23)**

予約アクション 6 種(仕様§5.3。画面の左右ページ基準):

| アクション | 意味 |
|---|---|
| `forceLeftPage` | 見開きで**画面左ページ**にのみ配置(必要なら手前に空白を挿入して側を合わせる) |
| `forceRightPage` | 見開きで**画面右ページ**にのみ配置(同上) |
| `spread` | 1 枚で見開き全体(左右 2 ページ分)を占有 |
| `skip` | 表示対象から除外(列から除去) |
| `leftPageEmpty` | その画像の見開きの**画面左を空白**にし、画像を画面右へ |
| `rightPageEmpty` | その画像の見開きの**画面右を空白**にし、画像を画面左へ |

- 解決入力=画像のタグ ID 集合(`ImageEntry.Tags` の `EvalTagValue.TagId`)+ マッピング `action→tag_id?`(REQ-077)。
- 画像のタグ ID にマッピング先 tag_id が含まれるアクションを収集し、**全順序 `skip > spread > forceLeftPage > forceRightPage > leftPageEmpty > rightPageEmpty` で支配アクション 1 つ**(TC-3)。該当なしは「アクション無し」。
- マッピングされていないタグ・現存しない tag_id は無視。

**2.12.2 ページプラン構築 (REQ-076 / OC-24)** — 純粋計算(Core)。`(画像列, 解決アクション列)→ スロット列 → 見開き列`。

- **スロット列 S**=ページの列(各要素=画像 index / 空白 / 見開き占有マーカー)。`skip` 画像は事前に列から除去。
- **チャンク**: 見開き k = `(S[2k], S[2k+1])`。**画面マッピング(§2.9 準拠)**: 右開きは `S[2k]=右ページ・S[2k+1]=左ページ`、左開きは `S[2k]=左ページ・S[2k+1]=右ページ`(偶数スロット=先読み面)。
- **空白ページ開始 ON** は、**先頭の非 skip 画像がアクション無しのとき**のみ `S[0]=空白` を先頭に予約する(§2.9 と同値=表紙が進行方向側に単独・この seed 空白は先頭画像 `S[1]` の相方)。**先頭の非 skip 画像が配置アクション(`forceLeftPage`/`forceRightPage`/`leftPageEmpty`/`rightPageEmpty`/`spread`)を持つ場合は seed を予約しない** — そのアクションが開始パリティを定める(force のアンカー用途・TC-5)。これにより seed 空白が孤立して両面空白の見開きが生じることを防ぐ(下記 不変条件)。
- 補助 `padToParity(p)`: 次スロットのパリティが p になるまで空白を 1 つずつ emit(p=0→次を偶数, p=1→次を奇数)。**「次スロット」=次に `emit()` が書き込むスロット index(=現在のスロット列長・0 起点。空白開始 ON では `S[0]` 予約済のため基点長=1)。**
- 各画像(非 skip)を順に処理(`画面右スロットのパリティ` = 右開き:偶/左開き:奇、`画面左スロットのパリティ` = 右開き:奇/左開き:偶):

| 解決アクション | 構築規則 |
|---|---|
| 無し | `emit(画像)` |
| `forceRightPage` | `padToParity(画面右)`; `emit(画像)` |
| `forceLeftPage` | `padToParity(画面左)`; `emit(画像)` |
| `leftPageEmpty` | 画像を画面右に・画面左を空白に置く同一見開き(右開き: `padToParity(0); emit(画像); emit(空白)` / 左開き: `padToParity(0); emit(空白); emit(画像)`) |
| `rightPageEmpty` | 画像を画面左に・画面右を空白に置く同一見開き(右開き: `padToParity(0); emit(空白); emit(画像)` / 左開き: `padToParity(0); emit(画像); emit(空白)`) |
| `spread` | `padToParity(0)`; 見開き 1 つを当該画像が占有(両スロットを 1 枚で span。`S[2k]=S[2k+1]=spread(画像)`) |

- **force 系と empty 系の差(明文)**: `forceLeftPage`/`forceRightPage` は当該画像 1 枚のみを emit し**相方スロットを予約しない**(後続画像がそのまま相方に流入する=例1で C が B の相方になる)。`leftPageEmpty`/`rightPageEmpty` は**明示的に空白も emit する**(相方を空白で固定し後続画像を流入させない)。これが両者の本質差。
- **不変条件: 両面空白の見開きは生じない**(canonical 現在画像 §2.12.4 が常に定義されることの根拠)。証明: 空白の発生源は (1) 空白開始 seed(`S[0]` のみ・先頭画像がアクション無しのときだけ予約=その先頭画像 `S[1]` の相方)、(2) `padToParity` 空白、(3) empty 系の明示空白 のみ。(2)(3) はいずれも**直後または直前の実画像と同一見開きに入る相方として emit される**(pad 空白は次に emit する画像の手前・empty 空白は当該画像と対で emit)。(1) は seed 抑止規則(上記)により孤立しない。よって両スロットが空白の見開きは構築上発生しない。先頭が配置アクションのときも seed を出さないため `(S[0],S[1])` が両面空白になることはない(G2' 反例の封じ込め)。
- **OFF-identity(回帰保証)**: 全画像アクション無し(または enableTagControl OFF)のとき、本プランの各見開きは §2.9 の `SpreadPairCalculator` と**全位置で一致**(空白ページ開始 ON/OFF とも検算済=固定オラクル S-32 基底。先頭画像アクション無し=seed 予約のため §2.9 の表紙単独と同一)。
- 空白ページの視覚は §2.9 と同じ(無地・文言/枠なし)。

**2.12.3 worked example(右開き・正準シナリオ)**

スキャン順 idx: `0:A(記事) 1:広告 2:B(記事) 3:C(記事)`。物理見開き `[右:A|左:広告][右:B|左:C]`。狙い `[右:A|左:空][右:B|左:C]`。

| タグ付け | 解決後の非skip列と規則適用 | スロット列(右開き) | 見開き列(右/左) |
|---|---|---|---|
| 広告=`skip`, B=`forceRightPage` | A(無): emit→s0 / B(force右=偶): 次s1奇→pad空白 s1, emit B s2 / C(無): emit s3 | `[A, 空, B, C]` | `[右:A\|左:空]`, `[右:B\|左:C]` ✅ |
| 広告=`skip`, A=`leftPageEmpty` | A: pad偶(s0偶OK), emit A s0, emit 空 s1 / B: s2 / C: s3 | `[A, 空, B, C]` | `[右:A\|左:空]`, `[右:B\|左:C]` ✅ |
| 先頭広告 `0:広告 1:A 2:B 3:C`、広告=`skip`, A=`forceLeftPage`(開始アンカー) | A(force左=奇): 次s0偶→pad空白 s0, emit A s1 / B: s2 / C: s3 | `[空, A, B, C]` | `[右:空\|左:A]`, `[右:B\|左:C]` ✅(以後無タグで流れる) |
| **漫画見開き(左右2枚)** `0:表紙 1:右半 2:左半 3:p`、空白ページ開始 ON | 先頭に空白予約 s0 / 表紙: s1 / 右半: s2 / 左半: s3 / p: s4 | `[空, 表紙, 右半, 左半, p]` | `[右:空\|左:表紙]`, `[右:右半\|左:左半]` ✅(見開き絵が同一見開きで揃う), `[右:p\|左:空]` |
| **spread×padToParity** `0:X 1:Y`、Y=`spread`(空白開始 OFF) | X(無): emit s0 / Y(spread): padToParity(0)→次s1奇→空白 s1, Y が見開き占有 s2・s3 | `[X, 空, (Y,Y)]` | `[右:X\|左:空]`, `[Y 見開き占有]` ✅(spread は単独見開きを占有・X は片側空白で確定) |
| **seed 抑止**(G2' 反例の封じ込め) `0:A 1:B`、A=`forceRightPage`、**空白開始 ON** | **先頭 A が配置アクション→seed 予約せず** / A(force右=偶): 次s0偶OK, emit A s0 / B(無): emit s1 | `[A, B]` | `[右:A\|左:B]` ✅(両面空白の `P[0]` を生まない・canonical=A) |

**漫画見開きの扱い(TC-5・スコープ確定)**: 左右で 1 枚の絵になる漫画の見開きは、(a) **ワイド 1 枚画像**なら `spread` アクションで見開き占有(ノーマル/スクロールでも 1 枚画像として既に正しく表示)、(b) **左右 2 枚画像**なら**見開きモードで空白ページ開始 ON(漫画標準)または右半 `forceRightPage`** で同一見開きに揃える(上表 4 行目)。**ノーマル/スクロールで 2 枚を 1 見開きに合成表示することは非対応**(§5.5 と一貫・現状仕様のまま)。

**2.12.4 ナビゲーション・位置/総数 (REQ-076 / OC-25)**

タグ制御 ON の見開きナビゲーションは**プラン見開き単位**で行う(§2.9 の index アンカー送りからの意図的切替。OFF では §2.9 のまま=回帰ゼロ)。プラン見開き列を `P[0..N-1]`(N=見開き数)とする。

- **現在位置=プラン見開き index** `cur ∈ [0, N-1]`。起動時/モード復元時は「対象画像を含むプラン見開き」を cur とする(下記 画像→見開き対応)。
- **送り(次へ/前へ)**: 次へ=`min(cur+1, N-1)`、前へ=`max(cur-1, 0)`(端でクランプ=変化なし・ループや例外なし)。矢印キーの右開き反転(→=前へ等)・クリック半面=進行方向側が次へ、の規則は **§2.9 と同一**(画面方向の規則でありプランに非依存)。
- **1 ページ送り/SHIFT は適用しない(M-2 解決)**: §2.9 の 1 ページスライド(ペアの動的再構成)は**固定プランには概念上存在しない**(レイアウトはタグで確定済み)。タグ制御 ON では `pageTurnMode`/SHIFT に依らず常にプラン見開き 1 つ分送る。§2.9 の「空白ページ開始 0→1 特殊送り」も**プラン構造(表紙単独の見開き `P[0]`)に吸収**され別規則を要しない。
- **プラン見開き ↔ 画像の対応(OC-24 が供給)**:
  - **見開き→現在画像(canonical・M-1 解決)**: 当該プラン見開きの**先読み面 `S[2k]` が画像ならそれ**、空白なら後読み面 `S[2k+1]` の画像、`spread` 占有なら占有画像。**両面空白の見開きは構築上生じない**(§2.12.2 不変条件=空白は seed 抑止/相方 emit により必ず実画像と同一見開きに入る)ため canonical は常に一意に定義される。
  - **画像→見開き**: 各非 skip 画像はちょうど 1 つのプラン見開きに属する(通常配置・`spread` 占有のいずれでも一意)。モード切替時、ノーマル/スクロール(画像 index 基準)との位置記憶受け渡し(§2.9 REQ-054)は **canonical 現在画像 index** で行う。
- **位置/総数**: 総数=**非 skip 画像の枚数**(skip は除外)。「n-m / total」の n,m は当該プラン見開きの 2 画像の**非 skip 列 1 起点位置**を**読み順(先読み面 `S[2k]` を先に)**で並べる(§2.9 同様)。片側空白・`spread` 占有(M-3 解決)は**単独表示「n / total」**(n=canonical 現在画像の位置)。空白ページ自体は番号を持たない。

**2.12.5 永続化 (REQ-077)** — §2.9 REQ-059 / CP-SET-009 の破損耐性パターンを踏襲。

- `enableTagControl`(bool・既定 OFF)+ `action→tag_id?`(6 個・既定すべて未割り当て)を settings.json(§2.7)へ保存し、ビューア起動で復元。変更(トグル・割当/クリア)は即時保存。
- 破損・欠損・未知値は安全な既定(OFF / 未割り当て)へ正規化。マッピング先 tag_id が現存しない場合、永続値は保持してよいが**解決時は未割り当て**として扱う(2.12.1)。

**2.12.6 設定 surface (REQ-078)** — 表示契約を明記(ECO-003/004/GF-V4-04 教訓)。

- ビューア設定ドロワーに「タグ制御モード」トグル(`enableTagControl`)+「タグ制御マッピング」モーダルへの導線。
- モーダル(ビューア内中央オーバーレイ=ECO-019 同型)は 6 つの予約アクション各行に **[グリフ / アクション名+英名+説明 / 割り当てるタグ picker]** を提示(=提示フィールドの表示契約)。picker は割当タグを**色ドット+ラベル**で、未割り当ては「未割り当て」と表示。メニューは作成済みタグ一覧(色ドット)+「未割り当て(クリア)」。選択で当該アクションのマッピングを更新し即時保存(REQ-077)→ 次のプラン計算(REQ-076)へ反映。
- 作成済みタグ一覧・色は `ITagRepository`/`TagService` を WindowService 経由でビューアへ供給(ビューアは App 層 surface)。

**核/表面・受入観点**:
- 核=アクション解決(OC-23)・ページプラン構築(OC-24)・ナビゲーション(OC-25)=純粋計算(Core 非依存)。ViewModel 単体で観測可能にする。
- 表面=トグル/モーダル/picker 描画・プラン描画。
- 受入: OC-23/24/25 を unit exact(2.12.2 規則・2.12.3 例・competing-tag 解決・OFF-identity・canonical 現在画像・プラン送り)+ 固定オラクル S-32〜(全 cross-factory=純粋論理)。永続化は unit ラウンドトリップ+破損耐性(CP-SET-009 拡張)。モーダル表示パリティ・トグル動線は golden(G-11・承認者 maintainer)。

## 3. 不変条件(M-BOM へ前倒し)

| ID | 不変条件 | 関係する REQ |
|---|---|---|
| INV-001 | エンティティ ID は生成後不変。再リンクでも missing 側 image_id を保持する(関連の保全)。削除時の参照破損は FK(CASCADE/SET NULL)で管理し、SET NULL 化された参照は読み取り側がフォールバックする | REQ-001, REQ-017, REQ-028 |
| INV-002 | 日時は常に UTC・ISO 8601(ミリ秒 3 桁・literal Z)。文字列ソート=時系列ソートが成立する。同値時の順序は id 昇順で安定させる | REQ-002, REQ-032, REQ-033 |
| INV-003 | (image_id, tag_id) は image_tags 内で高々 1 行(UPSERT) | REQ-026 |
| INV-004 | タグ階層・ビュー階層に循環が存在しない(自己・子孫への親付け替えは常に拒否) | REQ-022, REQ-034 |
| INV-005 | relative_path は正規形(スラッシュ区切り・先頭末尾スラッシュなし)でのみ格納。比較は case-insensitive | REQ-014 |
| INV-006 | 一括操作(バッチタグ付け等)は全適用か無適用(原子性) | REQ-027 |
| INV-007 | numeric タグ値の格納は InvariantCulture の不変文字列、比較は常に数値比較(辞書順比較を使わない) | REQ-020, REQ-031 |
| INV-008 | 参照切れ(削除済みタグ・ノード・ビュー)に遭遇した読み取り処理は例外で停止せず、無視またはフォールバックする | REQ-028, REQ-037, REQ-042 |
| INV-009 | **(v2.0 woven 安全制約へ格上げ)** ユーザーの物理画像ファイルを移動・削除・結合・書き換えしない(読み取り専用)。本制約は特定 REQ に紐づく規則ではなく**全機能・全ループ横断の織り込み制約**であり、すべての製造単位が暗黙に従う。アプリ内の「削除」「除外」「マージ」等を将来導入する場合も DB 上の論理操作に限る(原典 porting-spec README 最重要原則)。物理ファイル操作を意図的に追加する場合は本制約の改訂(ECO)+確認ダイアログ+取り消し方針の定義を要する。merge/trash/permanent-delete 導入ループでは L3 物理差分 CP(操作前後のファイルシステム不変検査)を必須で追加する(00-charter-v2.md) | 全 REQ(横断) |
| INV-010 | 既定の画像一覧・ビュー評価・NodeGraph の値抽出は status=normal のみを対象とする。missing/deleted/pending のタグ付け状態は既定の表示系に反映されない(ただし v3.0 のトラッシュは deleted を意図的に一覧する専用閲覧であり、この「既定の表示系」には当たらない例外 — §2.10.5) | REQ-016, REQ-031, REQ-035 |
| INV-011 | **(v3.0)** マージはタグ集合の union(衝突時マージ先優先・マージ先が NULL/空ならマージ元値・simple は存在 union)+マージ元 status=deleted の論理操作のみで、マージ元の image_tags は削除しない。全工程は原子(失敗時全ロールバック)。物理ファイルには触れない(INV-009 に従属) | REQ-066 |
| INV-012 | **(v3.0)** pHash は画像ピクセル内容のみの決定的関数(同一内容→常に同一 pHash)。ハミング距離→類似度%の変換は単調非増加で spec 固定。pHash のビット表現が原典と一致する必要はない(CPOL-103 adapter)が、当方のレシピは固定し再現可能 | REQ-061, REQ-062 |
| INV-013 | **(v4.0)** トラッシュ復元は deleted→normal を**記録パスに物理ファイルが存在する場合のみ**許し、不在時は deleted→missing とする(存在しない画像を normal 一覧へ戻さない=幽霊 normal 防止)。物理ファイルは読み取りの存在確認のみ(INV-009 従属) | REQ-070 |
| INV-014 | **(v4.0)** 完全削除は images 行の削除のみ(image_tags/image_features/image_similarity は FK CASCADE)で、**物理画像ファイルには触れない**(INV-009 従属)。完全削除・復元はともに deleted 状態の画像にのみ許可する | REQ-070, REQ-071 |
| INV-015 | **(v4.0)** relink は missing 側 image_id を不変に保ち(INV-001)、置換に使う候補は **user タグを持たない(pending または untagged-normal)** ものに限る(タグ付き候補の消費=タグ損失を禁止し、該当時はマージへ案内)。確定で候補行は削除し missing 行が当該パスを所有する。candidate_link_id は候補ヒントであって確定の真実ではない | REQ-068, REQ-069 |

## 4. 沈黙次元の第1回掃討(silence-checklist)

| 次元 | 宣言 | 内容/参照 |
|---|---|---|
| 日時表現 | specified | ISO 8601 UTC、ミリ秒 3 桁、literal Z のみ (REQ-002) |
| ID 形式 | specified | UUIDv4 小文字 36 文字 (REQ-001) |
| 文字列照合(タグ名一意性) | specified | case-sensitive・序数 (REQ-021) |
| 文字列照合(パス) | specified | case-insensitive (REQ-014) |
| 文字列ソート(名前順) | specified | 大文字小文字を無視した序数比較(OrdinalIgnoreCase)、同値時 id 昇順 (REQ-029, REQ-038) |
| 正規表現の安全性 | specified | マッチタイムアウト 1 秒。不正・タイムアウトは条件不成立+警告 (REQ-031) |
| 長いパス(MAX_PATH 超) | specified | .NET の長パスサポートを有効化。それでも読めないファイルはスキップ+警告ログ(REQ-012 スキャンの実装規則 — 受入はスキャン治具に含む) |
| シンボリックリンク/ジャンクション | specified | ディレクトリのリパースポイントは辿らない(無限ループ防止)。ファイルのリンクは通常ファイルとして扱う(REQ-011/012 — 同上) |
| Unicode パス正規化 | specified | OS が返したパス文字列をそのまま使用(NFC/NFD の変換は行わない)(REQ-014 — 同上) |
| ファイルサイズ表示上限 | specified | 単位は GB まで。1024GB 以上も GB 表示 |
| 数値の文化圏依存書式 | specified | 格納・比較は InvariantCulture (INV-007)。表示のみロケール書式 |
| 正規表現方言 | specified | .NET Regex。不正パターンは条件不成立+通知 (REQ-031) |
| エラー語彙(エラーコード体系) | deferred-to-phase3 | M-BOM で全列挙(DuplicateTagName 等は REQ に既出) |
| 並行実行(スキャン中の UI 操作) | deferred-to-phase3 | M-BOM で排他方針を確定(最低限: スキャン二重起動の禁止) |
| ファイルサイズ表示丸め | specified | 1024 進・小数 1 桁。整形器=OC-7 共有・list/grid が担う(ECO-004。旧 REQ-043 は ECO-023 で撤回) |
| サムネイルのディスクキャッシュ上限 | specified | 上限なし・自動削除なし(原典準拠)。手動クリアは V1 外 |
| ファイル更新時のサムネイル陳腐化 | specified | 許容する(キーはパスのみ依存、原典準拠の既知制約) (REQ-040) |
| 画像の EXIF 回転(Orientation) | specified | **v4.1(ECO-049/REQ-085)で表示系に適用**(サムネ・ビューア・実効寸法 — §2.5)。pHash 入力・hash・スキャンは非適用のまま(REQ-084 変種が向き耐性を担う)。V1 裁定時の「原典も未適用」はコード上の意で、原典 Electron は Chromium 既定により表示は正立だった可能性が高い(ECO-049 §2) |
| テーマ(ダークモード) | out-of-scope | 原典でも実装未確認。V1 はライトのみ |
| アクセシビリティ | exploratory | 探索プローブで観測のみ(合否に入れない) |
| 性能(大規模ライブラリ) | exploratory | 1 万画像での操作可能性を探索プローブで観測。固定オラクルに入れない |
| ログ・診断 | deferred-to-phase3 | M-BOM でログ層を定義 |
| マルチウィンドウ/多重起動 | specified | 多重起動は禁止(2 つ目は既存をアクティブ化)。単一ウィンドウ(§2.6 シェルへ v2.0 で本文化 — 受入は L1 スモーク) |
| DB 同時アクセス(プロセス内) | deferred-to-phase3 | M-BOM で接続戦略(単一接続/プール)を確定 |
| インストール/配布形態 | out-of-scope | V1 はビルド成果物の直接実行。インストーラは後続 |

**第2回掃討(v2.0 — ビューア拡張の沈黙次元)**

| 次元 | 宣言 | 内容/参照 |
|---|---|---|
| Page キーの見開きでの意味論 | specified | PageDown/PageUp は論理キー(常に次へ/前へ)。矢印キーのみ空間キーとして右開きで反転 (§2.9) |
| スクロールの読み込みウィンドウ定数 | deferred-to-phase3 | E-BOM で実装定数を確定(契約は「全件一括でない+中央最近傍追跡」のみ — REQ-055) |
| 空白ページの視覚 | specified | 無地(背景と同系の空面)。エラー表示・プレースホルダ文言・枠線を出さない (§2.9) |
| モード切替 UI の形態 | specified | ビューア上部コントロールバーに集約。細部レイアウトは K-DESIGN 範囲で製造判断 → golden G-8 承認 (§2.9) |
| ビューア設定の保存タイミング | specified | 変更即時保存(明示の保存ボタンなし) (REQ-059) |
| スクロールのマウスホイール | specified | 標準スクロール(OS/コントロール既定)。ホイールでのページ送り変換はしない |
| 見開きのウィンドウリサイズ追従 | specified | リサイズ時は同一ペアを保ったまま再レイアウト(高さ統一・90% 上限を再計算)。アニメーションは要求しない |
| モード切替時のスクロール位置復元の粒度 | specified | 画像単位(記憶 index の画像の先頭が表示領域に入る)。ピクセル単位の復元は要求しない (REQ-054) |
| 見開きの先読み(プリロード) | deferred-to-phase3 | E-BOM で方針確定(契約外。表示正しさのみが合否) |
| ビューア内の回転・ズーム・フルスクリーン | out-of-scope | V1 から継続(原典に実装なし — §6) |
| タグ制御モード(タグでページ配置制御) | specified | §2.12(ECO-022)で実装=見開きフル+scroll/normal は skip のみ(§5.5)。enableTagControl 既定 OFF で §2.9 完全同一。配置=決定的ページプラン(OC-24) |
| タグ制御の competing-tag 解決順 | specified | 全順序 skip>spread>forceLeftPage>forceRightPage>leftPageEmpty>rightPageEmpty で支配 1 つ(§2.12.1・TC-3)。原典のリアクティブ補正からの意図的 divergence(ECO-022 §0.1) |
| タグ制御の総数/位置の数え方 | specified | 総数=非 skip 枚数・番号は非 skip 列の 1 起点・空白は番号なし・spread は 1 番号(§2.12.4) |
| タグ制御のページ送り単位/座標系 | specified | ON はプラン見開き単位で送る(`cur±1` クランプ・OC-25)。1 ページスライド/SHIFT/pageTurnMode は固定プランに概念上不適用。0→1 特殊送りはプラン構造に吸収。現在画像は canonical(先読み面優先)で導出しモード復元に用いる(§2.12.4・G2' M-1/M-2/M-3 補正) |
| タグ制御 空白開始 seed と先頭配置アクションの競合 | specified | 先頭の非 skip 画像が配置アクション(force*/empty/spread)を持つとき空白開始 seed を予約しない=両面空白の見開きを生まない(§2.12.2 不変条件・G2' 再監査 blocker 封じ込め)。canonical 現在画像が常に一意 |
| タグ制御マッピング先タグの削除 | specified | 永続値は保持してよいが解決時は未割り当て扱い(§2.12.5)。破損耐性は CP-SET-009 拡張 |
| スライドショー・フィルムストリップ | out-of-scope | 原典未実装の拡張候補(viewer-spec §6) |

**第3回掃討(v3.0 — 類似検索+マージの沈黙次元)**

| 次元 | 宣言 | 内容/参照 |
|---|---|---|
| pHash リサイズ補間方式 | specified | 双線形に固定(決定性のため)。SkiaSharp で明示指定 (§2.10.1/ADR) |
| pHash グレースケール係数 | specified | round(0.299R+0.587G+0.114B)・8bit 整数 (§2.10.1) |
| pHash の DC 成分の扱い | specified | 中央値計算から DC[0,0] を除外。ビットは DC 位置も含む 64bit・行優先 (§2.10.1) |
| pHash の原典ビット一致 | specified | 不要(CPOL-103 adapter)。当方の決定的レシピを固定オラクル(S-19 系)で凍結 (§2.10.1) |
| 類似度変換の丸め | specified | 区分線形・最近傍整数(0.5 切り上げ)・[0,100] クランプ・単調非増加 (§2.10.2) |
| 閾値の境界条件 | specified | 閾値以上(≧)を結果に含める。既定 70・範囲 50〜100 (§2.10.4 REQ-064) |
| 同類似度のソート安定性 | specified | id 昇順タイブレーク (§2.10.4) |
| 特徴量キャッシュの無効化方式 | specified | 内容ベース(file_size/modified_date/hash 変化)のみ。時間失効(原典 24h TTL)は不採用 (§2.10.3) |
| ペアキャッシュの正規化 | specified | id1<id2 に正規化し (A,B)=(B,A) を同一キャッシュ (§2.10.3) |
| マージ先=マージ元の自己マージ | specified | 同一指定は拒否。マージ先・マージ元とも非 deleted を前提 (§2.10.5) |
| マージの値タグ衝突 | specified | マージ先優先(マージ先 NULL/空ならマージ元値)・simple は存在 union (§2.10.5 / INV-011) |
| マージのトランザクション境界 | specified | 単一トランザクション・原子(失敗時全ロールバック) (§2.10.5 / INV-006) |
| 物理ファイル非破壊の実証 | specified | マージ前後でバイト・mtime 不変を L3 物理差分 CP-MERGE-001 で検査(INV-009 実アクション初適用) (§2.10.5) |
| criteria 検索の status 対象(v4.0) | specified | 用途別固定: 単体=normal(INV-010)/ relink 候補=pending∪normal (§2.11.1) |
| criteria 空条件の扱い(v4.0) | specified | 1 条件も未指定なら非実行・全件返さない(誤操作防止) (§2.11.1) |
| relink 置換候補に許す status(v4.0) | specified | pending ∪ untagged-normal。タグ付き候補は消費拒否→マージ案内(タグ損失防止) (§2.11.2 / INV-015 / ゲート記録 v4.0 B-1) |
| 復元時に物理ファイルが不在(v4.0) | specified | deleted→missing(normal へ戻さない=幽霊 normal 防止) (§2.11.3 / INV-013) |
| 完全削除の物理境界(v4.0) | specified | images 行削除+FK CASCADE のみ・物理ファイル非破壊。L3 物理差分 S-26 で実証 (§2.11.4 / INV-014) |
| 完全削除・復元の対象 status(v4.0) | specified | deleted のみ(normal/pending/missing は対象外) (§2.11.3-4 / INV-014) |
| 大規模類似検索の応答性 | exploratory | 1,000 枚規模を探索プローブ P-07 で観測(合否に入れない。原典に既知 UX 課題) |
| ORB / detailed モード | out-of-scope | pHash-only 確定(§裁定記録 2)。OpenCV ネイティブ依存を回避。後続ループ |
| criteria 条件検索(hash/名前/サイズ AND) | out-of-scope | 実質リンク修復用途 → 修復ループ(V4) |
| relink 修復(missing/pending 再リンク) | out-of-scope | 状態遷移拡張が大きく独立ループ(V4)。裁定は charter-v2 §裁定記録 2 |
| トラッシュの復元・完全削除 | out-of-scope | V3 は deleted 一覧の閲覧のみ。RVP-STATUS-003/004 は後続ループ |

## 5. トレース表

| REQ | 実現節 | 受入観点(深さ) | 原版証拠(設計者用・工場非開示) |
|---|---|---|---|
| REQ-001 | §2.0 | unit: ID pattern | image-repository.ts:742, tag-repository.ts:113 |
| REQ-002 | §2.0 | unit: 形式 exact | manager.ts:65,458 |
| REQ-003 | §2.0 | L2: PRAGMA 照会 | manager.ts:126-129 |
| REQ-004 | §2.0 | L2: スキーマ同値 | manager.ts:52-87,137-144 |
| REQ-005 | §2.0 | L1: ファイル存在 | main.ts:63-64 |
| REQ-010 | §2.1 | unit: CRUD+CASCADE | manager.ts:293-302,189 |
| REQ-011 | §2.1 | unit: 拡張子ベクタ | scanner.ts:10,522 |
| REQ-012 | §2.1 | unit〜L2: 遷移 4 ケース | scanner.ts:114-472, file-verifier.ts:201-217 |
| REQ-013 | §2.1 | unit: 既知ベクタ | file-verifier.ts:125-141 |
| REQ-014 | §2.1 | unit: 正規化ベクタ | file-verifier.ts:77-94 |
| REQ-015 | §2.1 | unit: last_scan+サマリ | scanner.ts:430-436,464-471 |
| REQ-016 | §2.1 | unit: 既定表示の絞り | shared/types/images.ts:7, image-repository.ts:50,107,162 |
| REQ-017 | §2.1 | unit: id 保持・関連保全 | file-verifier.ts:387-439, image-repository.ts:581-736 |
| REQ-020 | §2.2 | unit: 3 種ラウンドトリップ | shared/types/tags.ts:20-38 |
| REQ-021 | §2.2 | unit: 重複拒否 | migration009, tag-repository.ts:188-192 |
| REQ-022 | §2.2 | unit: SET NULL+循環拒否 | manager.ts:204(循環拒否は意図的差分) |
| REQ-023 | §2.2 | unit: 形式ベクタ | manager.ts:199-201, ColorPicker.tsx:17-71 |
| REQ-024 | §2.2 | unit: 順序保持 | manager.ts:207-212 |
| REQ-025 | §2.2 | unit: 境界ベクタ | manager.ts:214-222(範囲拒否は意図的差分) |
| REQ-026 | §2.2 | unit: UPSERT | tag-repository.ts:477-485 |
| REQ-027 | §2.2 | unit: 原子性 | tag-management.service.ts:160-200 |
| REQ-028 | §2.2 | unit: 4 テーブル状態 | manager.ts:204,231-232,261,277 |
| REQ-029 | §2.2 | unit: 並び+件数 | tag-repository.ts:252 |
| REQ-030 | §2.3 | unit: CRUD+CASCADE | manager.ts:236-262 |
| REQ-031 | §2.3 | unit: 演算子×境界ベクタ | view-search.service.ts:104-201 |
| REQ-032 | §2.3 | unit: 更新トリガ | view-repository.ts:307 |
| REQ-033 | §2.3 | unit: 並び+limit | view-repository.ts:40,492, ipc/views/index.ts:61 |
| REQ-034 | §2.4 | unit: ノード CRUD+循環拒否 | manager.ts:265-279, view-tag-hierarchy-repository.ts:526-563 |
| REQ-035 | §2.4 | unit: 期待ノード木(0/1/2 件境界) | nodeGraph/types.ts:8-15, utils.ts:20-219 |
| REQ-036 | §2.4 | unit〜L2: パス→条件→画像集合 | utils.ts:228-308 |
| REQ-037 | §2.4 | unit: 3 ケース(あり/なし/参照切れ) | useNodeGraph.ts:86-110, utils.ts:316-354 |
| REQ-038 | §2.3 | unit: フィールド×方向ベクタ | (候補値確定は意図的差分) |
| REQ-040 | §2.5 | L2: 寸法・形式・キャッシュ | ipc/images/thumbnails/index.ts:11-45 |
| REQ-041 | §2.6 | unit+golden+探索プローブ | TagBasedImageGallery.tsx(NewUI)+useImageSelection.ts:24-34(SHIFT union)。v1.3 改訂: 旧引用 ImageGrid.tsx:23,43-50 は旧 UI 系 — ECO-002 出所分析 |
| REQ-042 | §2.6 | unit+golden | ImageList.tsx:33-35,102-119 |
| ~~REQ-043~~ (ECO-023 撤回) | §2.6 | ― | ImageDetailModal.tsx(原典はモーダル。新設計は詳細廃止。OC-7 整形器のみ list/grid で継続) |
| REQ-044 | §2.6 | unit: ナビゲーション+golden | useViewerNavigation.ts:163-170(Esc・フィットは意図的差分) |
| REQ-046 | §2.6 | unit: 適用/連番ロジック+golden G-7 | TaggingPanel.tsx, NumericTagDialog.tsx:64-128(v1.2 追加・ずる裁定) |
| REQ-045 | §2.5 | unit: LRU 動作 | ProxyImage.tsx:8-34 |
| REQ-050 | §2.7 | unit: フォールバック+golden | i18n.config.ts:2-3, i18n/request.ts:39-45 |
| REQ-051 | §2.7 | unit+golden | language.ts:36-48(即時反映は意図的差分) |
| REQ-052 | §2.7 | unit: ラウンドトリップ+破損耐性 | language.ts:7, main.ts:109-110(拡張は意図的差分。v1.3: 表示モード・選択コレクション追加/列数廃止) |
| REQ-053 | §2.6 | unit: コレクションフィルタ+golden G-1 | ImageGallery.tsx(selectedCollection), CollectionSidebar.tsx, porting-spec RVP-INV-003(v1.3 追加・ECO-002 CR-2) |
| REQ-054 | §2.9 | unit: OC-13 操作列ベクタ+golden G-8 | ImageViewer.tsx:27-84(modeIndexMapRef), types.ts:16(v2.0 追加) |
| REQ-055 | §2.9 | unit: OC-11 追跡ベクタ+golden G-8+探索プローブ | ScrollMode.tsx:33-59,283-329,414-441(v2.0 追加) |
| REQ-056 | §2.9 | unit: OC-9 全組合せベクタ+golden G-8 | SpreadMode.tsx:86-134(v2.0 追加) |
| REQ-057 | §2.9 | unit: OC-10 境界含む全組合せ+golden G-8 | ImageViewer.tsx:86-207, SpreadMode.tsx:595-698(v2.0 追加) |
| REQ-058 | §2.9 | unit: OC-12 高さ統一ベクタ+golden G-8 | viewerUtils.ts:9-31,67-84, SpreadMode.tsx:547-569,768-822(alignMode のスクロール不採用は実装準拠の確定。v2.0 追加) |
| REQ-059 | §2.9 | unit: ラウンドトリップ+範囲外既定化 | useViewerSettings.ts(原典は未永続化 — 永続化は意図的差分・移植版設計。v2.0 追加) |
| REQ-060 | §2.6 | unit: 条件サマリ整形ベクタ+golden G-1/G-6 | 50-as-built.yaml golden_findings GF-01〜05(v2.0 追加・承認者裁定) |
| REQ-061 | §2.10.1 | unit: OC-14 決定性+合成/性質ベクタ | phash-utils.ts(DCT 32×32→8×8 64bit。ビット一致不要=CPOL-103。v3.0 追加) |
| REQ-062 | §2.10.2 | unit: OC-15 段階式境界ベクタ | similarity-evaluator.ts(popcount・段階式変換。v3.0 追加) |
| REQ-063 | §2.10.3 | unit/L2: OC-18 再計算/正規化/CASCADE+スキーマ | migration 007/008, similarity-cache.service.ts(時間失効は不採用=意図的差分。v3.0 追加) |
| REQ-064 | §2.10.4 | unit: OC-16 候補/閾値/安定ソート+探索プローブ | similar-image-service.ts(simple/pHash), find-similar-with-scores.ts(v3.0 追加) |
| REQ-065 | §2.10.4 | unit: 結果整列/空状態+golden G-9 | SimilarImageSearchModal.tsx, RepairModal.tsx(detailed 非表示は §裁定記録 2/4。v3.0 追加) |
| REQ-066 | §2.10.5 | unit: OC-17 union/衝突/原子+L3 物理差分+golden G-9 | image-merge.service.ts, ImageMergeModal.tsx, CPOL-009, RVP-MERGE-001/002(v3.0 追加) |
| REQ-067 | §2.10.5 | unit: 対象選択/プレビュー+golden G-9 | ImageMergeModal.tsx, TrashModal.tsx(復元/完全削除は後続。v3.0 追加) |
| REQ-068 | §2.11.1 | unit: OC-19 AND 絞り込み/status 対象/安定順/空条件非実行 | RepairModal.tsx(criteria 検索), CPOL-008(v4.0 追加) |
| REQ-069 | §2.11.2 | unit: OC-20 候補プール/タグ安全/確定/ID 不変/原子性 | RelinkService(V1 既存), RepairModal.tsx, RVP-REPAIR-004(v4.0 拡張) |
| REQ-070 | §2.11.3 | unit: OC-21 存在分岐(T6/T7)+L3 物理差分(S-26) | TrashModal.tsx(復元), RVP-STATUS-003(v4.0 追加) |
| REQ-071 | §2.11.4 | unit: OC-22 行削除/CASCADE/deleted 限定+L3 物理差分(S-26) | TrashModal.tsx(完全削除), RVP-STATUS-004(v4.0 追加) |
| REQ-072 | §2.11.5 | unit: 候補選択/確認/結果表示+golden G-10 | RepairModal.tsx, TrashModal.tsx, K-DESIGN(v4.0 追加) |
| REQ-076 | §2.12.1〜2.12.4 | unit: OC-23 解決順+OC-24 配置/OFF-identity/画像対応+OC-25 ナビ+固定オラクル S-32〜 | tagControlUtils.ts(applyTagActions・決定的化は意図的 divergence ECO-022 §0.1) |
| REQ-077 | §2.12.5 | unit: ラウンドトリップ+破損耐性(CP-SET-009 拡張) | useViewerSettings.ts(enableTagControl/tagActionMapping。永続化は移植版設計=REQ-059 同方針) |
| REQ-078 | §2.12.6 | unit: マッピング更新+golden G-11(表示パリティ) | ViewerControls/タグ制御マッピングモーダル(Downloads モック・ECO-019 in-tab popup 同型) |

## 6. カバレッジ監査(リバース工程 — 未トレース要素の分類)

原典に存在するが本ループの REQ にトレースされない要素の全数分類。

**out-of-scope(後続ループ/ECO で対応、チャーター準拠)**
| 原典要素 | 行き先 |
|---|---|
| ~~類似画像検索・重複検出(image_features/image_similarity、pHash)~~ | **v3.0 で実装(§2.10、REQ-061〜067。pHash・simple のみ)** |
| 類似検索の ORB / detailed モード(orb_descriptors/orb_keypoints) | 後続ループ(pHash-only 確定=§裁定記録 2。OpenCV ネイティブ依存を回避) |
| criteria 条件検索(hash/名前/拡張子/mtime/サイズ AND) | 後続ループ(修復ループ V4 — 実質 relink 用途) |
| relink 修復(missing/pending 再リンク・candidate_link_id) | 後続ループ(修復ループ V4。裁定は charter-v2 §裁定記録 2) |
| トラッシュの復元・完全削除(RVP-STATUS-003/004) | 後続ループ(V3 は deleted 一覧の閲覧のみ実装) |
| ワークスペース(workspaces/workspace_images) | 後続ループ |
| バックアップ/復元(16 IPC)・DB 修復・view_revisions | 後続ループ |
| ~~見開き・スクロール・ページ送りモード~~ | **v2.0 で実装(§2.9、REQ-054〜059)** |
| ~~タグ制御モード(タグでページ配置を自動制御)~~ | **ECO-022 で実装(§2.12、REQ-076〜078)**。原典で未実装だったマッピング設定 UI・永続化を移植版設計で新規実装 |
| ノーマル/スクロールでの漫画見開き 2 枚合成(2 枚=1 見開き絵を 1 枚として並べる) | 別 ECO(必要時)。TC-5=見開きモード限定(空白開始/force で揃う・ワイド 1 枚は全モード表示可)。cross-mode 合成は新データモデル+2 モード見開き描画を要し実需要なし(maintainer 2026-06-30) |
| Undo/Redo | 後続ループ |
| 既存 viewprism.db からのデータ移行 | ECO(必要時) |

**ignored-with-reason(原典スキーマ・文書に存在するが実装未到達 — 本実装でも採用しない)**
| 原典要素 | 理由 |
|---|---|
| view_conditions.logic_operator(OR)・group_id・field 条件 | 原典で評価ロジック未実装。V1 は AND のみ(REQ-031)。スキーマ列も設けない(必要時に ECO) |
| ビューの export/import | 原典で IPC 未実装(仕様書記載のみ) |
| 組み込みビュー(「全画像」等) | 原典に実装なし。V1 は「全画像」相当を UI 上の固定入口として提供するか E-BOM で判断 |
| USN Journal / リアルタイムファイル監視 | 原典に実装なし(文書記載のみ)。手動スキャンのみ |
| EXIF 抽出(exifreader) | 原典で利用コードなし。詳細パネルの解像度はデコード時取得で代替 |
| ビューアのズーム・パン・回転・スライドショー | 原典に実装なし |
| ストーリービュー | 原典に実装なし(V1 チャーターの語のみ。原典 modes/ は normal/scroll/spread の 3 系 4 種で全部 — v2.0 リバースで確認) |
| ビューアの CustomNormalMode | 原典スタブ(NormalMode と同一実装・呼び出し経路なし — v2.0 リバースで確認)。移植しない |
| ビューア設定のセッション限り破棄(原典挙動) | 原典は設定未永続化だが、利便性のため永続化を採用(REQ-059 — 意図的差分。原典挙動の再現はしない) |
| テーマ切替(ダークモード) | 原典で実装未確認 |
| app_version テーブル | マイグレーション基盤(REQ-004)に統合し、独立要求にしない |

**unknown(原典の格納先・挙動が特定できず — S-BOM 登録し版更新時に再調査)**
| 原典要素 | 内容 | V1 での確定 |
|---|---|---|
| 画像詳細のレーティング(5 つ星) | UI に存在するが格納先カラム不明 | V1 除外。再調査後に ECO 判断 |
| images.status='deleted' への遷移操作 | 論理削除を起こす UI/IPC が特定できず | deleted は予約値とし遷移を実装しない(§2.1 遷移表で確定済み) |
| exclude_patterns のパターン形式(glob/regex) | 原典で解釈コード未確認 | ファイル名完全一致として確定済み(REQ-010)。原典挙動の再調査は不要 |

---
## ゲート記録(G2/G2')

### v1.1(loop-v1-core)
- 実施日: 2026-06-11
- マルチリーダー監査: リーダー数 N=1(チャーター: 実用適用)
  - G2(第 1 回): 所見 22 件(blocker 5 / major 16 / minor 1)→ **通過不可**。
    主要欠陥: NodeGraph 値抽出の status 未規定、条件評価のエッジケース未規定、ステータス遷移の列挙漏れ、
    観測契約未定義、golden 判定基準なし
  - 補正: blocker 5 件・major 全件を本書 v1.1 に反映(§2.1 遷移表・判定優先順、§2.3 エッジケース規則、
    §2.4 status=normal 限定、§2.6 空状態+golden 基準 G-1〜G-5、§2.8 観測契約 OC-1〜OC-8、§4 追加 6 次元)
  - G2'(再監査): blocker 0 / major 0 / minor 3(AUDIT-101: NodeGraph 構造変化時の選択リセット詳細、
    AUDIT-102: 削除済みタグ列の幅配分、AUDIT-103: DB 接続戦略)→ **通過**。minor 3 件は E-BOM/M-BOM へ申し送り
- MeasurementCapability: 全 REQ が adequate へ到達可能(unmeasurable ゼロ)= **yes**
  - human-approval-required の REQ と承認者: REQ-041〜044, REQ-050〜051(golden 基準 G-1〜G-5)— 承認者 maintainer
  - 観測契約が必要な REQ(in-process): §2.8 OC-1〜OC-8 として定義済み

### v2.0(loop-v2-viewer)
- 実施日: 2026-06-13
- マルチリーダー監査: リーダー数 N=1(チャーター v2: 実用適用)。リーダーには本書のみ供与(独立性維持)
  - G2(第 1 回): blocker 3 / major 6 / minor 8 / トレース破れ 6 系統 → **通過不可**。
    blocker(いずれも §2.9): スクロールの画像サイズ未規定、余白クリックの二義性(クリック領域の範囲)、
    scroll のキー意味論未規定。major: 位置記憶の初期値・追跡タイブレーク・片側空白の統一高さ・
    条件サマリ書式精度(以上 v2.0)/タグ一覧比較器の本文内矛盾・キャッシュ TTL 意味論(以上 v1 域の
    文書是正 — 実装を確認し実装側に文面を一致、意味論変更なし)。トレース: REQ-003/004/005 本文不在、
    CP/NFR/オラクル ID の参照先不在、§4 specified 行の REQ 参照欠落、見出し不整合
  - 補正: 全件を本書 v2.0 に反映(§2.9 画像サイズ・クリック半面規則・キーボード共通基盤、REQ-054 初期値、
    OC-11 タイブレーク、OC-12 片側空白規則、REQ-060(e) i18n テンプレート書式、§2.0 DB 基盤本文化、
    参照注記、G-4 前提・G-8 0 起点 ほか)
  - G2'(再監査・同一リーダー継続): 第 1 回指摘 **17/17 解消**。新規は軽微 3(エスケープ例示表記・
    片側空白規則のスコープ明示・REQ-056 番号基準)→ 即時是正済み → **通過**(blocker 0 / major 0 / 残差 0)
- MeasurementCapability(v2.0 追加分): 全 REQ adequate = **yes**
  - REQ-054〜058: 計算ロジック=OC-9〜13(unit exact)+golden G-8(human-approval-required、承認者 maintainer)
  - REQ-059: unit ラウンドトリップ(REQ-052 既存治具へ追加)
  - REQ-060: (e)=unit 整形ベクタ、(a)〜(d)=golden G-1/G-6 v2.0 追加項(human-approval-required、承認者 maintainer)
  - スクロール読み込みウィンドウ定数・先読み方針は契約外(E-BOM 確定、合否対象外)— 測定対象から明示除外
  - human-approval-required の総括(v2.0 時点): G-1〜G-8 — 承認者 maintainer。観測契約: OC-1〜OC-13

### v3.0(loop-v3-similarity)
- 実施日: 2026-06-13
- マルチリーダー監査: リーダー数 N=1(チャーター v3: 実用適用)。リーダーには**本書のみ供与**(独立性維持)。
  補助として Codex(別系統モデル)に pHash レシピの知識検証を依頼(DCT-II 正規化・SkiaSharp 版差の決定性・
  丸め規約)— M-3 と丸め固定の補強に反映(設計者側補助・成果物で評価)
- G2(第 1 回): **blocker 3 / major 10 / minor 4 + トレース破れ** → **通過不可**。
  - blocker: B-1=類似検索の候補 status(normal/pending/missing)と INV-010(既定の表示・抽出は normal のみ)の
    正面衝突で製造装置が一意に作れない / B-2=距離→類似度%変換の中間値・丸めが本書から導けず固定オラクル(OC-15)
    が作れない=合否不能 / B-3=CP-MERGE-001・S-19・P-07 等の本書外 ID が「本書外で閉じる」明示なく宙吊り
  - major(抜粋): image_features/similarity スキーマの §2.0↔§2.10.3 不一致・cache_key 生成規則未確定・
    DCT-II 正規化/符号規約未規定・多元マージの値決着順・特徴量再計算時のペアキャッシュ連鎖無効化・
    status フィルタとキャッシュ参照順・閾値刻み・simple/detailed 用語未定義・トラッシュ母集合スコープ
- 補正: 全件を本書 v3.0 に反映 —
  - B-1: **候補を status=normal 限定**に確定(§2.10.4・OC-16・REQ-064/066 へ波及。missing は pHash 計算不能・
    pending/missing 候補は relink 修復 V4。INV-010 と整合)
  - B-2: §2.10.2 に**変換式(区分線形+Floor(raw+0.5)+クランプ)と期待値ベクタ**(d=0→100…64→0)を明示
  - B-3: CP-MERGE-001(33-control-plan)・S-19 系(41)・P-07(42)に「本書外で閉じる」を明記
  - major: §2.0↔§2.10.3 のスキーマ参照統一・cache_key=正規化ペアに確定・DCT orthonormal/符号/cos 固定表/
    (int)(y+0.5) 丸め・多元 id 昇順先勝ち・連鎖無効化・フィルタ先行・整数閾値・「精度モード」別語化・
    トラッシュ=選択コレクション scope
- G2'(再監査・独立リーダー): 第 1 回 **blocker 3/major 主要を解消確認**(OC-15 距離変換は本書だけで検算可能と
  実証)。残は major 1(G-9 の "simple" がタグ種別 simple と二義)+minor 2(INV-010 のトラッシュ例外注記・
  pHash の cos/符号が ADR 依存)→ **即時是正**(精度モードを「pHash のみ/pHash+ORB」へ別語化・OC-17 を
  「simple タグ union」へ・INV-010 に例外注記)→ **通過**(blocker 0 / 残 major 0 / minor 1=pHash の hex が
  ADR pin で本書外に閉じる=意図された設計境界。性質ベースオラクル OC-14/S-19 系で合否は本書で閉じる)
- MeasurementCapability(v3.0 追加分): 全 REQ adequate = **yes**
  - REQ-061: OC-14(決定性・縮退・距離 0・近傍<閾値・非近傍>閾値の性質ベース)+固定オラクル S-19 系。
    16hex の exact は ADR pin(本書外で閉じる — CPOL-103 adapter)
  - REQ-062: OC-15(§2.10.2 の式+期待値ベクタで unit exact)
  - REQ-063: OC-18(再計算・連鎖無効化・正規化・CASCADE+L2 スキーマ)。REQ-064: OC-16(normal 限定・閾値・安定ソート)
  - REQ-066: OC-17(union/衝突/多元/原子)+ L3 物理差分 CP-MERGE-001(物理非破壊・本書外で閉じる)
  - REQ-065/067: golden G-9(human-approval-required、承認者 maintainer)
  - human-approval-required の総括(v3.0 時点): **G-1〜G-9 — 承認者 maintainer**。観測契約: **OC-1〜OC-18**

### v4.0(loop-v4-repair-lifecycle)
- 実施日: 2026-06-14
- マルチリーダー監査: リーダー数 N=1(チャーター v4: 実用適用)。リーダーには**本書のみ供与**(独立性維持)
- G2(第 1 回): **blocker 2 / major 3 / minor 2** → **通過不可**(是正前)。
  - **B-1(中核)**: criteria-relink の置換候補に許す status が未確定 — pending は ScanJudge 3a(exact-hash)でしか
    生じず、再エンコード/改名(hash 変化)で生じる候補は normal。relink が normal を消費できないと criteria 検索の
    修復価値が出ず、消費できると**タグ付き normal のタグ損失**リスクが出る=製造装置が一意に作れない
  - **B-2**: 復元 deleted→normal を物理不在でも許すか未確定(幽霊 normal 行の事故)
  - major: criteria の status 対象集合(用途別)未明示・空条件の扱い未定義・完全削除/復元の対象 status(deleted 限定)未明示
  - minor: candidate_link_id の「ヒント/真実」の位置づけ・完全削除 UI の非破壊文言
- 補正(本書 v4.0 §2.11 に反映 — 起案時に maintainer が裁定):
  - **B-1**: 置換候補 = **pending ∪ untagged-normal**。**タグ付き候補は relink 拒否→マージ案内**(タグ損失防止 — INV-015)。
    criteria の再エンコード修復ケースを満たしつつデータ安全を保つ(§2.11.2 / OC-20 / 裁定 5)
  - **B-2**: 復元は**物理存在時のみ normal・不在は missing**(幽霊 normal 防止 — INV-013 / 裁定 2)
  - major: criteria status 対象=単体 normal / relink 候補 pending∪normal(§2.11.1)・空条件は非実行・完全削除/復元は deleted 限定(INV-014)
  - minor: candidate_link_id=候補ヒント(非確定)を明記(§2.11.2 / INV-015)・完全削除は非破壊明示文言必須(§2.11.5 / 裁定 6)
- G2'(再監査): B-1/B-2 解消確認(状態機械 T1〜T8 が本書だけで一意・タグ安全ガードで relink のデータ損失が閉じる)。
  残差 0 → **通過**(blocker 0 / major 0 / minor 0)
- MeasurementCapability(v4.0 追加分): 全 REQ adequate = **yes**
  - REQ-068: OC-19(AND 絞り込み・status 対象・安定順・空条件非実行の unit exact)
  - REQ-069: OC-20(候補プール・タグ安全・確定・ID 不変・原子性の unit exact)
  - REQ-070: OC-21(T6/T7 分岐・不変)+ L3 物理差分 S-26(物理非破壊・本書外で閉じる)
  - REQ-071: OC-22(行削除・CASCADE・deleted 限定)+ L3 物理差分 S-26
  - REQ-072: golden G-10(human-approval-required、承認者 maintainer)
  - human-approval-required の総括(v4.0 時点): **G-1〜G-10 — 承認者 maintainer**。観測契約: **OC-1〜OC-22**

### ECO-022(タグ制御モード・フェーズ2・§2.12)
- 実施日: 2026-06-30
- 独立リーダー監査: N=1(本書のみ供与・原典/オラクル/BOM/コード非開示で独立性維持)
- G2(第 1 回): **blocker 0 / major 3 / minor 4** → **要補正**。中核(§2.12.1 解決・§2.12.2 ページプラン構築)は一意実装可能・worked example/OFF-identity 検算一致。指摘は §2.12.4 ナビゲーションに集中:
  - **M-1**: 2 ページ送り着地後の「現在画像」未定義 / **M-2**: 既定送り(プラン見開き座標系)と SHIFT/1ページ(画像座標系)の混在+「§2.9 同規則」の適用座標不定+タグ制御版ページ送りの観測契約欠落 / **M-3**: spread 跨ぎ送り未規定
- 補正(第 1 回): ナビを**プラン見開き単位へ一本化**(SHIFT/pageTurnMode 非適用・0→1 はプラン構造に吸収)・現在画像 canonical 導出(先読み面優先)・画像↔見開き対応・**OC-25(タグ制御ナビゲーション計算器)新設**・minor 4 件(padToParity 基点・force/empty 差・spread worked example・spread 番号)。
- G2'(再監査): M-3 閉・M-1/M-2 部分閉・minor 全閉。ただし補正が依存した「両面空白の見開きは生じない」が**反例で破綻**(空白開始 ON × 先頭配置アクションで `(S[0],S[1])=(空,空)`)= **新規 blocker 1**。
- 補正(第 2 回): **空白開始 seed 抑止規則**(先頭の非 skip 画像が配置アクションを持つとき seed を予約しない=force アンカー意図 TC-5 とも合致)+ **不変条件「両面空白の見開きは生じない」を証明付きで明記**(空白源 seed/pad/empty の網羅・pad/empty は相方 emit・seed は抑止で孤立しない)+ worked example 6 行目 + OC-24 ベクタ + §4 競合行。
- G2'(再々監査): blocker **完全に閉じた**(両面空白チャンクは到達不能を網羅的パリティ論証で確認)・OFF-identity 不破壊・新規所見ゼロ → **通過**(blocker 0 / major 0 / minor 0)。
- MeasurementCapability(ECO-022 追加分): 全 REQ adequate = **yes**
  - REQ-076: OC-23(解決順 exact)+ OC-24(ページプラン・OFF-identity・canonical・seed 抑止)+ OC-25(プラン送り・モード復元)= unit exact + 固定オラクル S-32〜(全 cross-factory)
  - REQ-077: unit ラウンドトリップ + 破損耐性(CP-SET-009 拡張)
  - REQ-078: golden G-11(human-approval-required、承認者 maintainer)= マッピングモーダル表示パリティ
  - human-approval-required の総括(ECO-022 時点): **G-1〜G-11 — 承認者 maintainer**。観測契約: **OC-1〜OC-25**
