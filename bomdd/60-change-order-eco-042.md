# Change Order — ECO-042(applied): FL-001 裁定の取り込み — グリッドに列は持ち込まない(現状追認・doc-only)

> ECO-025 の残未確定 FL-001 の maintainer 裁定(2026-07-05)の取り込み。FL-002/FL-004 → ECO-039 と同型の
> 「設計確定の取り込み」だが、本件は**現状追認で実装変更ゼロ= doc-only 同期のみ**。

## 1. 裁定(maintainer 2026-07-05・CAD `5e1d32e`)

- **グリッド(アイコン表示)に列は持ち込まない。**
- 表示列(ヘッダー+セル)は**リスト専用の描画面**。アイコンへの表示列の露出は
  **FL-003 の範囲(並び替え候補=表示列+ソート中タイルのソート項目表示)まで**とし、
  これ以上列情報をタイルへ足さない。タイルの基本描画= サムネイル+名前(+閲覧時タグドット)を維持。
- ソートの共有は FL-003(決定済み・実装済み)の範囲で完結 — FL-001 の残論点は「列」のみだった。
- CAD 記録: review_points.md FL-001 → 決定・docs/screens/file_list.md「主張(v2)」へ明文
  (ViewPrismUI `5e1d32e`)。

## 2. 適合判定 — 現状追認(実装変更ゼロ)

- 現行実装(ECO-025 β・golden approved)のグリッドタイル= サムネイル+名前+
  ソート中のみソート項目(+閲覧時タグドット)。列ヘッダー/セルはリストのみ = **裁定に既に適合**。
- よって src/tests 変更なし・オラクル/golden 影響なし(視覚不変・変更ゼロ)。
- 62(migration)不要(DB 不変)・35-dsbom 不要(surface 新設なし)。

## 3. 同期内容(doc-only)

- 30-ebom E-UI-BROWSE-022 invariants へ since ECO-042(FL-001 裁定の明文)を追記。
- 20-spec ソート/一覧節(S-a 明文化箇所の近傍)へ 1 文追記。
- register: ECO-042 = applied(golden: n/a)。ECO-025 の「残未確定」から FL-001 を解消。

## 4. 残ゲート

なし(裁定=gate① は受領済み・現状追認につき golden 不要)。ECO-025 系の残未確定は
**VE-004(空のビュー名保存)のみ**となった。
