# ECO-123 — CMP-006 インスタンス契約値への実装追随 — 作業タブ ⋯メニュー幅 200→208(裁定済み)+画像タブ 表示軸メニュー幅 260→240(悉皆で発見)

- 起票日: 2026-07-20
- 報告者: AI(CAD 側 REG-C3 裁定「As-Built 乖離= VP2 側 ECO で追随」の実行+起票時悉皆の追加所見)
- 種別: 実装是正候補(視覚寸法の転写ドリフト・挙動不変)
- baseline: ViewPrism2 main `0d3e0a5` / ViewPrismUI 部品表改版後(REG-C1〜C4 裁定済み)

---

## 1. 症状/要求

CMP-006 PopoverMenu が Standard へ昇格し(REG-C3 裁定 2026-07-20)、幅がインスタンス契約値として
固定された: **表示軸= 240 / ⋯= 208 / 並び替え= 252 / 移動先= 240**。「同型インスタンスの面間複製は
同値必須」。実装を全インスタンス悉皆で突合した結果、乖離 2 件:

1. **作業タブ ⋯メニュー= 200**(契約 208)— CAD 側 As-Built 乖離リスト①に記帳済みの**裁定済み追随対象**。
   51-cheat-log 2026-07-19 の R3 予告(ECO-112 の面間非対称)の実行。
2. **画像タブ 表示軸メニュー= 260**(契約 240)— **本起票の悉皆で発見**(CAD 側リスト外の 3 件目)。

## 2. 工程診断

| 工程 | 判定 | 根拠 |
| --- | --- | --- |
| CAD(部品表 CMP-006) | 健全 | Standard 昇格済み・インスタンス契約値は mock 実測に基づく(⋯=208/軸=240 は dc.html 実測と一致= 起票時に再実測済み) |
| 実装 | **逸脱 2 件** | ①WorkTabView.axaml:765 `Width="200"`(契約 208)②ImageTabView.axaml:767 `Width="260"`(契約 240) |

**結論: 実装層の転写ドリフト 2 件。**

## 3. 切り分け済みの事実

### 確定

1. **悉皆の母集合**: `Classes="popupMenu"` 全 7 サイト(grep 実測)。
   - 一致 4: ⋯ImageTab=208 ✓ / 並び替え両タブ=252 ✓✓ / 移動先WorkTab=240 ✓
   - 乖離 2: ⋯WorkTab=200 ✗ / 軸ImageTab=260 ✗
   - 契約対象外 1: 表示列ポップオーバー=304(部品表のインスタンス列挙に幅契約なし=
     「部品表に無い要素は mock が正」で現状維持)
2. **mock 裏取り**: 画像タブ dc.html の実測= ⋯メニュー `width:208px`・軸メニュー `width:240px`。
   **`width:260px` は mock に存在しない**= 260 は製造時の転写ドリフト。
3. **混入コミット**: 260= `45a6c77`(2026-06-18・M3b タグビュー軸の実消費=製造時から・潜伏約 1 ヶ月)。
   200= `f211fa9`(2026-06-29・作業タブ複製実装= ECO-112 R3 で既知)。
4. **マスキング**: 歴代 golden はメニュー幅を実測せず(GfSortMenuVisualParityTests は並び替えメニュー
   252 のみ pin・⋯/軸メニューの幅 probe は不在)。ECO-109/112 の許容差分リストにも 260/200 は無い=
   裁定で許容された差分ではなく未検出のドリフト。
5. **行高は対象外(裁定済み)**: ⋯メニュー既存行≒37(契約 42)は「ECO-112 golden 許容済みのため
   急がない・収束は次回当該面 fix か SRC-011 と同便」(CAD 側 As-Built 乖離②の裁定)= 本 ECO で触れない。

### 未検証

- 260 で製造された理由(意図的な拡幅か単純ドリフトか)は git メッセージからは不明。ただし mock 不在+
  許容差分記録なしから**ドリフト推定**。長いビュー名の表示は MaxHeight=360+内部スクロールが担っており
  幅 240 で機能上の問題はない見込み(golden で確認)。

## 4. 是正方針(案・着手時確定)

**案A(推奨)**: 2 サイトの Width を契約値へ是正(200→208・260→240)。
プローブ= 両メニューの幅 pin(是正前赤)を新設。**ECO-122 の写像(RegistryContract)が先に無い場合は
テスト内定数で pin し、ECO-122 fix 時に写像参照へ置換**(推奨順序= 本 ECO → ECO-122 なら
写像の契約値と実装が最初から一致し、置換は機械的)。

diff 規模: src 2 行+probe。挙動不変(幅のみ)。

## 5. 影響 BOM

- **src**: WorkTabView.axaml:765(200→208)・ImageTabView.axaml:767(260→240)
- **tests**: メニュー幅 probe 新設(⋯WorkTab/軸ImageTab・是正前赤→緑転。既存固定 Oracle 行は
  変更しない= R6)
- **CP**: CP-UI-G1 刻印(accept 時・転写ドリフトの潜伏実績つき)
- **CAD**: 無変更(契約は確定済み)。ただし **As-Built 乖離リストへの 3 件目(軸 260→240)追記を
  CAD 側へ申し送り**(部品表は VP2 側で編集しない= 恒久運用柱4)

## 6. 残ゲート

- **gate①(裁定)**: 不要(①は REG-C3 で裁定済み。②は Standard 契約+mock 実測一致+ドリフト証拠=
  実装逸脱の明白事例。ただし②の 260→240 は視覚変化を伴うため gate② で実機確認)
- **gate②(golden)**: **必要(軽量)— §8 の基準で維持者確認待ち。**

## 7. 実施記録(2026-07-20 fix)

- **R5(プローブ先行)**: GfPopoverMenuInstanceWidthTests 新設。是正前赤 2 本を実測=
  「表示軸メニュー幅が 260(契約 240)」「作業タブ ⋯メニュー幅が 200(契約 208)」= 診断どおり。
- **是正= 案A**: ImageTabView.axaml 軸メニュー Width 260→240・WorkTabView.axaml ⋯メニュー
  Width 200→208(各 1 行+ECO コメント= 契約値と混入コミットを明記)。
- **横断規約(ECO-080)**: 幅のみ・文言なし= i18n 非該当。ScrollViewer.Padding 非該当= K-AVALONIA 適合。
- **機械受入(4 点)**: フルビルド 0 error・**0 警告** / Tests **847/847**(probe 3 本緑転含む)/
  Oracle 109+2skip / validate_bom 0/0。
- **R7**: 転写対象は幅のみで mock 実測値(208/240)との一致を probe が機械 pin(ECO-116 型=
  captures 並置でなく実測 pin)。diff に幅以外の視覚変更なし。視覚破綻の確認は §8 golden 基準へ。
- **R8(独立レビュー・fresh context)**: 所見 7 件全処置。
  - 問題なし(機械裏取り済み)5 件: 幅 240/208 で内容が崩れない構造(軸ラベルは
    TextTrimming あり・Sub 行は固定 loc 文字列のみ/⋯ 208 は画像タブ既 golden 面と同構造)・
    Popup 単一開の前提成立(全 Popup 既定 false)・悉皆 8 サイトで契約違反ゼロ・
    混入コミット言及の正確性(git -S 裏取り)・フルスイート共存(実測緑)。
  - **スコープ内(軽微)1 件= 処置済み**: 移動先メニュー 240 の pin 欠落 → 契約 4 値の全数 pin を
    閉じる 1 Fact を追加(契約適合の green pin・是正なし)。Tests 847/847 で緑確認。
  - **スコープ外 2 件= 51-cheat-log 記帳(2026-07-20)**: ①列ピッカー(幅 304)が部品表未登録
    ②chip overflow の実装は chipPopCard で popupMenu クローム写像未検証(ECO-122 lint first-run で
    顕在化見込み)。
- **diff 規模**: src 2 行(+コメント 2 行)・tests 1 ファイル新設(probe 3 本)。

## 8. 停止点= golden 合格基準(gate②・軽量)

実機で以下を確認してください:

1. **画像タブ**: ツールバーの表示軸ボタン(FS/ビュー名)をクリック → メニューが開く。
   幅が従来よりわずかに狭い(260→240)が、**FS/各ビューの行・チェック・ラベルが崩れない**こと。
2. **同・長いビュー名**: 名前の長いビュー(あれば)で行ラベルが**省略表示(…)で 1 行に収まる**こと
   (折返し・はみ出しがない)。
3. **作業タブ**: ⋯メニューを開く → 幅が従来よりわずかに広い(200→208)。削除・ゴミ箱の 2 行が
   自然で、ゴミ箱件数バッジ(deleted がある場合)が行内に収まること。
4. **回帰**: 画像タブ ⋯メニュー(208)・並び替えメニュー(両タブ 252)・移動先メニュー(240)の
   見た目が従来どおり(今回無変更・probe pin 済みの確認)。

合格なら `/eco-accept eco-123` を指示してください。
