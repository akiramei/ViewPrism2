# ECO-110: 右ペイン開閉時のグリッド作業対象喪失 — スクロールアンカー契約の未定義

- 起票日: 2026-07-18
- 報告者: maintainer(UX 所見)
- 種別: 不具合是正候補(UX・CAD layoutInvariant の保存対象曖昧 → gate① 裁定要)
- status: staged

## §1 症状

画像タブ(グリッド表示)で、右ペインが出る文脈モード(タグ編集モード・整理モード)を
開始すると、一覧の幅が縮む(右ペイン=Dock Right・幅 344px)ことでグリッドが再流動し、
**直前まで見ていた画像がビューポートから消える**。

体験シーケンス(報告文の再構成):

1. ユーザーはグリッドを見ながら「この画像たちにタグ付け/整理をしよう」と意思決定する。
2. モード変更ボタンを押下 → 右ペインが表示され、中央グリッド幅が 344px 縮む。
3. UniformGridLayout の列数が減り全行が再流動。スクロールオフセット(ピクセル値)は
   保持されるが、**同じオフセットが指す内容が変わる**ため、見ていた画像がスクロールアウト。
4. ユーザーは作業対象を見失い、スクロールで探し直す。

- 対象: グリッド(アイコン)表示のみ。リスト表示は行が全幅・行高固定のため、幅変化で
  行の再流動が起きず、オフセット保持=内容保持が成立する(症状なし)。
- 終了時も同型(右ペインが消えて幅が広がる → 逆方向の再流動)。

## §2 工程診断(R2)

| 工程 | 判定 | 根拠 |
| --- | --- | --- |
| CAD(ViewPrismUI) | **曖昧(保存対象が未定義)→ gate①** | `docs/screens/image_tab.md` layoutInvariant 行87 に「**右ペインの開閉が中央を壊さない**: 文脈モードの開始/終了で右ペインが出入りしても、中央のスクロール位置・グリッド段組が破綻しない」が**存在する**。しかし「スクロール位置が破綻しない」の保存対象が **(a)ピクセルオフセット**か **(b)可視コンテンツ(見ていたアイテム)**かを規定していない。現実装は (a) を満たし (b) を破る — 本症状はまさにこの曖昧さの間隙。 |
| BOM(30-ebom/32-mbom) | **未宣言** | E-UI-BROWSE-022 の acceptance(30-ebom:430-451)に右ペイン開閉×スクロール保全の観点なし(行447 の layoutInvariant 参照はリスト構造のみ)。E-UI-MODE-041 側にもなし。 |
| 実装 | **アンカー処理の不在(確定)** | ImageTabView.axaml.cs に Offset/BringIntoView/ScrollIntoView/anchor いずれも 0 ヒット(起票時 grep 実測)。グリッド= ScrollViewer+ItemsRepeater/UniformGridLayout(ImageTabView.axaml:1049-1132)・右ペイン= Width=344 の Dock Right(同 :110 タグ編集/:314 整理)。開閉時に何の補正もしない。 |

**診断確定: CAD の保存対象曖昧が根、実装は曖昧さの弱読み(オフセット保持)で製造されている。**
是正の要否ではなく**是正の契約(何を保存するか)**が未確定 → **gate①(CAD 裁定)が必要**。
CAD 是正(行87 の精緻化)が先、製品コードは後(/eco-fix は裁定後)。

## §3 切り分け済みの事実

### 3.1 確定(起票時実測)

- 右ペインは Dock Right・固定幅 344(ImageTabView.axaml:110 `EditMode` / :314 `IsOrganizeContext`)。
  開閉はオーバーレイでなく**中央幅の増減**として作用する。
- グリッドは `ScrollViewer > ItemsRepeater + UniformGridLayout(MinItemWidth=164)`
  (ImageTabView.axaml:1049-1052)。幅 −344px ≒ 2 列減 → 全行再流動。
- コードビハインド・VM とも、モード遷移時にスクロール位置へ触れる処理は皆無
  (保持も補正もしない= Avalonia ScrollViewer の素の挙動でオフセットが残るだけ)。
- 閲覧(browse)時は選択不可(ECO-017: 既定選択はタグ編集・作業の選択許可モードで有効化)。
  したがって**モード開始時点では「作業対象」は選択状態として存在せず、視認位置だけが手掛かり**。
- リスト表示は症状なし(§1)。作業タブは workspace 型(ペイン幅可変)だが本症状の報告面は
  画像タブのみ — 同族面の悉皆は裁定後の fix でスコープ確定(R3: 本 ECO で無断拡大しない)。

### 3.2 疑い(未検証)

- **CAD mock(HTML)はブラウザ標準のスクロールアンカリング(CSS `overflow-anchor:auto` 既定)
  により、右ペイン開閉時も先頭可視要素が自動維持される可能性が高い**。真なら CAD 行87 は
  暗黙に「可視コンテンツ保持」を意図しており、mock では無記述でも自然充足していたことになる
  (=実装だけが弱読み)。mock 実挙動は未実測 — 裁定時に maintainer が mock で確認可能。
- 開閉アニメーション有無・レイアウト確定タイミング(Avalonia の Measure/Arrange 後に補正が
  必要)は fix 時のプローブで確定。

## §4 是正方針(案・gate① 裁定対象)

いずれも CAD 行87 の精緻化(ViewPrismUI 側改稿)→ BOM 宣言 → 実装の順。

### 案A: 先頭可視アイテムアンカー(推奨)

右ペイン開閉(開始/終了とも)の直前にビューポート先頭の完全可視アイテムを記録し、
再レイアウト後にそのアイテムが先頭付近に来るようオフセットを補正する。
ブラウザ標準スクロールアンカリングと同等= mock の自然挙動(§3.2 疑い)とも整合。

- CAD 改稿: 行87 に「保存対象=可視コンテンツ(先頭可視アイテムのアンカー)。ピクセル
  オフセットの保持は充足と見なさない」を追記。
- 実装: ImageTabView.axaml.cs にモード遷移フック+アンカー実測/補正(推定 50〜80 行)。
- diff 規模: 中(コードビハインド 1 面+headless probe 2〜3 本)。VM/Core 無変更。
- golden: 実機シナリオ 1〜2 点(開始時/終了時に視認画像が可視のまま)。R7 captures は
  動作契約のため対象外(静止画で検証不能)— probe が機械受入の主体。

### 案B: 案A+作業対象優先

選択アイテムがある場合は先頭可視より選択先頭の可視維持を優先(BringIntoView)、
なければ案A へフォールバック。

- ただし §3.1 の通り**モード開始時は選択が存在し得ない**(閲覧=選択不可)ため、
  案A との差分は実質**終了時**(選択を残したまま閲覧へ戻る遷移があるか次第)のみ。
- diff 規模: 案A+小。効果の増分が薄い可能性 — 開始時の症状(報告の主訴)には案A で十分。

### 案C: 現状追認(オフセット保持を契約と明文化)

CAD 行87 を弱読み(a)に固定し、報告された体験問題を容認する。**推奨しない**
(報告の主訴を仕様化で棄却することになる。mock 自然挙動(§3.2)が真なら CAD とも矛盾)。

### サブ裁定: 適用範囲

- **(a) 右ペイン開閉のみ(推奨)**: 症状起点に最小スコープ。
- (b) 中央幅が変わる全事象へ一般化(左ペイン折畳み 276↔64=行83 の同族・ウィンドウ
  リサイズ)。効果は大きいが検証面が広がる — (a) の効果確認後に拡張起票が安全。

## §5 影響 BOM(案A 採択時の見込み)

- CAD: `../ViewPrismUI/docs/screens/image_tab.md` layoutInvariant 行87 精緻化(maintainer 裁定文面)。
- src: `ImageTabView.axaml.cs` のみ(モード遷移フック+アンカー補正)。XAML/VM/Core 無変更見込み。
- tests: headless probe — 開閉前後で先頭可視アイテム不変(是正前=赤で真因裏取り・R5)。
- BOM: E-UI-BROWSE-022 acceptance へアンカー契約 1 行(M4)。
- CP: CP-UI-G1 系へ観点刻印(accept 時)。
- 既存固定 Oracle 行は変更しない(R6)。

## §6 残ゲート

1. ~~**gate①(裁定・human)**~~ **裁定済み(2026-07-18 maintainer)**: **案A(先頭可視アイテム
   アンカー)+適用範囲 (a)(右ペイン開閉のみ)**。§3.2 の疑いは**確定へ昇格**: maintainer が
   mock を Chrome で確認し、右ペイン開閉時に先頭可視要素が維持される(overflow-anchor 既定の
   自然充足)ことを実機確認済み — CAD 行87 は暗黙に可視コンテンツ保持を意図していた。
   CAD 改稿済み(ViewPrismUI `81deead`・行87 精緻化=保存対象/非充足条件/mock 確認/適用範囲)。
2. ~~/eco-fix eco-110(probe 先行)~~ **実施済み(2026-07-18・§7)**。
3. **gate②(golden・human)**: 実機シナリオ検収(基準=§7.4)。

## §7 実施記録(2026-07-18・/eco-fix)

### 7.1 プローブ先行(R5)と真因の実測

`CpUiG1GridScrollAnchorTests` 4 本を先行追加(開=タグ編集/整理の Theory・閉・先頭ガード)。
是正前実測=**開: アンカー `img015.jpg` が完全スクロールアウト(実体化からも外れる)・
閉: `img009.jpg` 同**。3 本赤/先頭ガード 1 本緑 — §1 の症状を機械で固定。

**fix 中に確定した追加事実(設計を変えた)**: `ToggleEdit`/`ToggleOrganize` は
`Recompute()` で **Items(ImageItemVM)を再構築してから一括通知(string.Empty)する**
(ImageTabViewModel.cs:1834-1840/2039-2046)。したがって
「PropertyChanged 通知時点で旧段組の Bounds を実測する」案は不成立 — 通知時点で旧セルは
破棄済み。probe のアンカー同定も VM 参照でなく **Name(アイテム同一性)** で行う必要があった。

### 7.2 是正(案A の実装構造)

View 局所(ImageTabView.axaml.cs)の 3 段構造。XAML は x:Name 2 箇所付与のみ(視覚不変):

1. **TrackGridAnchor**: レイアウト確定(LayoutUpdated)ごとに先頭完全可視アイテム
   (ビューポート上端以下・最上段最左の実体化セル)の index を常時追跡。offset 0 は -1
   (先頭表示中は補正しない)。走査は実体化セル数のみ= IMG-014 EvaluateToolbar と同水準。
2. **エッジ検出**: VM は一括通知のため、View 側で ShowRightPane の前回値と比較して
   開閉エッジを検出し、追跡済み index を pending へ昇格。
3. **RestoreGridAnchor**: 開閉後最初のレイアウト確定で一度だけ、pending index を
   TryGetElement/GetOrCreateElement(仮想化外も実体化)→ ビューポート相対 Y を実測して
   オフセット補正(クランプ 0 以上)。失敗時も再試行しない(発振防止)。

VM/Core/他 XAML 無変更。i18n 文言なし(横断規約 ECO-080 非該当)。

### 7.3 機械受入(2026-07-18・全緑)

- build 0 error(既知 AVLN5001 警告のみ)
- Tests **806/806**(802 既存+probe 4 本が赤→緑転)/ Oracle 109+2skip / validate 0/0
- **因果検証**: エッジ採用 1 行を一時無効化すると probe 3 本が赤へ戻ることを実測
  (緑転が是正実装に帰属することの裏取り)→ 復元済み
- R7 セルフゴールデン=**対象外**(視覚不変・動作契約。probe が機械受入の主体)

### 7.4 golden 合格基準(gate②・maintainer 実機)

1. グリッド表示で下方へスクロールし、画面先頭の画像を覚える → **タグ編集モード開始** →
   右ペインが出た後も、その画像がビューポート先頭付近に見えている(作業対象を見失わない)。
2. 同様に**整理モード開始**でも維持される。
3. モード中にスクロール位置を変え、**モード終了**(右ペインが閉じ幅が広がる)でも
   直前の先頭画像が維持される。
4. **先頭(未スクロール)表示中の開閉では位置が動かない**(余計な補正なし)。
5. 退行なし: リスト表示の開閉挙動は従来通り・グリッドの選択/ダブルクリック等の既存操作不変。
