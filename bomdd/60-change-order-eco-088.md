# Change Order — ECO-088(ビュー軸チップ行の折返し不動作 — WrapPanel が横 StackPanel 測定で無効化されクリップ)

- 起票日: 2026-07-15
- 報告者: maintainer(IMG-023「47 件級チップの収まり」の実害確認指示)→ AI が実機実測で欠陥と確定
- 種別: 不具合(実装の CAD 乖離 — mock の `flex-wrap` は転写済みだが親コンテナが折返し契機を奪う配線欠陥)
- status: applied(2026-07-15 gate② 合格でクローズ)

## 1. 症状

画像タブのチップ行(タグビュー軸の階層ナビゲーション)が**折返しもスクロールもせず、
ウィンドウ右端で切り捨て**られる。定義値展開(REQ-096)で 47 件級(都道府県)の値ノードを
展開すると、ウィンドウ幅に収まらない値ノードへ**いかなる操作でも到達できない**。

実測(2026-07-15・スクラッチ DB=`VIEWPRISM2_DATA_DIR` 差し替えで実ライブラリ不変):

| 条件 | 観測 |
|---|---|
| 最大化(2896px 級) | 47 チップ中 **20 件のみ可視**。山梨県以降は画面外 |
| 幅 1400px | **8 件のみ可視** |
| 通常ホイール(チップ行上) | 行は動かない |
| Shift+ホイール(水平) | 行は動かない |
| オーバーフローの視覚手がかり | なし(省略記号・スクロールバー・「他 N 件」表示のいずれも出ない) |

再現手順: textual タグ(候補値 47 件=都道府県)を作成 → ビュー階層ノードに配置し
展開モード=定義値 → 画像タブでビュー軸に切替え、該当ノードへ潜る。
再現 seed はスクリプト化済み(scratchpad/seed_img023.py 相当 — tags/textual_tag_settings/
views/view_tag_hierarchies の 4 INSERT。tag type は `textual`)。

**定義値展開の中核価値(付与 0 件でも全定義値を定義順で安定表示=ECO-086 裁定 d)が、
47 件級=REQ-095/096 の起票動機そのものの規模で沈黙的に機能しない。**

## 2. 工程診断(R2)

| 工程 | 判定 | 証拠 |
|---|---|---|
| CAD(ViewPrismUI) | **健全** | mock `資料/画像タブ/ViewPrism2 画像タブ.dc.html` の `<!-- tag chip row -->` コンテナは `display:flex; align-items:center; gap:8px; padding:11px 18px; …; flex-wrap:wrap`(2026-07-15 grep 実測)= **折返しが CAD 正典**。prose `image_tab.md` はチップの内容次元(VC-IMG-5/6=件数追随・定義順・0 件淡色・未定義値)を定義済み |
| BOM | 健全 | E-UI-AXIS-NAV-040(画像タブ表示軸ナビゲーション)が surface として存在。CP-DEFEXP-086 は定義値チップの内容(破線/バッジ/0件表示)を headless 実測するが、**行コンテナのオーバーフロー挙動は検査次元になかった**(検査の谷間 — 下記潜伏機序) |
| 実装 | **逸脱と確定** | [ImageTabView.axaml:991-994](../src/ViewPrism2.App/Views/ImageTabView.axaml#L991): チップ行 ItemsControl の ItemsPanel は `WrapPanel`(= mock flex-wrap の転写)だが、**親が `Orientation="Horizontal"` の StackPanel**。横 StackPanel は子を無限幅で測定するため WrapPanel は折返し契機を得られず、そのまま溢れて親 Border にクリップされる |

- **混入コミット**: `bcd861d`(2026-06-17「画像タブ製造 M1+M2」)— チップ行の**初版から**
  この構成(`git log -S` + `git show` で実測。WrapPanel と横 StackPanel が同時に導入されている)。
- **潜伏期間**: 約 28 日(2026-06-17 → 2026-07-15)。
- **マスキング要因**: チップ数が常に 1 行に収まる少数だった。mock のデモも職種 5 件級で、
  ECO-086 の golden(実機)も同デモ規模のため素通り。**1 行に収まる限り WrapPanel の
  折返し不動作は視覚に現れない**(機能等価な潜伏)。47 件級を初めて実データで並べた本実測
  (IMG-023 の実害確認)が最初の可視化条件だった。
- **未確定事項との関係**: review_points **IMG-023** は「47 件級の折返し行数上限・行内
  スクロール・検索/絞り込み導線が**未設計**」を記録している。本 ECO はその**手前**にある
  「CAD が既に定義している折返し(flex-wrap)が実装で機能していない」欠陥であり、両者は別物。
  IMG-023 の未設計次元(後述 §6)は本 ECO のスコープ外に保つ。

## 3. 切り分け済みの事実(確定/未検証の分離)

確定(実測済み):

1. mock のチップ行コンテナは `flex-wrap:wrap`(§2 表)= 折返しは設計済み・未設計ではない。
2. 実装の WrapPanel は横 StackPanel 内にあり折り返さない(47 チップで実測・§1 表)。
3. 水平/垂直ホイールとも行は動かない = 切り捨てられた分への到達手段が存在しない。
4. チップ行は中央ペインの固定クロム(image_tab.md「クロム固定・本体のみスクロール」)の一部。
   1 行時のレイアウトは折返し是正後も不変(WrapPanel の単一行配置は同一)= 既存 golden の
   視覚は不変見込み。

疑い(未検証・/eco-fix のプローブで確定させる):

- (a) 折返し有効化後、47 件級でチップ行が 3〜4 行に膨らみ、固定クロムがサムネ領域を
  圧迫する(mock flex-wrap の素直な帰結だが、行数上限は CAD 未定義= IMG-023 の範囲)。
- (b) `chipHint` ラベル(「階層を掘る」)と折返し 2 行目以降の左端整列の関係
  (mock はヒント含め同一 flex コンテナ= 2 行目はヒント幅を継がない)。

## 4. 是正方針(案・着手時確定)

真因構造そのものを消す(ECO-038 原則): チップ行から**横 StackPanel を撤去**し、
ItemsControl(WrapPanel)が**有限幅で測定される**コンテナ構成へ変える。
案= Grid 2 列(Auto=chipHint / *=ItemsControl)または chipHint を WrapPanel 内の先頭要素として
mock と同型化。折返し行数の上限や行内スクロール等の**新設計は持ち込まない**
(そこは IMG-023 = CAD 裁定待ち。本 ECO は CAD 既定義の折返しの回復のみ)。

プローブ(是正前赤の実測計画): headless で 47 チップのビューを描画し、
全チップの実描画矩形が可視幅内に折返し配置されること(是正前= 21 件目以降の矩形 X が
可視幅を超える/クリップされることを赤で固定)。GF-073/086 様式の余白・はみ出し実測系。

## 5. 影響 BOM

- 実装: `src/ViewPrism2.App/Views/ImageTabView.axaml`(チップ行コンテナのみ・タイル/ソート等は不変)
- テスト: 折返し probe 新設(CpUi 系・47 件級)。既存固定 Oracle 行は変更しない(R6)
- CAD: mock/prose は**既に正**= 変更なし。`image_tab.md` visualContract へ
  **チップ行のオーバーフロー次元(VC-IMG-8 級)を lazy 遡及**(ECO-084/086/087 と同じ様式)
- ViewPrismUI `docs/review_points.md` IMG-023: 本実測(47 件で 20/8 件しか見えない・
  折返し復旧後は行数膨張が論点)を追記し、未設計次元を「折返し後の行数上限・行内スクロール・
  検索導線」へ精緻化
- E-BOM: E-UI-AXIS-NAV-040 は名称・責務不変(受入観点の追補のみ)

## 6. 残ゲート

- gate①(裁定): **不要** — CAD 健全・実装逸脱と確定(mock の flex-wrap が正典)。
- gate②(golden): 47 件級での折返し表示の実機確認+既存 5 件級デモの視覚不変
  (R7= 面全体並置・非既定構成= en 切替を含む)。
- スコープ外(IMG-023 に残る CAD 未定義・**本 ECO では触らない**): 折返し後の行数上限
  (固定クロム膨張とサムネ領域の緊張)・チップ行内スクロールの要否・47 件級での検索/
  絞り込み導線。折返し復旧の実機観測(gate②)が IMG-023 裁定の入力データになる。

## 7. 実施記録(2026-07-15 /eco-fix)

**プローブ先行(R5)**: CpUi088ChipWrapTests 2 本を CAD VC-IMG-8(本 fix で lazy 遡及)から生成。
是正前赤を実測 — 47 件テスト FAIL: 8 件目「茨城県」で実描画矩形 right=1460.0 > 可視幅 1366
(1 行のまま溢れて Border クリップ= §2 診断どおり)。5 件級 pin は緑(1 行・幅内=既存視覚の基準)。

**是正(真因構造を消す)**: チップ行の親 `StackPanel Orientation="Horizontal"`(子を無限幅で測定=
WrapPanel の折返し契機を奪う構造)を撤去し、`Grid ColumnDefinitions="Auto,*"`(ヒント列+可変列)へ。
ItemsControl は * 列で**有限幅測定**となり WrapPanel(mock flex-wrap の転写)が機能する。
多段時の行間は `WrapPanel LineSpacing="8"`(mock gap:8px の転写・1 行時は効かない=視覚不変)。
ヒントの後続間隔は旧 Spacing=8 と同値の Margin で維持。diff= ImageTabView.axaml 1 コンテナ+
制約コメント(新設計は持ち込まない — 行数上限等は IMG-023 のまま)。

**横断規約(ECO-080)**: 新規文言なし(i18n 不要)・VM/DB 不変。

**機械受入**: build 0/0・**Tests 709/709**(probe +2 全緑転)・Oracle 109+2skip(R6 不変)・
validate_bom 0/0。CP-CHIPWRAP-088 を 33-control-plan へ登録。

**セルフゴールデン(R7・面全体並置=チップ行サーフェスの全 VC 次元+共用面)**:

| # | 差分/次元 | 分類 |
|---|---|---|
| 折返し(VC-IMG-8・47 件全数が可視幅内・行数≥2) | 転写(是正そのもの・probe 実測) | — |
| 1 行時の配置(5 件級=mock デモ規模・既存 golden の視覚) | 不変(probe pin 緑+ヒント間隔は旧値と同値) | — |
| VC-IMG-5(件数のモード追随)/VC-IMG-6(定義順・0件淡色・未定義値)/VC-IMG-7(パンくず) | 不変(ChipVM/VM 無改修・既存 probe 全緑=709 に包含) | — |
| FS 軸の絞り込みチップ(同一コンテナ共用) | 同修正が適用(折返し可能化)・既存テスト緑 | — |
| **多段時の 2 行目開始位置**: mock flex は コンテナ左端(ヒントの下)/実装 Grid はヒント列の右 | **未裁定の候補差分** — mock は多段のデモを持たず(5 件=1 行)厳密には未定義。発明を避け構造最小変更(Grid)を選択。多段面の最終設計は IMG-023 裁定で確定 → golden で提示 | 要 golden 判定 |

転写漏れ 0(ヒント位置・チップ意匠・行下線・背景は無改修)。

**実機採取(起票時・是正前)**: スクラッチ DB(47 都道府県 seed)で最大化 20/47 件・幅 1400px
8/47 件・ホイール/水平ホイール不動を確認済み(§1)。是正後の実機確認は gate②。

## 8. 残ゲート(更新)

- gate②(golden)のみ。§6 のスコープ外(IMG-023)は不変。

## 9. クローズ(2026-07-15 golden 合格)

**gate② 承認(maintainer 実機)**: 47 件級(都道府県)の複数行折返し=北海道〜沖縄県まで全数到達・
狭幅(1400px 級)での行数追随・1 行時の視覚不変(少数チップ+FS 軸絞り込みチップ=同一コンテナ共用面)・
**多段時 2 行目開始位置=ヒント列の右(Grid)を許容**(R7 で提示した未裁定候補差分の裁定。mock は
多段デモを持たず未定義 — 最終形は IMG-023 の多段設計で確定)・en 切替。

**クローズ 3 点セット**: CP-CHIPWRAP-088 へ golden 承認+潜伏実績(混入 bcd861d・28 日・
1 行に収まる限り不可視の機能等価潜伏)を明記(再発防止)/ register `applied`+golden approved / 本節。
**M4 同期**: 挙動仕様の変更なし(CAD 既定義の折返しの回復=REQ 新設不要)。E-BOM E-UI-AXIS-NAV-040
説明へ ECO-088 追補・CAD 正典= ViewPrismUI `28f7c06`(VC-IMG-8)+review_points IMG-023 実測追記。

**教訓**:

1. **部品の転写が正しくても、親コンテナの測定意味論が部品の契約を殺す — レイアウト契約の検査は
   コンテナ込みの実レイアウトで行う**。WrapPanel(mock flex-wrap の正しい転写)が横 StackPanel の
   無限幅測定で折返し契機を失い、初版から 28 日潜伏した。GF-084-01(アイコン用固定幅部品の流用)・
   GF-086-01(等分 Stretch 前提部品の内容幅流用)に続く**部品適用条件系譜の 3 例目** — ただし本件は
   「部品自体の取り違え」でなく「正しい部品を殺す文脈」であり、適用条件の検査対象を部品選定から
   **導入コンテナの測定文脈**へ広げる(rule of three 充足=BomDD 昇格候補)。
2. **検査 fixture の規模は「起票動機の規模」で張る — デモ規模は本番規模を代表しない**。定義値展開
   (ECO-086)の要求動機は 47 都道府県だったが、受入 fixture は候補値 3 件へ簡略され、mock デモも
   5 件級 — 規模がもたらす次元(オーバーフロー)が全検査面から抜けた。機能等価な潜伏(1 行に収まる
   限り視覚に現れない)はこの谷間で成立した(BomDD 昇格候補)。
3. **review_points の「未設計」懸念は実測で分解してから裁定に出す**。IMG-023 は「47 件級の収まりは
   未設計」と記録していたが、実害の実機実測が「CAD 既定義の折返しの実装欠陥(本 ECO=裁定不要)」と
   「真に未設計の次元(行数上限・行内スクロール・絞り込み導線=IMG-023 残置)」に分離した —
   実測が工程診断を精密化し、裁定の負担を最小化した(観測 1 例目)。
