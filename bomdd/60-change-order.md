# Change Order — ECO-001(Phase 7 変更オーダー)

> 納品後の仕様変更・機能追加の再入口(playbook §8)。本 ECO は **文書のみの是正**(spec/E-BOM の記述明確化)であり、製造物(コード・固定オラクル・テスト・golden)は一切変更しない最軽量適用。
> したがって 61(影響分析詳細)・62(データ移行)・63(不要改変監査)・部分再製造・回帰は **不要(N/A)**。理由は §2・§4 に記す。

## 0. 変更前 baseline の凍結
- As-Maintained 個体: factory-01 / commit `41d2f97`(BOM v1.2、tag `loop-v1-r1` の凍結オラクル)
- データ fixture: **N/A**(挙動変更なし。永続データ・移行を伴わない)
- 既存固定オラクル: S-01〜S-12(`tag:loop-v1-r1` で凍結)。**本 ECO で不変**。とりわけ **S-01(リネーム追跡 E2E)** が「再スキャン後: 旧行=missing+新行=pending(candidate=旧行 id)」を期待し PASS 済み = 是正後の記述と既に整合

## 1. 変更要求
- ECO-ID: **ECO-001**
- 変更内容(1〜3行):
  `20-spec.md §2.1 スキャン` の手順番号付けが、実行順を「各ファイル判定(手順 3)→ missing 化(手順 4)」と**誤って示唆**していた。
  実際の正しい実行順は **missing 化(手順 4/5)を判定(手順 3)より先に**適用する(単一スキャンのリネームを `旧=missing`＋`新=pending(candidate)` に確定するため)。
  字義通りに読むと将来の再製造・別工場が誤った順序を実装し、固定オラクル S-01 を落とすリスクがあるため、記述を明確化する。
- 種別: **欠陥修正(文書忠実度)** — 仕様プローズのみの欠陥。製造物(コード/オラクル)は当初から正しい
- REQ への反映: REQ-012/REQ-016 の**改訂なし**(意味は不変)。§2.1 本文に「実行順序(重要)」注記を追加し、手順 3/4/5 に相互参照を付与

### 発見経緯(検査器としてのリバース入力)
原典 `view-prism` の source-grounded リバース資料(`docs/porting-spec/`)と V1 成果物の突き合わせで検出。
- 原典の権威的根拠: **RVP-SCAN-003**(08-reverse-trace-matrix)、`02§1 L25`(「先に normal 画像の missing 判定を行い、その後に追加/更新/再リンク候補を処理する」)、`06§2 手順5-11`
- V1 側の整合確認: コード `ScanService.cs:117-143`(missing 化を判定より先に実行する旨を明示コメント済み)・固定オラクル S-01(PASS)
- 切り分け: **実装は正しく、仕様プローズのみが弱い**。原典リバース資料が V1 仕様の微細なズレを見つける検査器として機能した事例

## 2. 影響分析(トレース逆引き+影響なし予測)
| 段 | 影響 ID | 備考 |
|---|---|---|
| 仕様節 | 20-spec.md §2.1(スキャン手順) | 記述明確化のみ。規則・遷移表・REQ の意味は不変 |
| E-BOM 部品 | E-SCAN-005 | 不変条件を 1 行追記(missing 化の先行を明文化)。graph_edges/consumers 変更なし |
| M-BOM unit | M-SCAN-005 | **変更なし**(実装は既に正しい順序) |
| Control Plan 特性 | CP-SCAN-004 | **変更なし** |
| 固定オラクル行 | S-01 | **変更なし**(既に正しい期待値で凍結・PASS) |
| K-BOM / 調達部品 | — | 影響なし |
| 製造物(code/test/golden) | — | **影響なし** |

- **影響なし予測(反証可能・凍結)**: 本 ECO は spec/E-BOM の**記述**のみ変更し、コード・オラクル・テスト・golden の挙動には一切影響しない。
  反証条件 = 「文書是正後にビルド/全テスト/固定オラクルのいずれかが赤化」。挙動を変えていないため発生し得ない(予測: 影響ゼロ)。

## 3. BOM 改訂
- bom_rev: **v1.2 → v1.2(spec-doc-rev:ECO-001)** — 挙動・製造物・オラクル凍結 tag `loop-v1-r1` は不変。spec/E-BOM の文書改訂のみ
- 改訂ファイル: `20-spec.md`(§2.1 実行順序注記+手順 3/4/5 相互参照)、`30-ebom.yaml`(E-SCAN-005 不変条件 1 行追記)
- 変更分の受入(オラクル・ファースト): **新オラクル行なし**。是正後の期待値は既存 S-01 が既に体現しているため追加不要
- 治具の凍結条件: **N/A**(オラクル不変)

## 4. 部分再製造
- 再製造/改修対象: **なし**(N/A)。コードは当初から正しい順序を実装済み
- 再利用 unit: 全 unit(変更なし)
- 工場投入: **不要**。製造物に触れないため fresh factory 投入は行わない
- 渡すもの/渡さないもの: N/A(工場投入なし。隔離規律は将来ループで継続)

## 5. 回帰+変更受入(失敗5分類で帰属)
- **N/A**(製造物無変更)。確認として既存緑状態を参照: ビルド警告 0 / unit 190 全緑 / 固定オラクル 20/20 PASS(50-as-built)。本 ECO はこれらを再実行する必要を生じない
- 結果: 文書是正のみ完了。`regression` / `change miss` / `data-preservation miss` / `unnecessary modification` いずれも該当なし

## 6. 記録
- As-Built 追記(50): `eco` セクションに ECO-001、`reverse_input` に porting-spec の所在を追記
- Service BOM 更新(53): **N/A**(未作成。Phase 6 で作成時に ECO-001 を既知事項として引き継ぐ)
- cheat-log(51・C6系): **該当なし**(工場のずるではなく設計者側の仕様プローズ欠陥。本 ECO 自体が記録)
- metrics(52・ECO 行): `eco` ブロックに ECO-001(doc-only・影響ゼロ)を追記
