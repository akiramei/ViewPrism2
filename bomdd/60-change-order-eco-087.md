# Change Order — ECO-087(applied): タグ作成/編集ダイアログの mock 忠実化(2 カラム化・ヘッダ・選択肢 UI・意匠)

- 起票日: 2026-07-14 / 報告者: maintainer(ECO-086 golden 中の実機/mock 並置スクリーンショット)
- 種別: 不具合(実装の CAD 乖離=製造時転写の面欠陥。ECO-086 golden 所見からの R3 分離起票・裁定 A)
- baseline: main `c43a286`

## 1. 症状

ECO-086 の golden(2026-07-14)で maintainer が mock(`資料/タグタブ/ViewPrism2 タグ作成ダイアログ.dc.html`
= Design Direction 02)と実機のタグ作成ダイアログを並置し「かなり違う」と確認。

**差分の全列挙(2026-07-14 並置)**:

| # | 差分 | 備考 |
|---|---|---|
| 1 | 全体形態: mock=**2 カラム**(左=基本情報/右=種別別設定・幅 768)/実機=**1 カラム**(幅 440) | CAD prose(tag_tab.md「レイアウト」節)も左右分割を明記 |
| 2 | ヘッダ: mock=アイコン+タイトル+**種別で変わるサブタイトル**+X クローズ/実機=プレーンタイトルのみ | |
| 3 | 選択肢 UI: mock=ピル行(ドラッグハンドル+ラベル+×)・「+追加」青ボタン・「登録済みの選択肢 (N)」件数ヘッダ+「ドラッグで並び替え可能」右上配置/実機=素の ListBox+↑↓/削除ボタン | 挙動(追加・削除・並べ替え=REQ-024)は適合。見た目のみ乖離 |
| 4 | フッター/意匠: mock=青「✓ 保存」・角丸・カード影/実機=標準ボタン | |
| 5 | 値の扱いの ⓘ アイコン: mock=説明カードに ⓘ /実機=なし | ECO-086 新設面の小差(本 ECO で吸収) |

## 2. 工程診断 — 実装逸脱と確定(CAD 健全・gate① 裁定不要)

| 工程 | 判定 | 根拠 |
|---|---|---|
| CAD(ViewPrismUI) | **健全** — mock(DD02)+prose(tag_tab.md「タグ作成/編集ダイアログ」節=左/右/下の 3 領域)とも 2 カラムを明記 | 乖離時は常に CAD が正 |
| BOM/検査 | **谷間あり** | 過去 golden(G-6/ECO-007)は「種別セグメント・候補値並べ替え・カラー 9 色・高さ固定」の**項目列挙検査**で、**面全体の mock 並置が検査次元に無く**乖離が潜伏(GF-084-01「検査次元に無いものは素通り」と同族)。tag_tab.md visualContract は ECO-086 で新設したが VC-TAG-1〜3(新設要素)のみ=面全体の VC 未整備 |
| 実装 | **逸脱** | ECO-007 製造時に mock の**部品単位**(セグメント/カラー/プレビュー帯)のみ転写し、全体形態(2 カラム・ヘッダ・選択肢ピル UI・意匠)が未転写のまま(混入=製造時からの原始乖離・golden 谷間でマスク) |

**結論**: 実装逸脱(+検査の谷間の併存)。CAD は健全のため gate① 裁定は不要 — ただし fix 時に
CAD 側 visualContract へ**面全体の検査次元**(VC-TAG-4〜: 2 カラム形態・ヘッダ・ピル行・件数ヘッダ・
フッター意匠・日英収まり)を lazy 遡及で先に生成してから着手する(/eco-fix 手順 1 の規律)。

## 3. 切り分け済みの事実

- 挙動(REQ-021〜025: 名前/種別/カラー/説明/候補値の追加・削除・並べ替え/numeric 設定/値の扱い
  REQ-095)は全て適合(Tests 700/700)。乖離は**視覚・レイアウトのみ**。
- ECO-086 で入れた「ボディ単一スクロール」(GF-086-01 是正)は 2 カラム化後も layoutInvariant
  (ヘッダ固定/ボディ単一スクロール/docked フッター+プレビュー帯固定)として維持対象。
- mock の 2 カラムは幅 768。小画面での縮退(1 カラム化 or MaxWidth)は mock に定義なし →
  発明せず、fix 時に必要なら review_points(ViewPrismUI)へ登録。

## 4. 是正方針(案 — 着手時確定)

1. CAD(ViewPrismUI): tag_tab.md visualContract へ VC-TAG-4〜(面全体の検査次元)を lazy 遡及で追補
   (mock は改版不要=既に正)。
2. 実装: TagEditorWindow.axaml を mock 準拠へ — 2 カラム Grid(左=名前/種別/カラー/説明、右=種別別
   設定+値の扱い)・ヘッダ(アイコン+タイトル+種別サブタイトル+X)・選択肢ピル行(ハンドル+×)+
   青「+追加」+件数ヘッダ・フッター意匠。VM は表示用の派生(サブタイトル・件数)を追加する程度。
3. プローブ: headless 実レイアウト(2 カラム存在・ヘッダ/ピル行/件数・日英収まり=余白実測)を
   VC-TAG-4〜 から生成し、是正前赤→緑転。
4. オラクル: 既存固定行不変(R6)。

## 5. 影響 BOM

- CAD: ViewPrismUI docs/screens/tag_tab.md(visualContract 追補のみ・mock 不変)
- 実装: TagEditorWindow.axaml(全面)+TagEditorViewModel(表示派生の小改修)
- テスト: CpDefExp086UiTests のタグ編集系 probe は維持(スクロール/セグメント余白)+新規 VC probe
- 既存固定オラクル・DB・i18n 語彙: 変更なし(文言は既存キー再利用が主・不足分のみ追加)

## 6. 残ゲート

- gate① 裁定: 不要(実装逸脱・CAD 健全)。
- gate②(golden): mock 並置で面全体の一致確認(許容差分があれば裁定)。**是正+機械受入は完了(§8)— golden 待ち。**

## 8. 実施記録(2026-07-14 /eco-fix — 是正+機械受入完了・golden 待ち)

**手順 1(CAD lazy 遡及)**: ViewPrismUI tag_tab.md へ **VC-TAG-4〜8**(面全体の検査次元: 2 カラム+
4 段クローム/ヘッダ種別サブタイトル/選択肢ピル行+件数ヘッダ/docked 強調保存/日英収まり)を新設
(`d432596`・mock 不変)。**意図的差分の記録**: mock シンプル型説明の「あとからテキストや数値に変更も
できます」は ECO-008(種別は作成後変更不可)により転写しない(CAD 注記済み)。

**プローブ先行(R5)**: CpTagDlg087Tests 新設(VC-TAG-4〜8 から生成・headless 実レイアウト)。
是正前実測= **6/6 不合格**(1 カラム縦積み・HeaderSubtitle/OptionCountHeader/ピル行 × の不在・
0 件プレースホルダ不在)→ 是正後 6/6 緑転。

**是正内容(TagEditorWindow.axaml 全面+VM 表示派生)**:

- 幅 440→768・4 段クローム: ヘッダ(タグアイコンバッジ+タイトル+**種別で追随するサブタイトル**
  〈作成時のみ=mock は作成を定義・編集は非表示で発明回避〉+X)/ボディ 2 カラム(左 316px=
  基本情報・縦区切り・右=種別別設定)/固定プレビュー帯/docked フッター(強調保存 ✓)
- テキスト型: 「選択肢を追加」+入力+強調追加ボタン(**入力空は非活性**=mock addReady)・
  「登録済みの選択肢 (N)」件数ヘッダ+ドラッグ注記・**ピル行**(ハンドル+ラベル+× 削除=
  RemovePredefinedValueItemCommand)・0 件は**破線プレースホルダ**(Rectangle 重ね)・
  ↑↓/削除ボタン列は撤去(mock に無い — D&D+× へ集約。VM の Move コマンドは unit 用に存続)
- 数値型: 定義域 Grid+**プリセット 3 チップ**(★評価 1–5/% 0–100/pt 0–10=ApplyNumericPreset)+
  情報カードへ ⓘ(差分⑤)
- シンプル型: 用途説明カード新設。説明欄=複数行 textarea(mock 準拠)
- **維持**: ボディ単一スクロール(GF-086-01)・値の扱いセグメント(ECO-086)・候補値リスト内部
  スクロール(GF-01)・D&D 並べ替え(code-behind 無改修=ValuesList/TitleText の x:Name 互換)
- i18n: ja/en 各 15 キー(サブタイトル 3・基本情報・選択肢系 4・プリセット 4・シンプル説明 2・
  プレビューと用途)。全文言 Loc 経由(ECO-080 lint 適合)

**機械受入**: build 0/0・**Tests 706/706**(新規 CpTagDlg087Tests 6 本。GF-086-01/ECO-086 の既存 probe は
2 カラム化後も全緑=収まり・値の扱い・スクロール契約の維持を機械確認)・Oracle 109+2skip(R6 不変)・
validate_bom 0/0。CP-TAGDLG-087 を 33-control-plan へ登録。

**セルフゴールデン(R7・面全体並置=ECO-086 教訓③の 3 分類)**:

| # | 差分 | 分類 |
|---|---|---|
| 2 カラム/ヘッダ/ピル行/フッター/プリセット/ⓘ/textarea | 転写済み(probe 6 本+文言突合=ドリフト 0) | — |
| シンプル型説明の「あとから変更可」一文 | **意図的差分**(ECO-008 裁定引用・CAD 注記済み) | 裁定済み |
| 編集時のヘッダサブタイトル非表示 | **意図的差分**(mock は作成のみ定義=発明回避) | 記録済み |
| カラー hex が入力可(mock は表示のみ)・スウォッチ 20px(mock 28px) | **裁定済み許容**(REQ-023 自由 hex=既存機能・E7 golden 済み) | 裁定済み |
| 保存の押下可否可視(検証不能時も押せて検証エラー表示) | 既存挙動維持(CAD layoutInvariant「押下可否が見える」適合) | — |

転写漏れ 0。

**スコープ外所見(R3)**: tag_tab.md「タグモデル」共通属性の「種別: 作成後も変更可能」が ECO-008 裁定と
矛盾する旧記述(CAD prose 内の残骸)— CAD 注記に既記載・prose 本文の是正は ViewPrismUI 側の軽微
doc 課題として残置(発見時に是正)。

## 9. GF-087-01 是正記録(2026-07-14 — golden 所見→同日是正)

**所見(maintainer 実機)**: 候補値をドラッグで並べ替えられない。

**工程診断=従来からの潜伏欠陥の顕在化**: Avalonia の ListBox(SelectingItemsControl)はクラスハンドラが
「未選択項目の押下=選択更新成功」で `e.Handled=true` にするため、XAML の `PointerPressed`
(bubble・handledEventsToo=false)へイベントが届かず、**未選択の行では D&D を開始できない**
(選択済みの行なら 2 度目の押下で動く)。従来は ①↑↓ボタンが並べ替えの実用経路としてマスク
②選択視覚があり「選択→再ドラッグ」が成立し得た — ECO-087 で ↑↓ を撤去(mock 準拠)+ピル化で
選択視覚も消えたため「並べ替え不能」として顕在化した。

**プローブ先行(R5)**: 是正前赤=ピル行にドラッグハンドル要素(pillHandle)が存在しない(0/2)。
headless の実ポインタ注入(MouseDown)で「未選択行のハンドル押下がドラッグ起点へ届き Handled で
消費される(選択と分離)」ことを是正後に実測(D&D フル経路=DoDragDropAsync 以降は headless で
再現不能のため golden の検査項目)。

**是正(真因構造を消す)**: mock どおり**ドラッグハンドル(⠿)を明示のドラッグ起点**にする —
ハンドル Border の direct `PointerPressed` で D&D を開始し `e.Handled=true`(クラスハンドラの選択処理と
競合しない・選択とドラッグの分離)。行全体からの D&D(選択済み行のみ有効=従来のバブル経路)は併存。
×ボタン・追加・件数ヘッダ等の既存 probe は全緑(無影響)。

**機械受入(再)**: build 0/0・**Tests 707/707**(GF probe +1=計 CpTagDlg087Tests 7 本)・
Oracle 109+2skip(R6 不変)・validate 0/0。

## 10. クローズ(2026-07-14 golden 合格)

**gate② 承認(maintainer 実機・mock DD02 並置)**: 2 カラム+4 段クローム・種別サブタイトル追随・
選択肢ピル行(**ハンドル D&D 並べ替え=GF-087-01 是正後は未選択行でも 1 回で掴める**・×削除・
件数追随・0 件破線)・数値プリセット・シンプル用途カード・説明 textarea・値の扱い/ボディ単一
スクロール(ECO-086/GF-086-01)維持・en 切替。意図的差分(ECO-008 一文非転写・編集時サブタイトル
非表示・hex 自由入力)も承認。

**クローズ 3 点セット**: CP-TAGDLG-087 へ golden 承認+潜伏実績(GF-087-01=D&D の選択競合)を明記
(再発防止)/ register `applied`+golden approved / 本節。
**M4 同期**: spec §2.6 タグ作成/編集ダイアログ記述へ as-built 追記(2 カラム+4 段クローム・
ピル行 D&D)・E-UI-TAGS-026 の name/invariants 追補。CAD 正典= ViewPrismUI `d432596`(VC-TAG-4〜8)。

**教訓**:

1. **バブル頼みのジェスチャ開始はコントロールのクラスハンドラ(handled)と競合する — 操作の起点は
   専用要素の direct ハンドラで持つ**(GF-087-01)。ListBox の選択更新が press を handled にするため、
   行バブルの D&D は「未選択行で開始不能」という発見困難な半死状態になっていた。mock がハンドル
   (⠿)を描いていたのは操作起点の明示でもあった — 視覚だけ転写して操作起点を転写しない忠実化は
   半分の忠実化。
2. **代替導線の撤去はマスクされていた欠陥を顕在化させる — 撤去前に主経路を実機で通す**。↑↓ボタンは
   D&D 欠陥の実用マスクだった。mock 準拠で導線を減らす変更は「残る導線が本当に動くか」の確認を
   probe/golden 基準へ明示的に含める(read-across= ECO-077 教訓「導線変更は文脈パラメータ棚卸し」の
   ジェスチャ版)。BomDD 昇格候補。
3. R7 面全体並置(ECO-086 教訓③の初適用)は有効に機能した — 差分 3 分類で転写漏れ 0 を確認した上で
   golden へ出し、残った所見は視覚でなく**対話ジェスチャ**だった。並置(静的視覚)はジェスチャを
   検査できない — 視覚契約に「操作の起点・ジェスチャ」次元を持たせる(VC-TAG-6 に D&D 起点=
   ハンドルを追補済み)。

## 7. 関連

- ECO-086(発見元 golden・値の扱い面は本 ECO の 2 カラム化で右カラムへ移設される)
- ECO-007(混入元=部品単位転写)・GF-084-01/GF-086-01(検査次元の谷間の同族)
