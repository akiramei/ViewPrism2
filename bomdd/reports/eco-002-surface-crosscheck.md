# ECO-002 表面次元の全数突き合わせ(P0-1)

日付: 2026-06-12 / 実施: 設計AI / 方法: porting-spec **RVP-\* 全行**(08)+**02/03/06 の挙動記述**を V1 20-spec.md(§2.1〜§2.7、特に §2.6)と行単位で照合。
判定区分: ✅一致 / 🔷既知の意図的差分(フラグ済み・golden 裁定対象) / 🆕CR(脱落 → ECO-002 へ追加) / ⏸DEFER(スコープ外・明示記録) / 📋META(資料側への所見)

## 1. 横断不変条件(RVP-INV)

| 行 | 判定 | 照合結果 |
|---|---|---|
| RVP-INV-001 物理ファイル非破壊 | ✅ | V1 は INV-009(scan/thumb 読み取り専用)。削除系機能なし。V2+ で woven 格上げ(ECO-001 申し送り済み) |
| RVP-INV-002 状態 4 値 | ✅ | 全 4 値定義済み。deleted は V1 予約(遷移表で明文化) |
| RVP-INV-003 コレクション境界 | 🆕 | **CR-2(確定済み)**。G1④で検出済みの脱落 |
| RVP-INV-004 messages/ja・en が正 | ✅ | K-I18N で変換済み(E-I18N-ASSETS-027) |

## 2. コレクション/スキャン(RVP-SCAN、02§1)

| 項目 | 判定 | 照合結果 |
|---|---|---|
| RVP-SCAN-001〜005(順序・拡張子・同一性・pending 削除) | ✅ | ECO-001 で照合・是正済み |
| 02§1「コレクション一覧に名前、パス、画像数、スキャン中状態」(03: アイコン+スピナー+ホバー削除) | 🆕 | **CR-8**(CR-2 へ統合): V1 左ペインは名前+スキャンボタンのみ。**画像数+スキャン中表示**を追加。パスはツールチップ可。削除は V1 の管理ダイアログ方式を維持(意図的差分として記録) |
| 02§1「選択中のコレクションは永続化し、次回起動時に復元」 | 🆕 | **CR-5**(CR-2 へ統合): settings に last_collection_id を追加 |
| 02§1 初回スキャン優先・件数返却 | ✅ | REQ-015 サマリ(粒度差は ECO-001 注記済み) |
| 02§1 コレクション削除の確認ダイアログ | ✅ | REQ-010(連鎖削除の確認必須) |
| サイドバー折りたたみ(03§1) | ⏸ | V2(小・優先度低) |

## 3. 画像一覧/ビュー(RVP-VIEW、02§2、03§1)

| 項目 | 判定 | 照合結果 |
|---|---|---|
| RVP-VIEW-001 filesystem ビュー | 🔷 | V1 は左ペインツリーで AND パス意味論(§2.6 フラグ済み)。golden G1 で承認者はツリー自体を否認せず → 維持 |
| RVP-VIEW-002 tag-based view | ✅ | NodeGraph(REQ-035〜037) |
| RVP-VIEW-003 view フィールド | ✅/⏸ | display_columns/sort/favorite/home_tag あり。**view_revisions は V1 スキーマに無し → DEFER(V2。既存 DB 移行なしのため安全)** |
| 02§2「コレクションとビューを選択して表示。どちらか未選択なら選択を促す空状態」 | 🆕 | CR-2 の仕様詳細に含める(コレクション未選択 → 中央案内) |
| 02§2「表示モード(グリッド/リスト)はユーザー設定として永続化」 | 🆕 | **CR-6**: settings に display_mode を追加(REQ-052 改訂) |
| 02§2「ビューに表示列が設定されている場合、表示列からソート候補を生成」(動的ソート・タグ列) | ⏸ | **01 が Should と明記** → V2 へ明示 defer。V1 は固定軸(CR-4 後: name/modified/size) |
| 02§2 デフォルトソート「名前、日付、サイズ」 | ✅(CR-4 で整合) | 「日付」の 2 軸展開が G1③ → created_date 除外で原典 3 軸と一致 |
| ViewHeader フォールバック(06§8: 選択ビュー消失 → filesystem へ) | ✅ | V1: FindViewItem ?? 全画像(CR-2 後: 選択コレクションの全 normal 入口)— 同型のフォールバック |
| パンくず | 🔷 | ツリー常時表示で代替(§2.6 フラグ済み) |

## 4. グリッド/リスト/選択(03§1)

| 項目 | 判定 | 照合結果 |
|---|---|---|
| 03 L96「**列数・正方形セル・選択順バッジは現行互換の必須仕様ではない**(採用するなら移植版の設計判断)」 | 📋 | CR-1(列数撤去)の裏付け。正方形セル+選択順バッジは V1 設計判断として維持(golden で承認者が否認していない) |
| 03 L117「**Ctrl/Shift クリックの選択規則は固定しない。採用する場合は受入条件を別途定義**」 | 📋 | CR-3 の実装定義をここで確定: **原典 useImageSelection.ts:24-34 方式 — SHIFT+クリック=最後の選択アイテムからクリック位置までの範囲を既存選択へ union(置換しない)。選択順バッジは index 順で末尾に追番**。unit ベクタ化 |
| 通常クリック=ビューア(原典) vs V1 ダブルクリック=ビューア | 🔷 | V1 は「クリック=選択 / ダブルクリック=ビューア」(§2.6 規定・golden G-4 対象)。原典は通常時クリックでビューア・モード中は選択。V1 方式を維持(モードレスで選択と閲覧を両立する設計判断。golden で承認者が操作系自体を否認していない) |
| モード排他(03 L344) | ✅ | V1 はタグ編集モードのみ(整理/作業は V2+)→ 排他は自明 |
| 空状態群 | ✅ | §2.6 空状態規則 |

## 5. タグ/ビュー編集 UI(02§3〜4、03§3)

| 項目 | 判定 | 照合結果 |
|---|---|---|
| 02§3「タグには…**親タグを設定できる**」 | 📋✅ | **原典 NewUI の TagManagementPanel に親タグ UI は存在しない**(grep で parent 言及ゼロ)。02§3 はサービス/DB 能力の記述(新旧混在)。V1 も同様(サービス層 REQ-022 のみ・UI なし)→ **原典新 UI と一致。CR 不要**。V2 で UI 露出を検討可 |
| 02§4「ビューには名前、説明、お気に入り、**ソート設定、表示列**を設定できる」 | 📋✅ | **原典 NewUI にソート/表示列の編集 UI は無い**(ViewManagementPanel は favorite の表示のみ、displayColumns の編集箇所は NewUI に無し)。DB/サービス能力の記述。V1 ダイアログ(名前+説明+favorite)は NewUI 同等以上 → CR 不要 |
| V1 実装がビュー編集で IsFavorite を編集可(仕様 §2.6 は「名前+説明」) | 📋 | 実装が仕様を上回る(正方向)。**v1.3 で §2.6 に favorite を追記**(仕様を実装・原典能力に合わせる) |
| タグ作成ダイアログ(型別フォーム・候補値・カラー) | ✅ | §2.6 v1.2(G-6 対象) |
| 階層編集(D&D・別名・条件・ホームタグ・バッチ保存) | ✅ | §2.6 v1.2(G-6 対象) |

## 6. ビューア(02§2、RVP-UI-003)

| 項目 | 判定 | 照合結果 |
|---|---|---|
| 02§2「ESC **または背景クリック**で閉じる」 | 🆕 | **CR-7**: V1 は Esc+閉じるボタンのみ。背景クリック閉じるを追加(REQ-044 改訂) |
| RVP-UI-003 拡張モード(scroll/spread/custom) | ⏸ | チャーター明示除外(ビューア拡張ループ=V2)。01 でも Should |
| フィット表示(縮小のみ)・端停止・位置/総数 | ✅ | REQ-044(Esc・フィットの差分はトレース済みの意図的差分) |

## 7. その他の領域

| 項目 | 判定 | 照合結果 |
|---|---|---|
| RVP-STATUS-001〜004(pending 承認/ゴミ箱) | ⏸ | チャーター除外。**注意書き**: 原典は再スキャン新規→pending(承認待ち)の趣旨の記述あり(06§3)。V1 遷移表は新規→normal(3b)。V2 で pending 承認フロー導入時に**要裁定**(固定オラクル S-01 は relink 候補意味論で凍結済み) |
| RVP-REPAIR-001〜005(修復モーダル群) | ⏸ | V1 は基本再リンク(REQ-017)のみ。高度修復は V2+(RVP-REPAIR-004/005 の矛盾裁定もそのとき) |
| RVP-SIM/MERGE/WORK/BACKUP 全行 | ⏸ | チャーター明示除外(V2+)。porting-spec が source-grounded 入力として待機 |
| 作業タブ(03§4) | ⏸ | チャーター除外(§2.6 で「V3 スコープ」と明文) |
| 設定タブ構成(03§5: バックアップ/一般/情報) | ⏸/✅ | V1 は一般(言語)のみ。バックアップ=V2+。情報=V2(小) |
| 03 L328「言語切替は UI 固定文言のみ。**タグ名・ビュー名・コレクション名・候補値は対象外**」 | ✅📋 | G5 の正否判定を明文支持(タグ名不変は正しい)。DF-3 の受入基準に引用 |
| ダークモード(03) | ✅ | §2.6 受入観点で「ダークテーマ不要」と明文化済み |
| トースト 3 種(03 L347) | 🔷 | V1 はステータスバー+ログ(§2.3)。P-04 プローブの観測対象として既登録 |
| destructive 操作の確認ダイアログ(03 L345) | ✅ | フォルダ削除・タグ削除・階層キャンセルに確認あり |
| RVP-UI-001 主画面=NewUI / RVP-UI-002 旧 UI=drop | ✅📋 | V1 タブ構成は NewUI 準拠(作業タブ除外は明文)。**G1① の教訓: トレース引用に新旧 UI 区分を必須化(P1-4)** |
| RVP-ENV-001 Windows 前提 | ✅ | チャーター(Windows のみ) |

## 8. 結論

- **新規 CR: 4 件**(CR-5 コレクション永続化 / CR-6 表示モード永続化 / CR-7 ビューア背景クリック / CR-8 コレクション項目の画像数表示)。いずれも小規模・表面のみ・原典挙動準拠。CR-5/CR-8 は CR-2 の実装詳細に統合
- **仕様文書補正: 1 件**(ビュー編集ダイアログの favorite を §2.6 へ追記 — 実装は既に正)
- **明示 DEFER: 動的ソート(01 Should)・view_revisions・サイドバー折りたたみ・pending 承認フロー(V2 裁定注意書き付き)・修復/類似/マージ/作業/バックアップ(チャーター済み)**
- **G1④型(明確な記述を仕様化で落とした)の追加脱落は CR-5/6/7/8 の 4 件で打ち止め**を確認(RVP 全行+02/03/06 を走査)
- 📋 メタ所見: porting-spec 03 L96/L117 は列数・選択規則を「現行互換でない/未固定」と**正しく注意喚起していた**。02§3〜4 の「設定できる」系はサービス能力と UI 露出が混在 — 今後のトレースでは 03(UI 仕様)と突き合わせて UI 露出を確認する
