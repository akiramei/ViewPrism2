# Change Order — ECO-012(画像タブ 残 UQ クローズ — 設計フェーズ完了)

> 画像タブ UI-IR/UI-BOM 抽出(`bomdd/ui/image-tab/`)の**残 UQ 全件**(I03/I06〜I15・DS1/DS2)を maintainer が裁定(2026-06-17)。根幹4決定(ECO-010/011)に続く**設計フェーズの締め**。大半は**モック確認/既存整合**で E-BOM 影響なし。実タッチポイント(I03/I06/I10/I13/I15)のみ E-BOM を同期する。
> **帰属: design_decision(確認含む)**。是正は v1.3 method どおり同期。本 ECO は **起票 + E-BOM(30)同期**段階。spec/M-BOM/Control Plan の全面同期と実装は**画像タブ製造フェーズ**。

## 0. 変更前 baseline
- As-Built: commit 2817162(ECO-011 適用後)。固定オラクル `tag:loop-v4-r1`(S-01〜S-31)不変。
- 本 ECO は **表示/設計契約のみ**(スキーマ・永続・固定オラクル不変)。

## 1. 変更要求
- ECO-ID: **ECO-012**
- 発生契機: 残 UQ(I03/I06〜I15・DS1/DS2)の裁定。設計フェーズのクローズ。
- 内容: 残 UQ の disposition 確定 + 実タッチポイントの E-BOM 同期。
- 種別: **設計決定/確認**。

## 2. disposition 一覧(13件)
### 決定(4件・maintainer 裁定)
| UQ | 決定 | E-BOM |
|---|---|---|
| **I03** コレクション行 | パスは **tooltip**(CR-8 踏襲・行は名前+件数に簡潔化=タグタブ UQ-E1 整合)+ サイドバー**折り畳み(276/64 rail)採用** | E-UI-SHELL-021 |
| **I06** 複数ビュー切替 | 表示軸セレクタが**ビュー管理のビュー一覧を展開**して選択(画像タブ自完結・E-VIEWSVC-009 消費)。単一ビューは直接入る | E-UI-GRID-022 |
| **I07** 整理/作業/⋯ | **プレースホルダとして残す**(挙動は製造後/V3 へ繰延。作業は V3 作業タブ関連) | なし(表示のみ) |
| **I15** 数値広範囲 | 狭い範囲(目安 ≤10・★評価等)は**範囲セル**、広い範囲は**ステッパ/数値入力へフォールバック**(閾値分岐) | E-UI-TAGASSIGN-029 |

### 確認(9件・モック/既存整合)
| UQ | 確認 | E-BOM |
|---|---|---|
| **I08** ⌘K 検索 | **不採用**(tag-tab UQ-E4 と整合) | なし |
| **I09** base/付与タグ | **区別なし**。画像のタグは全て付与(image_tags・E-TAGSVC-008)。ビュー所属は条件評価(E-EVAL-002/E-GRAPH-003)で算出・削除=付与解除。**モックの baseTags はプロト用ダミー** | なし(明確化) |
| **I10** FS チップ集計 | 現フォルダ**表示中の画像**に実在するタグを集計(現階層スコープ・再帰せず) | E-UI-GRID-022 注記 |
| **I11** 複数選択 共通タグ | 共通タグのみ表示・付与は全選択へ・削除も全選択から(REQ-027 原子バッチ) | 確認(整合) |
| **I12** グリッド選択視覚 | セル枠+チェック+リング(GF-02・行背景なし)/リスト=行淡青 | 確認(既存整合) |
| **I13** サムネタグドット | 閲覧時・左下・最大3(超過は先頭3+省略)。表示要素として正式化 | E-UI-GRID-022 注記 |
| **I14** ソート | フォルダ先頭固定+名前/更新日/サイズ・ソートなしで方向無効 | 確認(E-SORT-004) |
| **DS1** new 6部品 | 製造前に Components.axaml へ追加で合意 | design-system-bom.md |
| **DS2** extend 7部品 | 既存クラスに variant class を足す(構造が異なるもののみ新クラス) | design-system-bom.md |

- **影響なし予測(凍結)**: S-01〜S-31 不変。コレクションファースト(REQ-053)・タグビュー評価・GF-02 選択視覚・E-SORT-004 は不変(確認のみ)。

## 3. BOM 改訂(同期)
### 本 ECO で適用済(E-BOM = `30-ebom.yaml`)
- `E-UI-SHELL-021`: **(ECO-012/I03)** コレクションペイン折り畳み(276/64 rail)+ コレクション行はパス tooltip(行は名前+件数) invariant 追加。
- `E-UI-GRID-022`: **(ECO-012/I06)** 表示軸セレクタがビュー一覧を展開してタグビューを選択。**(ECO-012/I10・I13)** FS 軸チップは現階層表示中画像のタグ集計(再帰せず)・閲覧時グリッドのタグ色ドットは左下最大3。
- `E-UI-TAGASSIGN-029`: **(ECO-012/I15)** 数値インライン付与は狭範囲=セル・広範囲=ステッパ/数値入力フォールバック。
- bom_rev: v4.0 → **v4.0(eco:ECO-012)**。固定オラクル不変。

### 製造時同期(画像タブ製造で適用)
- `20-spec.md` / `32-mbom.yaml` / `33-control-plan.yaml`: 上記 + I07 プレースホルダ(整理/作業/⋯ は表示のみ・挙動 V3 繰延)・I08(⌘K 不採用)・I09(base/付与の区別なし=image_tags 一本)を反映。golden(CP-UI-G6/G7)に I11/I12/I13/I14 視覚項目。
- 固定オラクル: 追加なし。

## 4. 受入(計画 — 画像タブ製造時)
- I03: コレクション折り畳み 276/64・パス tooltip golden。I06: 軸セレクタのビュー一覧 golden。I15: 狭範囲セル/広範囲フォールバックの分岐 golden + 閾値 unit。
- I09: image_tags 一本(base/付与の区別なし)を unit で確認(削除=付与解除・カスケードは E-TAGSVC-008)。
- 回帰: S-01〜S-31 不変・GF-02/E-SORT-004/コレクションファースト不変。

## 5. provenance / lesson 連結
- 本 ECO で画像タブ**設計フェーズ完了**(根幹4決定=ECO-010/011 + 残 UQ クローズ=ECO-012)。全 UQ disposition は `bomdd/ui/image-tab/unresolved-questions.md` に確定。
- lesson: UI-IR 抽出で洗い出した UQ は「根幹の差し戻し(010/011)」と「確認/細部(012)」に層別でき、後者の多くは**モック確認 or 既存整合**で E-BOM 影響なし。差分帰属で層別すると、裁定コストを根幹へ集中できる(maintainer の判断を要したのは 13 件中 4 件)。
