# Change Order — ECO-056(staged): 整理トレイの v2 CAD 追随(3 ゾーン+マージ先解除+REQ-067 追記)+v1 以来の CAD 視覚/導線逸脱 6 点の是正

> ECO-055 golden 再確認時(2026-07-07)に maintainer が実機と v2 モックのスクリーンショット突合で報告した
> 8 所見の起票。診断の結果、8 所見は **v2 新設定義への追随 2 点**(計画済みの v2 追随 ECO の本体)と
> **v1 CAD の時点から定義されていたのに実装が逸脱していた 6 点**(ECO-014 製造ロット混入・
> ECO-050/055 同族)に分かれる。同一 surface(整理トレイ+検索結果ビュー+グリッドタイル)の同一
> golden 突合由来のため 1 ECO に束ねる(ECO-055 分離裁定の「順序実施=意味論先行」は ECO-055 fix
> 完了で満了 — 分離の根拠は消滅)。

## 1. 症状(2026-07-07・maintainer 実機×v2 モック突合 8 所見)

| # | 所見(モック → 実機) |
|---|---|
| 1 | モックはマージ先を解除(✕)できる → 実機はできない |
| 2 | モックは整理対象が増えても検索領域に影響しない → 実機はスクロールアウトする |
| 3 | 検索ボタンは「探す」 → 実機は「似た画像を探す」 |
| 4 | 条件 5 項目は 2 列グリッド配置 → 実機は WrapPanel(フロー) |
| 5 | 条件はチェックボックス行意匠(テーマ準拠) → 実機は素の ToggleButton(グレー) |
| 6 | タブは「条件検索」「類似画像検索」(この順) → 実機は「類似画像」「条件」(順・名称とも相違) |
| 7 | 検索結果ヘッダ「グリッドへ」でグリッドに戻れる → 実機は戻れない(整理終了以外に導線なし) |
| 8 | グリッドタイルはマージ先=チェック+バッジ・対象=チェック → 実機はマージ先=ラベルのみ・対象=チェックのみ |

## 2. 工程診断 — v2 追随 2 点+実装逸脱 6 点(CAD は全所見で健全・定義済み)

| # | CAD 定義 | 定義の起源 | 判定 |
|---|---|---|---|
| 1 | `clearDest`+散文「解除ボタン(v2)で指定を取り消せる」(image_tab.md L222/L353) | **v2 新設**(228081d・A-2 裁定済み) | v2 追随(REQ-067 追記も A-2 裁定済み) |
| 2 | 3 ゾーン固定レイアウト(散文 L206-209「内部スクロールは整理対象リストのみ」) | **v2 新設**(228081d・A-1 裁定済み) | v2 追随 |
| 3 | `runSearch` ボタンのラベルは「探す」 | **v1 から**(v2 同一) | 実装逸脱 |
| 4 | `grid-template-columns:1fr 1fr`(2 列グリッド) | **v1 から**(v2 同一) | 実装逸脱(ECO-055 notes で「配置は v2 ECO へ」と宣言済みの意図的暫定) |
| 5 | 条件行= 36px 行+チェックボックス+テーマ色(`o.rowStyle`/`o.boxStyle`) | **v1 から**(v2 同一) | 実装逸脱(同上・ECO-055 のチップ化は暫定形) |
| 6 | タブ「条件検索」「類似画像検索」(この順・この名称) | **v1 から**(v2 同一) | 実装逸脱(混入 `51ad8ee`) |
| 7 | 検索結果ヘッダに `backToGrid`「グリッドへ」(結果ビューからグリッドへ戻る導線) | **v1 から**(v2 同一) | **実装欠落(機能)**(混入 `51ad8ee`) |
| 8 | グリッドタイル= `it.selected` チェック+`it.isDest` バッジ(マージ先は両方) | **v1 から**(v2 同一) | 実装逸脱(混入 `51ad8ee`) |

- CAD(ViewPrismUI)は全所見で健全: v2 権威 `228081d`(2026-07-06 裁定 A-1〜A-4)+
  v1 から不変の定義群。**CAD 是正は不要**。
- E-BOM/spec: 3 ゾーンは spec の右ペイン共通クローム原則(「ヘッダ固定/本体スクロール/docked
  アクション常時可視」image_tab.md L85 参照)の整理トレイ実体化 — 実装がこの原則からも逸脱していた。
- 裁定は不要(A-1/A-2 は 2026-07-06 裁定済み・逸脱 6 点は CAD 権威確定済み)→ /eco-fix 可。

## 3. 切り分け済みの事実(確定と未検証を分離)

確定:

- 実装の現状(画像タブ・ImageTabView.axaml):
  - トレイ全体が単一 ScrollViewer(:456 まで)に入っており、マージ先も「マージを実行」(:452)も
    整理対象と一緒にスクロールアウトする(所見 2)。マージ先領域(:342-361)に解除導線なし・
    VM(ImageTabOrganizeViewModel)に ClearDest 相当なし(所見 1)。
  - タブ= segBtnText「類似画像」「条件」の順(:411-414・所見 6)。検索ボタン=「似た画像を探す」
    (:442・所見 3)。条件= WrapPanel+素の ToggleButton×5(:430-436・所見 4/5)。
  - 検索結果ビュー(:1070-)にヘッダなし=「グリッドへ」不在。`ShowSearchResults =
    _organizeMode && HasSearched && !OrganizeDone`(ImageTabViewModel:734)で、HasSearched を
    false に戻す導線が存在しない → グリッドへ戻る手段は整理終了のみ(所見 7)。
  - グリッドタイル= マージ先は cellTag ラベル「マージ先」のみ・整理対象は thumbCheck のみの排他
    (:873-879 コメントに「ECO-014: マージ先ラベル / 整理対象チェック(排他)」・所見 8)。
- 作業タブ(WorkTabView.axaml:761 ほか)も同型(CAD は「トレイの構造・操作は画像タブと同一部品」
  と明記 — work_tab.md L95)。両タブ対象。
- 混入と潜伏: 逸脱 6 点の混入は `51ad8ee`(整理モード製造②= M1+M2 surface・ECO-014 ロット。
  `git log -S '類似画像'` で特定)。ECO-050(しきい値)・ECO-055(条件欠落)と同一製造ロットの
  同族様式=「CAD 詳細(文言・順序・意匠・導線)が order に転記されず、検査面にも載らず潜伏」。
- マスキング: G-9/G-1 の golden 観点は機能中心(候補+一致率・マージ実行)で、v1 モックとの
  **詳細視覚突合(タブ名/順・ボタン文言・バッジ様式・戻る導線)が観点に不在** → 歴代 golden は
  外観大づかみで approved。今回 v2 モック受入(Phase 0/1)を経て maintainer がスクリーンショットを
  並べて突合したことで一括顕在化した。

未検証(fix 時に確定):

- 所見 8 の「一致」の細部: モックのマージ先タイルは selected チェック+isDest バッジの重畳だが、
  チェックの位置・バッジ文言(モックは「マージ先」相当)の最終形は v2 モック実装詳細を正として
  fix 時に確定。
- 「グリッドへ」で戻った後の検索結果の保持(再度結果へ行けるか、破棄か)— モックの状態遷移を
  fix 時に実測して従う。
- 検索結果ヘッダの構成要素(件数・検索方式ラベル)をどこまで持ち込むか — v2 モック基準で確定。

## 4. 是正方針(案 — 着手時確定)

- **レイアウト(所見 2)**: トレイを DockPanel 化 — マージ先=上部ピン・アクション(検索パネル+
  マージを実行)=下部ピン・整理対象リストのみ内部 ScrollViewer(spec 右ペイン共通クロームの実体化)。
- **マージ先解除(所見 1)**: VM に ClearDest(整理対象保持・実行不活性化)+XAML ✕ ボタン。
  **転送セッター/派生プロパティの全通知**(GF-055-01/ECO-038 教訓を先回りで適用)。
  REQ-067 に「マージ先の解除」を追記(A-2 裁定済み)。
- **視覚適合(所見 3/4/5/6/8)**: タブ名・順を「条件検索」「類似画像検索」へ、ボタン文言「探す」、
  条件を 2 列 Grid+チェックボックス行意匠へ、グリッドタイルのマージ先=チェック+バッジへ。
  すべて v2 モック(=v1 から不変の定義)基準。
- **戻る導線(所見 7)**: 検索結果に「グリッドへ」ヘッダ(HasSearched を戻す/結果保持はモック実測に
  従う)。両タブ。
- プローブ(R5): 機能系 2 点を是正前不合格で固定 — ①ClearDest 不在(マージ先解除の手段なし)
  ②検索結果からグリッドへ戻る導線なし(HasSearched を戻す public 経路の不在)。視覚系は golden 管轄
  (headless 視覚計測が可能な項目= タブ順・2 列 Grid は fact 化を着手時判断・ECO-040 基盤)。
- 受入: 両タブ対称の pin(共有部品化の範囲は着手時判断 — ECO-055 の OrganizeCriteria 先例)。
- 再発防止(クローズ時): CP-UI-G9/G1 へ「トレイ 3 ゾーン(スクロールアウト不可)・マージ先解除・
  結果から戻る・モック詳細突合(タブ名/文言/バッジ)」観点。

## 5. 影響 BOM

- impacted: M-UI-ORGANIZE-034(ImageTabOrganizeViewModel+ClearDest)/ M-UI-IMAGETAB-035
  (ImageTabView.axaml トレイ+検索結果+グリッドタイル・ImageTabViewModel)/ M-UI-WORKSPACE-029
  (WorkTabViewModel+WorkTabView.axaml 同型)。
- 台帳: 10-requirements REQ-067 追記(マージ先解除・A-2 裁定済み)。E-UI-MERGE-036 へ 3 ゾーン
  invariant の追記は着手時判断。
- 不変: Core・エンジン・オラクル(S-27/S-42)・DB・CAD(ViewPrismUI 無改変 — 全所見定義済み)。
- クローズ時: CP-UI-G9/CP-UI-G1 観点明記。

## 6. 残ゲート

1. ~~工程診断~~ → 完了(§2 — 裁定不要)
2. ~~/eco-fix eco-056~~ → 完了(§7)
3. **golden(maintainer 実機)**: §7 末尾の基準。
4. クローズ時: CP 観点明記+register applied+教訓。

関連: ECO-055(golden 再確認とは独立 — 本 ECO は基準 1 の合否に依存しない → 2026-07-07 基準 1 合格・ECO-055 は accept `714e7cf` でクローズ済み)。

## 7. 実施記録(2026-07-07 — 是正・機械受入完了・golden 待ち)

- **プローブ(R5)**: 新設 CpUi056OrganizeV2Tests 3 本(コマンドをリフレクションで解決=「導線の不在」
  そのものを実行時不合格にする形 — 最終形 API 直書きだと是正前コンパイル不能で実測が撮れないため)。
  是正前実測= **3 本すべて不合格・既存 565 全緑**: ①ClearMergeTargetCommand 不在(画像タブ)
  ②BackToGridCommand 不在(画像タブ) ③ClearMergeTargetCommand 不在(作業タブ)。
- **是正(VM)**: 子 VM(ImageTabOrganizeViewModel)に ClearMergeTarget(整理対象保持)/ClearTargets
  (すべて解除)/BackToGrid(_hasSearched=false のみ・**結果は保持**=モック実測どおり view 切替と観測等価)/
  ToggleSearchOpen(折りたたみ)+MergeButtonLabel v2 文言(N枚 → 1枚)+MergeBlockedNote+
  SearchMethodLabel。ホスト(ImageTabViewModel)にコマンド殻 4 種+転送プロパティ(**全通知=
  GF-055-01 教訓の先回り適用**)。WorkTabViewModel に同型を直実装(直バインド)。
- **是正(XAML 両タブ)**: トレイを **3 ゾーン DockPanel** へ再構成(上ピン=マージ先+整理対象ヘッダ/
  下ピン=検索折りたたみ+マージ実行+理由注記/中央=整理対象リストのみ ScrollViewer)。
  マージ先カードに解除 ✕+「この1枚が残ります」。タブ=「条件検索」「類似画像検索」(CAD の順・名称)・
  ボタン「探す」・条件= 2 列 Grid+チェック行意匠(condRow)。検索結果に「グリッドへ」ヘッダ
  (件数+方式ラベル)。グリッドタイル= 宛先も対象も「左上チェック+右上バッジ」の同型
  (宛先=緑 `OrganizeDestBrush #15A06B` 新設/対象=青 — モックの色意味論)。
- **スコープ判断の記録**: トレイ再構成部は v2 モック形へ全面追随(「すべて解除」・実行不可の理由注記・
  「(N枚 → 1枚)」文言・実行ボタン緑を含む — 所見 8 点の実現と同一部品の構成要素であり、golden は
  モック並置突合で測られるため部分追随は再所見を生むだけ)。**モック内語彙ゆれの記録**: グリッド
  バッジ・注記は「宛先」、トレイ見出し・結果ヘッダは「マージ先」— モックの表記をそのまま転写した。
  golden で違和感があれば CAD 側の裁定案件(下の基準 6 で明示確認)。
- **REQ-067 追記**(A-2 裁定 2026-07-06 の履行): 「マージ先の指定は解除できる(整理対象は保持・
  実行は不活性化)」を statement へ追記。
- **テスト対応表**: 新設 3(プローブ→合格転化・挙動 assert 込み: 解除=対象保持+実行/検索不活性+
  ホスト通知発火、戻る=結果保持+整理モード維持、作業タブ解除)・移行 1(CpUiG1OrganizeTests の
  MergeButtonLabel 文言 pin「(2 枚)」→「(3枚 → 1枚)」— 観点=対象数の反映は保存)・退役 0。
- **機械受入(4 点全緑)**: build 0/0・**Tests 568/568**(565+新設 3。プローブ合格転化)・
  **Oracle 109+2skip**(無改変)・validate_bom 0-0。
- diff: src 8(App.axaml ブラシ 1+Components スタイル群+VM 3+View 2)・tests 2(新設 1+移行 1)・
  bomdd 1(REQ-067)。Core・エンジン・オラクル・DB 無改変。

- **GF-056-01(golden 所見 2026-07-07 → 是正済み)**: 基準 3 が不合格(基準 1/2/4/5/6/7 は合格)。
  内訳 3 点= ①「条件検索」「類似画像検索」のタブ切替でパネル高さが変わる — モックは切替コンテンツを
  `height:150px` の**固定コンテナ**に収めており、初回転写で漏れた。是正= 両タブの切替部を
  `Panel Height="150"`(Top 揃え)で包む。プローブ= headless 実レイアウト実測(ECO-040 基盤)で
  searchPanel の Bounds.Height を両タブ実測 — **是正前 条件=274.0/類似=198.0 で不合格** → 是正後同一で
  合格転化(タブ切替の高さ不変 fact として恒久ガード)。②条件行の意匠が Fluent 標準 CheckBox のままで
  モックの 18px 角丸チェックと乖離 — 是正= 行ごと ToggleButton 化+自前 condBox(18px 角丸 5・
  OFF=白+灰枠/ON=青塗り+白チェック・checked 時 行が淡青ハイライト)。③スライダー下の目盛ラベル欠落 —
  是正= 「50%」「100%」(モック実測値 — 所見の「0%」はモックでは 50%)を追加。
  機械受入再走= build 0/0・**Tests 569/569**・Oracle 109+2skip・validate 0-0。

- **GF-056-02(golden 所見 2026-07-07 → 是正済み)**: GF-056-01 是正後の再確認で 2 点
  (基準 3 の高さ固定と目盛は合格)。
  ①**タブ切替で画像一覧がちらつく**(所見は「影響を与えるのか?」の疑義) — 診断= 影響を与えていた。
  ホスト SetSearchMethod が `Recompute()`= **Items 全再構築(Clear+Add)**を呼ぶ(`51ad8ee` 以来)。
  検索方式はグリッド内容と無関係= 過剰再構築。プローブ= タブ切替 2 回で Items.CollectionChanged が
  **4 回発火(是正前不合格)** → 全通知のみへ置換(WorkTab は元から名前列挙通知で非該当)して 0 回=
  合格転化(ちらつきの構造ごと pin)。
  ②**checked 条件行のラベルが白文字化し視認不能** — Fluent の `:checked` がテンプレート内
  ContentPresenter.Foreground を白にする(コントロール側の Foreground setter だけでは負ける)。
  プローブ= headless 実測でラベル実効 Foreground= **White(是正前不合格)** → ContentPresenter 側へ
  Foreground を明示(checked/checked:pointerover/checked:pressed)して合格転化。
  機械受入再走= build 0/0・**Tests 571/571**・Oracle 109+2skip・validate 0-0。

### golden 合格基準(gate② — maintainer 実機・v2 モックとの並置突合)

1. **3 ゾーン(所見 2)**: 整理モードで整理対象を大量(10 枚以上)に積む → マージ先カードと
   「マージを実行」ボタンはスクロールアウトせず常に見える。内部スクロールは整理対象リストのみ。
2. **マージ先解除(所見 1)**: マージ先カードの ✕ → マージ先のみ未設定に戻り(プロンプト表示)、
   整理対象はそのまま・「マージを実行」と「探す」は不活性化。
3. **検索まわり(所見 3/4/5/6)**: 「似た画像を探す」ヘッダで検索パネルが開閉。タブは
   「条件検索」「類似画像検索」の順。条件 5 種が 2 列グリッドのチェック行(ON で青ハイライト)。
   実行ボタンは「探す」。ECO-055 回帰= マージ先+条件 ON で「探す」が即活性。
4. **グリッドへ(所見 7)**: 検索実行 → 結果ヘッダの「グリッドへ」でグリッドに戻れる(整理モード維持・
   トレイ不変)。再度「探す」で結果へ。ヘッダに件数と方式ラベル。
5. **タイル(所見 8)**: マージ先タイル= 左上緑チェック+右上緑「宛先」バッジ+緑リング。
   整理対象タイル= 左上青チェック+右上青「対象」バッジ+青リング。
6. **語彙の確認**: グリッドバッジ「宛先」/トレイ「マージ先」の混在は v2 モックの転写 — この表記で
   よいか(NG なら CAD 側の是正を裁定)。
7. **回帰**: 作業タブの整理トレイも同型(1〜5 同様)・マージ実行 → 完了パネル → 取り消す(ECO-044)・
   類似検索(しきい値 70)・タグ編集/削除モードの排他が従来どおり。
