# Change Order — ECO-050(applied): 類似しきい値の既定値が仕様と不一致(整理トレイ 80/作業タブ 90 — 仕様既定 70)

> ECO-048 golden 実施時の R3 所見(51-cheat-log 2026-07-06)から昇格起票。maintainer 実機で
> 「規定値は 80」と認識された時点で顕在化(仕様の既定 70 がユーザー認知からも失われていた)。

## 1. 症状(maintainer 実機・2026-07-06)

- 画像タブ整理トレイの類似検索しきい値の既定が **80**(ECO-048 golden で実測)。
  仕様(REQ-064/065・§2.10.4)は**既定 70**・範囲 50〜100。
- 実害: 既定 80 のままでは、回転複製(スコア 70 台 — ECO-048)や異フォーマット複製
  (jpg×png=78 点 — 51 R3 #2 の経路系統誤差)が**既定で検出から漏れる**。70 へ下げれば拾える
  (maintainer 実機で確認済み)。

## 2. 工程診断 — 実装逸脱(仕様・E-BOM・CAD は健全)

| 工程 | 判定 | 根拠 |
|---|---|---|
| 仕様 | **健全・明確** | REQ-064「閾値(既定 70、範囲 50〜100、整数刻み)」・REQ-065/§2.10.4「閾値スライダー(50〜100・整数・既定 70)」 |
| CAD(ViewPrismUI) | 健全(数値は管轄外) | しきい値スライダーの存在のみ定義・数値既定なし(grep 実測)。数値は spec の管轄で spec は 70 |
| BOM | 健全 | E-UI-SIMILARITY-035 の requirement_refs=[REQ-065, REQ-064](30-ebom:581)。ECO-014 の Core 意味論不変宣言とも整合 |
| 実装 | **逸脱** | §3 — 整理トレイ 80・作業タブ 90(bare literal・裁定痕跡なし) |

- ECO-014 order(モーダル→整理トレイ置換)に数値既定の指定は**ない**(60-change-order-eco-014.md
  grep 実測: 「閾値スライダー」の存在のみ)= **工場の仮置きが仕様と突合されないまま混入**。

## 3. 切り分け済みの事実(確定と未検証を分離)

確定(コード読解・履歴):

- 実装の既定値は三者三様:
  - 独立モーダル SimilarSearchViewModel.cs:98 `_threshold = 70` = 仕様どおり
  - 整理トレイ ImageTabOrganizeViewModel.cs:33 `_similarThreshold = 80` = 逸脱
  - 作業タブ WorkTabViewModel.cs:75 `_similarThreshold = 90` = 逸脱(整理トレイ 80 とすら不一致)
- 混入コミット(`git log -S` で確定):
  - 80 = `0cf44ba`「整理モード製造①(M3-backbone)」(ECO-014 系・整理トレイ導入時)→
    `729039c`(ECO-036 切り出し)で ImageTabOrganizeViewModel へ移設・値は温存
  - 90 = `81500a8`「作業タブ(ECO-020/021 β)」= 整理トレイからの転写時に**さらにドリフト**
- マスキング(潜伏要因): 既定値は CP unit(エンジンは明示閾値で検査)にも golden チェックリスト
  (トレイの見た目・操作は G-9/G-1 で承認済みだが既定値は明示観点でない)にも載っておらず、
  機械・実機のどちらの検査面にもなかった。潜伏 = `0cf44ba` から本日まで。
- **追加所見(R3 候補)**: 仕様適合の 70 を持つ唯一の実装=独立モーダル
  (SimilarSearchViewModel/SimilarSearchWindow)は **WindowService.ShowSimilarSearchAsync の
  呼び出し元がゼロ=到達不能コード**(ECO-014 のモーダル→トレイ置換の残骸疑い)。
  本 ECO には混ぜない(51-cheat-log へ記録・撤去は ECO-024 型の別件)。

未検証(疑い):

- 作業タブの 90 に意図(スペース内は高精度で絞る等)があった可能性 — order/裁定記録に痕跡なし
  (bare literal・コメントなし)のため「意図なきドリフト」と判定しているが、maintainer の記憶に
  裁定があれば gate で申告を(その場合は spec 改訂=裁定案件に転じる)。

## 4. 是正方針(案 — 実装逸脱につき裁定不要・最小是正で着手可)

- **最小是正(推奨)**: 両 VM の既定値を 70 へ(literal 2 箇所)+ 既定値を pin する受入テスト
  (両 VM の初期 `SimilarThreshold == 70` = 仕様既定の unit 固定 — 今回の潜伏様式「既定値が
  どの検査面にもない」を直接塞ぐ)。範囲 50〜100・整数刻みは既存実装のまま。
- 根拠: 仕様が正(R2 上流優先)。かつ ECO-048 の実測が 70 の実用性を補強
  (回転複製 70 台・異フォーマット複製 78 点は既定 80 で漏れる)。
- 代替(採らない): 用途別既定(整理 80/作業 90)の正式化 — spec 改訂を要する裁定案件になるが、
  値に意図の痕跡がなく、漏れの実害が実測済みのため提案しない。
- プローブ(R5): 既定値 pin テストを先に追加し、**是正前に 80/90 で不合格**を実測してから 70 へ。

## 5. 影響 BOM

- impacted: M-UI-IMAGETAB-035(ImageTabOrganizeViewModel)/ M-UI-WORKSPACE-029(WorkTabViewModel)。
- 台帳改訂なし(spec/E-BOM/CAD は健全 — §2 診断どおり実装層に局在)。オラクル影響なし
  (エンジン・スコア意味論不変。UI 既定値のみ)。DB 不変。
- クローズ時: CP-UI-G9/CP-UI-G1 系に「既定しきい値=仕様値 70」観点を明記(再発防止)。

## 6. 残ゲート

1. ~~工程診断~~ → 完了(実装逸脱・**裁定不要**。ただし §3 末尾: 90 に裁定記憶があれば gate で申告)
2. ~~/eco-fix eco-050 — プローブ先行 → 最小是正 → 機械受入~~ → 完了(§7)
3. **golden(maintainer 実機)**: §7 末尾の基準。
4. クローズ時: CP 観点明記+register applied+教訓。

## 7. 実施記録(2026-07-06 — 最小是正・機械受入完了・golden 待ち)

- **プローブ先行(R5・実測裏取り)**: CpUi050ThresholdDefaultTests(既定値 pin・両 VM 構築直後の
  SimilarThreshold == 70)を先に追加し実行 — **画像タブ整理トレイ=不合格(80)・作業タブ=不合格(90)**
  を実測(既存 565 全緑)。潜伏様式「既定値がどの検査面にも載っていない」をこのテスト自体が恒久に塞ぐ。
- **是正**: 最小 diff — literal 2 箇所を 70 へ(ImageTabOrganizeViewModel.cs:33 /
  WorkTabViewModel.cs:75。コメントに ECO 番号+逸脱の由来を明記)。範囲 50〜100・クランプ・
  スライダー・エンジン意味論は不変。
- **機械受入(4 点全緑)**: build 0 error/0 warning・**Tests 567/567**(565+pin 2 —
  プローブ 2 件は合格に転化)・**Oracle 107+2skip**(不変・回帰ゼロ)・validate_bom 0-0。
- diff: src 2 行(+コメント)・tests 1 ファイル新設。台帳・DB・オラクル無改変。

### golden 合格基準(gate② — maintainer 実機)

1. 画像タブ 整理トレイ「似た画像を探す」を開く → **しきい値スライダーの初期値が 70**(従来 80)。
2. 作業タブの整理でも同様に**初期値 70**(従来 90)。
3. **既定のまま**検索して、回転複製(orientation_fixture_06_rot90.jpg — スコア 70 台)が結果に出る
   (ECO-048 では 80→70 へ手動で下げる必要があった操作が不要になる)。
4. 回帰: スライダーの可動域 50〜100・値変更後の検索が従来どおり。

## 8. クローズ(2026-07-06 golden 合格)

- **maintainer 実機確認(基準 1〜4 全 OK)**: ①整理トレイ初期値 70 ②作業タブ初期値 70
  ③既定のまま回転複製が検出(手動調整不要に)④可動域・変更後検索の回帰正常。
- **再発防止**: 機械= CpUi050ThresholdDefaultTests(両 VM の既定値 pin — 「既定値がどの検査面にも
  載っていない」潜伏様式を恒久封止)。golden= CP-UI-G9/CP-UI-G1 に初期値 70 の観点を潜伏実績つきで明記。
- **M4 同期**: 不要(実装層に局在・spec/E-BOM/CAD は元から健全 — ECO-038 前例)。
- **教訓(一般形・昇格候補)**: **仕様が数値を固定する箇所(既定値・上限・刻み)は「値そのもの」を
  unit で pin する** — 機能の存在・意味論は CP/golden の検査面にあっても、数値既定はどの面にもなく、
  工場仮置き(80)が混入し転写でさらにドリフト(90)した。転写時に正本(spec)と突合せず既存実装を
  写す複製様式は ECO-041(β-2 での同根転写)と同型の read-across。「仕様の数値」と「実装の数値」の
  一致は自明ではなく、pin テスト 1 行の恒久検査面に載せて初めて保存される。
